I'm continuing work on thegreenagents-studio (studio.thegreenagents.com). I've attached BLUEPRINT.md and a fresh repo zip.

Read BLUEPRINT.md start to finish before doing anything or touching any code. It's the project memory and carries the reasoning behind decisions, not just the what — respect the locked-in decisions and the working principles in it.

Start with the "What to do first in next chat" section at the top. Key context from the last session (2026-06-08 ninth session):

* Shipped #99 ("Remove from list" — soft-hide for Email Campaigns customer list). Three files: `server/db.js` (hidden_at additive migration), `server/routes/email.js` (filter in GET, auto-unhide in POST via case-insensitive name match, soft-hide in DELETE), `src/components/EmailSection.jsx` (red "Remove from list" button bottom-left of Edit Client modal + onDeleted wire-up). Confirmed working — operator verified `mail.idoyourquotes.com` removal. Soft-hide preserves all data; re-verifying the domain in AWS auto-unhides the row with full history (symmetric with #57's "AWS is source of truth for what's visible").

* #100 — SES SNS bounce/complaint wiring configured for `mail.engineersolutions.co.uk` end-to-end. Topics `bounces` + `complaints` already existed in AWS (created originally for Sendy — only Sendy subscribed). Studio HTTPS subscriptions added to both, auto-confirmed instantly. SES identity Notifications tab wired (Bounce → bounces topic + headers ON, Complaint → complaints topic + headers ON, Delivery deliberately left OFF). Test with `bounce@simulator.amazonses.com` landed in `email_sns_events` in <3s. **No code shipped for SNS — pure AWS configuration unlock.** Phase 2 handlers worked the moment SNS started delivering.

* **OPERATOR ACTION OUTSTANDING (top priority for next session if not already done):** repeat the SES Notifications wiring on the other 13 verified identities in eu-north-1 SES console. Same drill per domain: Identities → click domain → Notifications tab → Edit Feedback notifications → Bounce: bounces topic + headers ON, Complaint: complaints topic + headers ON, Delivery: leave No SNS Topic. ~90 seconds per domain. NO per-domain test needed — engineersolutions proved the wiring works. Until done, bounces on those 13 stay invisible to Studio. The full list of identities + the AWS console click path is in decision #100.

* #101 — three DEFERRED stubs locked in blueprint, don't pre-act unless I bring them up:
  - (a) **Reply-count over-counting in campaign report.** One-line SQL fix at `server/routes/email.js` around line 1609 needs `AND r.matched_campaign_id = es.campaign_id` added to the JOIN. Will REDUCE reported reply numbers visibly — that's the over-count being corrected. Safe one-file fix when picked up.
  - (b) **Click-tracking inflated by corporate mail security scanners.** Diagnosed via Wil@sinclair-intl.com 36-click case (mostly bot pre-fetches — multiple identical clicks within 1 second). Four mitigation options sketched (burst dedup recommended). Deferred until I surface again. If picked up, consider adding `ip_address` + `user_agent` columns to `email_link_clicks` first to future-proof.
  - (c) **Bounce-rate auto-pause campaign protection.** AWS already auto-pauses at ~10% but Studio could pause earlier. Prerequisite is #100's wiring done across all identities so calculation has no blind spots. All design questions still open (threshold, scope, window, recovery, alerting).

* **PARALLEL CONVERSATION STARTED THEN PARKED:** I want to move clearerpaths.co.uk + future TGA marketing domains to a SEPARATE AWS account for reputation isolation (so if I burn a domain it doesn't stop customer campaigns). Architectural plan agreed in the session — new "TGA Domains" sidebar entry above "Customers" under EMAIL CAMPAIGNS, add `aws_account` column to `email_clients` (default 'main' for every existing row, 'tga' for new ones), second set of `TGA_AWS_*` env vars in Render, parameterised loadAll per account, three-stage delivery (Stage 1 backend foundation no UI / Stage 2 UI + first TGA domain / Stage 3 reply polling + Mailboxes routing). **Not shipped — I paused this to fix a different bug first.** Pick up if I surface it again. Don't pre-build.

* Two lessons added: #78 (test sends bypass `email_sends`, so "test to bouncing address" only proves SNS delivery, not consequence-application — operator trusted SNS delivery as sufficient proof in this case); #79 (always probe what's already wired in AWS / Render / DB via screenshot or PRAGMA before prescribing — the AWS screenshot revealed Sendy had already created the SNS topics, saved a full topic-creation round).

* Carry-overs still open: (1) **#95 round-trip verification** from sixth session (Hot Prospects status/tag/viewed) — still pending; (2) **tracked write path verification (#87/#88)** — should self-confirm when Cube6's drip step 3 fires (watch for "of N tracked" sub-labels); (3) **broader email_replies backfill (#90)** — still optional, operator-deferred; (4) **Priority #0 Manson JSON-parse bug** — still operator-deprioritised; don't dive in unless I bring it up. If I do: full raw-response log from Render first.

* Decisions #76 through #101 are all confirmed working in production OR clearly noted as "configured" / "deferred" / "operator-action-outstanding" in the blueprint. Don't re-verify the confirmed ones.

How I work (also in the blueprint): I'm not a coder. I push files via GitHub Desktop and communicate with screenshots. Explain things in how-the-app-behaves terms, not code jargon. Parse-check every file before claiming it's done. Give me one file per location with explicit "this file goes here" paths. Honest pushback is expected and wanted — don't agree just to be agreeable, and don't relitigate decisions already locked in the blueprint. When I report a bug and I'm already on Render shell, ask me for the SQL diagnostic before writing any code (lesson #70). When I ask for design X, propose the best mechanism for the underlying goal with named alternatives BEFORE writing code (lesson #74). If a NOT-NULL-DEFAULT migration on an existing populated table is involved, surface the honesty think-through for what default existing rows should get (lesson #75). If I say "deploy failed" check what Render's dashboard actually shows before assuming the deploy ran (lesson #76). For any read-query that JOINs across many-table structures (subscribers→lists→email_clients, linked-set scoping, etc), smoke-test in-memory against the awkward customer shapes (Cube6 linked, Manson self-linked) BEFORE shipping — false-negative rendering is invisible to me (lesson #77). When checking my SQL output in Render shell, remember each meta-command (`.mode column`, `.headers on`) must be on its own line — I keep forgetting and pasting them together. When integrating with AWS / Render / a DB I've already touched, probe what's already there via screenshot or `PRAGMA` BEFORE prescribing actions (lesson #79).

No code jargon talk, discuss in terms of app flows, keep conversation short — short and sweet.

Ask me what I want to work on first before diving in. Natural choices are (a) if I haven't finished the SNS Notifications grind on the other 13 SES identities yet — that's the top hygiene priority, no code needed from you, just confirm progress and re-test if I want belt-and-braces. (b) ship the one-line reply-count SQL fix from #101a (small, safe, drops the over-counted numbers visibly). (c) pick up the AWS-account split (Stage 1 backend foundation — `aws_account` column, second SES service module, `GET /verified-domains?account=tga`, no UI yet). (d) verify the #95 round-trip from sixth session if still pending. (e) wait-and-watch for Cube6 step 3 to confirm the tracked write path. (f) something else I bring up.
