I'm continuing work on thegreenagents-studio (studio.thegreenagents.com). I've attached BLUEPRINT.md and a FRESH repo zip.

Read BLUEPRINT.md start to finish before doing anything or touching any code — start with the "⭐ NEXT CHAT — START HERE" block (currently the 2026-06-18 THIRTEENTH session) and the "⚠️⚠️ CURRENT TRUTH (#107)" block. It's the project memory and carries the reasoning behind decisions, not just the what — respect the locked-in decisions and don't relitigate them.

FIRST ACTION: the attached zip is the current truth — build on it, never an older version (the sandbox resets between sessions; an old zip silently rolls back shipped work — lesson #84). Many files carry shipped work; don't roll them back. The full lists are in the START HERE block's TWELFTH (Sales CRM) and THIRTEENTH "Files shipped this session" notes — recently touched include `db.js`, `index.js`, `email.js`, `portal.js`, `EmailSection.jsx`, `PortalApp.jsx`, `Sidebar.jsx`, `Dashboard.jsx`, `meta-api.js`, plus this session's `openai-image.js` (NEW), `gemini.js`, `openai.js`, `formspree-flagger.js`, `classify-replies.js`, `imap-poller.js`, `clients.js`, `campaigns.js`, `ClientDetail.jsx`, `CampaignProgress.jsx`, and all the CRM files.

Most recent session (2026-06-18, THIRTEENTH) shipped:
* gpt-image-2 "designed ad" image engine — per-client `image_engine` flag, piloting RGS only (everyone else stays Gemini). Hybrid: the model designs the ad with NO logo, Studio composites the real uploaded logo (guarantees logo fidelity). New `server/services/openai-image.js`; reads `OPENAI_AI_KEY`; needs a one-time OpenAI org verification (org TheGreenAgents).
* Brand kit panel on the client Brand tab (brand colours / logo description / type style / engine toggle).
* Run-campaign post-count dropdown (3/6/12/18/24, default 12) + "Add more posts" to a campaign that's in review (admin-only, only while awaiting approval; new posts avoid existing topics).
* Formspree lead detection rewritten to be body-based — any custom form subject (e.g. "Request a Quote") now flags as "Website Prospect"; product/verification mail still ignored. The "Website Prospect" badge is now ONE backend signal (`is_website_prospect`) so admin + portal can't drift.
* Out-of-Office now classified immediately at ingest (no waiting on the cron backlog). Badge renamed "Out of Office" everywhere user-facing. Palette: Out of Office → soft pink, Unclassified → dashed outline (the rest unchanged).
* RGS Meta setup (operator actions, no code): pixel 785231724583714 active and assigned; RGS ads on the shared account act_1754809155683350 temporarily because the Green Agents business hit Meta's new-business ad-account cap — migrate RGS to its own ad account once the cap lifts.

Open from this session (don't pre-act unless I raise it): delete the wrong RGS hot prospect "Formspree / noreply@formspree.io" (created by the manual Send-to-Hot-Prospects before the fix; optionally run `scripts/backfill-formspree-prospects.js` to add the real submitters); migrate RGS to a dedicated Meta ad account when the cap lifts; visitor-source ("how did they find us") needs a website/form change first; code text-overlay for ad-image spelling only if it ever regresses.

Still-open carry-overs from earlier sessions (don't pre-act unless I raise them): customer-portal CRM (final CRM phase — touches the sensitive PortalApp.jsx, mock first); Services profitability section; WorkTrackr ↔ IDYQ integration (separate repos — see WORKTRACKR_IDYQ_INTEGRATION.md); backend access-tightening on the older non-CRM routes; #100 SES SNS wiring on the other 13 verified identities (operator action, ~90s each); #101a one-line reply-count SQL fix; #101b scanner-vs-human click filter; #101c bounce-rate auto-pause; the AWS-account split for reputation isolation; the Manson JSON-parse bug (deprioritised).

How I work (also in the blueprint): I'm not a coder. I push whole files via GitHub Desktop and communicate with screenshots — give me ONE whole file per location with an explicit "this file goes here: <path>". Normal downloads are fine; no .txt copies. Explain things in how-the-app-behaves terms, not code jargon. Parse-check every file before claiming it's done; for any read-query that joins across the customer tables, smoke-test in-memory against the awkward shapes (Cube6 linked, Manson self-linked) before shipping (better-sqlite3 won't compile in-sandbox, so use Python sqlite3). Be careful editing PortalApp.jsx (sensitive — minimal changes, follow the existing nav/page wiring). Honest pushback is expected — don't agree just to be agreeable. When I ask for design X, propose the best mechanism + named alternatives (and mockups for screens) BEFORE writing code. Keep responses short and sweet.

Ask me what I want to work on first before diving in.
