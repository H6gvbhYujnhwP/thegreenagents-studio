I'm continuing work on thegreenagents-studio (studio.thegreenagents.com). I've attached BLUEPRINT.md and a FRESH repo zip.

Read BLUEPRINT.md start to finish before doing anything or touching any code — start with the "⭐ NEXT CHAT — START HERE" block (currently the 2026-06-25 FIFTEENTH session) and the "⚠️⚠️ CURRENT TRUTH (#107)" block. NOTE: #107 ("Facebook read-only") is SUPERSEDED — Facebook ad GENERATION came back into Studio (14th session) and the PUSH stage is now SHIPPED + LIVE (15th session). It's the project memory and carries the reasoning behind decisions, not just the what — respect the locked-in decisions and don't relitigate them.

FIRST ACTION: the attached zip is the current truth — build on it, never an older version (the sandbox resets between sessions; an old zip silently rolls back shipped work — lesson #84). Don't roll back shipped files — the full lists are in the START HERE "Files shipped this session" notes. Most recently touched (15th session): `server/db.js`, `server/services/meta-api.js`, `server/routes/facebook-ads.js`, `src/components/FacebookAds.jsx`.

Most recent session (2026-06-25, FIFTEENTH) shipped:
* Push approved Facebook ads → Facebook as PAUSED drafts (campaign→ad-set→ad, Leads objective, is_adset_budget_sharing_enabled=false, destination_type=ON_AD, daily budget, broad targeting, lead form attached). SAFETY RAIL: everything created PAUSED. Each pushed ad is stamped fb_ad_id so it's never re-pushed; result panel shows per-ad pushed/failed with the verbatim Meta error.
* CRITICAL FIX: ad set needs `destination_type=ON_AD` or Meta rejects a lead-form creative at the ad step. This is the fix that should clear the last push error once Meta's lead-gen ToS propagates.
* Setup-tab split: the Facebook screen now has Results · Ad approvals · Setup. Setup holds RAG + Logo + a NEW Default logo placement + Brand kit; Ad approvals is just Generate + ad cards + Push.
* Generate-only customers: "+ Add customer" can create a brand-new name-only customer, and the ad account is now OPTIONAL (generate ads, save image + copy text, never push). Used for one-offs like Sunbear Care.
* Generation status window: a staged progress window on Generate (indicative stages, not a live feed) that shows the FULL server error verbatim on failure.
* Meta-side (operator, no code): app taken LIVE (App ID 1899934957359420, business 27294175286874824); RGS Page (ID 1106760825860536) assigned to the Conversions API system user; lead-gen ToS accepted on the RGS + Green Agents pages; app Basic settings completed (1024×1024 icon, privacy/ToS URLs, data-deletion=instructions URL, bare app domain, website platform).
* SEPARATE website repo (green-agents-website): updated PrivacyPolicy.jsx with Meta Pixel / Lead Ads / data-deletion sections (for Meta app review). That file is NOT in this Studio repo.

Open from this session (don't pre-act unless I raise it):
* FINAL VERIFY the push end-to-end on RGS once Meta's lead-gen ToS propagates (~10 min) — and DELETE the empty shell campaigns ("RGS — Leads (Studio 2026-06-2x)") in Ads Manager first, since a re-push makes a fresh campaign.
* Lead-form dropdown auto-find does NOT work — the system-user token lacks `pages_manage_ads` (render log: "(#200) Requires pages_manage_ads permission"). Until the token is regenerated with pages_manage_ads + leads_retrieval, enter the form ID MANUALLY (RGS form ID 27472480415736929, an Active form, Sharing=Restricted). Restricted sharing will also block reading leads back into Studio later.
* Customer-portal Facebook approval flow (customers approve their own FB ads — mirrors the LinkedIn portal). Admin side done; portal side unbuilt.
* Option B: real per-ad streamed generation progress, if the indicative status window isn't enough. Deferred.

Still-open carry-overs from earlier sessions (don't pre-act unless I raise them): migrate RGS to a dedicated Meta ad account when Meta lifts the new-business cap (still on shared act_1754809155683350); delete the wrong RGS "Formspree / noreply@formspree.io" hot prospect (+ optional backfill); video ad creatives (image-only now); customer-portal CRM (touches sensitive PortalApp.jsx — mock first); Services profitability; WorkTrackr ↔ IDYQ integration; backend access-tightening on older non-CRM routes; #100 SES SNS wiring; #101a reply-count SQL fix; #101b scanner-vs-human click filter; #101c bounce-rate auto-pause; AWS-account split.

How I work (also in the blueprint): I'm not a coder. I push whole files via GitHub Desktop and communicate with screenshots — give me ONE whole file per location with an explicit "this file goes here: <path>". Normal downloads are fine; no .txt copies. Explain things in how-the-app-behaves terms, not code jargon. Parse-check every file before claiming it's done; for any read-query that joins across the customer tables, smoke-test in-memory against the awkward shapes (Cube6 linked, Manson self-linked) before shipping (better-sqlite3 won't compile in-sandbox, so use Python sqlite3). LinkedIn and Facebook are SEPARATE services — never share RAG/brand/clients between them. Be careful editing PortalApp.jsx (sensitive — minimal changes). Honest pushback is expected — don't agree just to be agreeable. When I ask for design X, propose the best mechanism + named alternatives (and mockups for screens) BEFORE writing code. Keep responses short and sweet.

Ask me what I want to work on first before diving in.
