# thegreenagents-studio — Blueprint

This document captures the complete architecture and current state of `studio.thegreenagents.com`. The next chat should read this start-to-finish before making any code changes.

---

## What this app is

A two-product platform for The Green Agents:

1. **Cold-outreach email** — multi-domain SES sending with per-domain inbox monitoring, reply triage, AI classification, auto-unsubscribe, scheduled drips, per-recipient personalisation.
2. **LinkedIn post generation** — Supergrow-integrated post generation with image creation. Posts go through a customer-approval flow, then push to Supergrow as live `queue_post`.

Both products share a database (better-sqlite3), an admin UI, and a per-customer portal at `/c/<slug>`.

**Stack:** Node.js + Express backend, React frontend, better-sqlite3 database, deployed on Render at `studio.thegreenagents.com`. Code lives in a GitHub repo the user pushes to via GitHub Desktop on Windows.

**User context:**
- The user (Westley/Wez) is non-technical for code but very technical for ops (AWS, DNS, Google Workspace).
- Sends emails as cold outreach for B2B services across his portfolio of company brands.
- Was previously using Sendy on the same AWS account; familiar with that workflow.
- Prefers plain-text instructions, no tree diagrams, no markdown when answering simple questions.
- Pushes code by Claude generating files → Wez downloads → drops into GitHub Desktop repo on Windows desktop → Render auto-deploys.
- Communicates by screenshots — Render logs, AWS console screenshots, error messages. Read carefully and engage with what's actually shown rather than guessing.
- **Values honest pushback over agreement.** When Wez proposes something risky or a pre-decision turns out wrong once code is in motion, surface it early. Don't sugar-coat. Don't silently work around bad assumptions.

---

## Architectural decisions you should not relitigate

1. **No SDK for SES sending** — we use raw HTTPS Signature V4 like Sendy does. The AWS SDK injects account-default config sets which we don't want. There's a Node-specific bug in the original signing chain (`digest('binary')` corrupts keys via UTF-16 round-trip) that's now fixed by keeping Buffers throughout.

2. **`X-SES-CONFIGURATION-SET: studio-no-tracking` header on every outbound** — env var `SES_CONFIGURATION_SET=studio-no-tracking` set in Render. Protects against future AWS account-level config-set defaults forcing tracking pixels.

3. **VDM Engagement Tracking is OFF** — manually disabled in AWS SES → Virtual Deliverability Manager → Settings. Don't re-enable it.

4. **MIME body uses base64 encoding, not quoted-printable** — quoted-printable corrupts URLs containing `=`. Stick with base64.

5. **Cold outreach defaults: tracking off, no visible unsubscribe footer.** The List-Unsubscribe MIME header alone is sufficient for compliance + Gmail/Outlook native unsub button.

6. **Auto-unsubscribe is aggressive and one-way for negatives.** No "snooze" fallback. Wez explicitly chose: unsub all negatives (both `hard_negative` AND `soft_negative`), period.

7. **Inbox model is FULL inbox view, not campaign-replies-only.** Phase 3.1.5 changed this. Classification (Phase 3.2) surfaces which ones are prospects.

8. **Drip ticker uses `Intl.DateTimeFormat` for timezone math, not a zone library** — works in Node 18+ with no external deps. DST handles itself.

9. **First-name parsing is rule-first with AI fallback, not always-AI** — keeps cost ~$0.01 per 500-name list instead of $0.15.

10. **Reply classifier uses three passes, fastest-first** — regex → heuristic → Haiku. Cost ~$0.001 per AI-classified reply.

### Customer-portal architecture (locked in earlier chats)

11. **Two parallel "client" tables exist by design** — `clients` (LinkedIn-side, Supergrow workspaces) and `email_clients` (email-side, send domains). They were built independently and DO NOT share IDs. The customer portal links them via `customer_services.linked_external_id`. **Don't try to merge them** — too much code reads from each, and the portal layer handles the join cleanly.

12. **Generic services model, not per-service columns.** Adding a new service (Facebook, SEO, Google Ads, anything) is `INSERT INTO services` — no schema change, no React changes, no service-specific code. Two tables drive the whole thing: `services` (catalogue) and `customer_services` (subscriptions). See "Customer portal — generic services design" below for full detail.

13. **`portal_enabled` flag separates "I have a portal" from "I'm a cold-email customer".** A customer like `mail.engineersolutions.co.uk` can be a cold-email customer (in `email_clients`) without having a portal. A customer like `Cube6` can have a portal without being a cold-email customer. They can also be both. The Customer Portal admin page filters by `portal_enabled = 1`.

14. **Self-links in `customer_services` are exempt from the cross-customer UNIQUE constraint.** The partial UNIQUE index has `WHERE linked_external_id IS NOT NULL AND email_client_id != linked_external_id`. Self-links represent "this customer's portal uses its own data" — they're a default state, not a cross-customer claim. Important: the original migration WITHOUT this exemption was deployed, and a fix was shipped that drops + recreates the index on every boot. Don't go back to the unconditional unique.

15. **Logo cross-sync is "option Z" — last write wins on both sides.** Uploading a logo on either the LinkedIn admin side OR the customer-portal admin side propagates to the other if a link exists. R2 keys differ (`logos/<id>/...` vs `logos/portal/<id>/...`) but both `logo_url` columns end up with the same URL.

16. **Slug auto-versioning on collision** — `tower-leasing` first, then `tower-leasing-2`, `-3`. Wez accepted that customer-facing URLs may end up looking versioned. **Don't try to "improve" this without re-asking.**

17. **Sessions are SQLite-backed, idle 7d / absolute 30d.** No in-memory Map (would die on Render restart).

18. **Customer "Approve all" → Supergrow uses live `queue_post`, NOT `create_post`/draft.** The customer's approval IS the green light. Differs from admin-side `deployToDrafts`.

19. **Admin sets initial portal passwords; portal shows "your password is temporary" banner on first sign-in.** No invite-link flow. Reset emails work the normal way for forgotten passwords later.

### Customer-portal final-mile decisions (locked in earlier chats)

20. **The customer portal mirrors the admin post-card layout exactly — image at natural aspect (no cropping), full body text with "Read full post" expand, content_pillar · format meta line, "📅 Day Time" schedule, four action buttons (Edit text / Rewrite post / New image / Approve).** The customer reviews exactly what'll be posted to LinkedIn — no truncated previews. Spinner overlays cover the card during text regen and sit over the image during image regen, with a "Please wait while your image/text is being regenerated" helper message.

21. **Regenerate is split into two backend routes — `/regenerate-text` (Anthropic only) and `/regenerate-image` (Gemini + R2 only).** Mirrors admin's "Rewrite post" / "New image" buttons. Combined 30/customer/day cap shared with the legacy combined `/regenerate` route — any text or image regen counts toward the same 30. The legacy combined route is kept in place but no longer wired to a UI.

22. **"View in Supergrow" link is hidden from customers everywhere.** Customers see the portal as a Green Agents product; they don't see the underlying Supergrow tool. Applies to both the live review grid and the read-only past-campaign history.

23. **Past campaigns history: ANY deployed campaign for this customer's linked LinkedIn client.** Specifically `stage IN ('deployed', 'done')` — meaning anything customer-approved via approve-all (`deployed`) AND anything admin pushed direct to Supergrow drafts before the portal existed (`done`). Read-only: same `PostCard` component as the live grid but with `readOnly` prop hiding the action row. Lives below the current-batch grid on the same LinkedIn Posts page (no separate "history" nav item).

24. **Customer portal sidebar uses identical colour to admin sidebar (`#0F6E56`).** TGA logo + tile + "The Green Agents / Studio" wordmark are visually identical at the top-left across both. Logo lives at `public/tga-logo.png` (Vite-bundled static asset, served from URL root).

25. **`generatePosts` runs without web search.** The web_search tool was the root cause of the 2026-05-07 Cube6 failure ("I need to conduct the required research before generating the posts" — Claude returned that as text and never produced JSON). The LinkedIn algorithm context now comes from the stored `algorithm_brief` (set by the admin "LinkedIn Algorithm" button). If the brief is null or older than 14 days, the campaign log shows a non-blocking warning. Both `generatePosts` and `regenerateSinglePost` also got an automatic JSON-parse retry — up to 2 attempts, with the second using an aggressive "JSON ONLY" override system message.

26. **Customer-portal reply send uses the mailbox the original arrived on.** Decision locked in chunk 3b-ii. The `email_inboxes.email_address` becomes the `From:` of the outbound. Maintains threading because (a) the recipient sees the same address that originally received them, (b) that mailbox is already SES-verified, (c) `In-Reply-To` and `References` headers wire the new message into the existing thread in Gmail/Outlook.

### Customer-portal further decisions (locked this chat)

27. **The legacy `'deployed'` campaign stage is dead — collapsed into `'done'` with a `deployed_by` column.** Earlier work set `stage='deployed'` on customer-portal approve-all, which the admin's CampaignProgress UI didn't recognise (its STAGES list goes `awaiting_approval → deploying → done`). Result: the admin opened a deployed campaign and saw a broken progress bar with active regen buttons that could overwrite live posts in Supergrow's queue. **Fix shipped:** customer approve-all now sets `stage='done', status='completed', deployed_by='portal'`. Admin deploy sets `deployed_by='admin'`. A one-shot migration in `db.js` converts any existing `'deployed'` rows to `'done'+deployed_by='portal'` on next boot. **Don't reintroduce a `'deployed'` stage value.**

28. **Admin regen/edit endpoints are stage-locked once a campaign is `done`.** `regenerate-image`, `regenerate-post`, and `edit-post` in `routes/campaigns.js` all return 400 if `stage === 'done'`. The admin UI also hides those buttons when `isDone`. Belt-and-braces: prevents stale browser tabs from blowing away posts already pushed to Supergrow.

29. **Admin sees "Customer approved" pill + read-only banner for portal-deployed campaigns.** Status pill in the campaign list reads "Customer approved" (blue) instead of "Deployed" (green) when `deployed_by='portal'`. Inside CampaignProgress, the header reads "✅ Customer approved & sent to Supergrow" and a blue banner says "All N posts pushed to Supergrow's scheduled queue. Editing and regeneration are locked because the posts are already live." The "Re-send all to Supergrow" button does NOT appear (re-sending live queued posts would duplicate them).

30. **The temporary-password banner on the customer portal is permanently removed.** Was a yellow strip at the top of the portal that nudged customers to change their initial password. Wez explicitly killed it — "I do not want this shown period." The `must_change_password` flag still flows from `/auth/check` (no DB change, no API change) but is no longer surfaced anywhere in the UI. Don't reintroduce.

31. **Customer-portal posts grid uses responsive auto-fill columns matching admin exactly.** Was hardcoded `'1fr 1fr'` (forced 2 columns regardless of viewport). Now `'repeat(auto-fill, minmax(340px, 1fr))'` with 16px gap — same rule the admin uses. On a wide screen this gives 4 columns; narrower viewports automatically reflow. Both the live review grid and the past-campaigns history expansion grid use the same value.

32. **Customer portal sidebar groups social services under "Social media" (not "Social posts").** The section was renamed to fit Facebook Pixels (a tracking service, not a posting one). All five items live there: LinkedIn Posts, Facebook Posts, Instagram, TikTok, Facebook Pixels.

33. **Three new social services seeded as `state='coming_soon'` in the catalogue: instagram, tiktok, facebook_pixels.** These join the existing `linkedin`, `facebook`, `email`. The admin Manage panel automatically shows them as "Coming soon" entries (no checkbox available). The customer portal automatically shows them in the sidebar with a sales pitch on click. Adding a new service in future is still a DB insert + frontend NavItem.

34. **`services.customer_pitch` column holds customer-facing sales copy.** Separate from the existing `description` column which is admin-facing operator text. Two audiences, two columns. Pitch text is re-applied on every boot via the seed block in `db.js` so wording tweaks ship via deploy with no manual DB work. Pitches landed for: facebook, instagram, tiktok, facebook_pixels, email. (linkedin pitch is null — that service is enabled for everyone who has it, never gated for sales.)

35. **`services` payload from `/api/portal/auth/check` now returns `{state, label, pitch}` per service, not bare strings.** Was `{linkedin: 'enabled'}` previously, now `{linkedin: {state: 'enabled', label: 'LinkedIn Posts', pitch: null}}`. All read sites in PortalApp.jsx updated to use `svc.linkedin?.state`. ServiceGate is defensive — handles either shape so a stale browser cache doesn't crash.

36. **Customer-side ServiceGate shows "Not Subscribed" + sales pitch — never "Coming soon".** Both `coming_soon` and `not_required` states render identically on the customer side. The customer doesn't need to know the DB-level distinction (which exists only to gate the admin's Manage panel checkbox). Pill says "Not Subscribed" in neutral grey. Body shows the service-specific sales pitch from `services.customer_pitch`.

37. **Admin sidebar has a single placeholder screen (`AdminComingSoon`) re-used for unbuilt services.** Four sidebar items — Facebook Posts, Instagram, TikTok, Facebook Pixels — all route to the same generic "Coming soon — Feature in development" component in `Dashboard.jsx`. Each has a "Soon" badge in the sidebar (admin needs to know which screens are real and which are placeholders). When a real admin tool ships for any of these, replace its branch in Dashboard's view router with a real component mount.

38. **(SUPERSEDED by #42 and #54.)** Originally specified auto-detect of logo background type (transparent vs white-bg vs dark-corner JPEG-as-PNG) with a different patch-colour decision per case. Both the auto-detect AND the per-case branching are gone — every logo now goes through the same upload-time trim pipeline (#54) and the same always-white compositor panel (#42). Kept here for history because earlier session lessons still reference it.

39. **(SUPERSEDED by #54.)** Originally specified that logo trim happens inline in the post compositor on every regen, branched on whether the source has alpha. The trim location moved to upload time in #54 — the file in R2 is now the canonical pre-trimmed form. The transparency branching logic itself is unchanged, just relocated to `services/logo-prep.js`. Kept here for history because earlier session lessons reference it; consult #54 for the live behaviour.

40. **IMAP poller crashes are logged, not fatal.** ImapFlow emits `error` events on the EventEmitter when sockets time out. Without listeners, Node treats them as unhandled and kills the entire process — taking the drip ticker and reply classifier with it. Fix shipped: `client.on('error', handler)` attached to every ImapFlow client at construction (both `pollOneInbox` and `testImapCredentials`). Belt-and-braces: top-level `process.on('uncaughtException')` and `process.on('unhandledRejection')` handlers in `server/index.js` log and continue rather than crashing.

### Customer-portal further decisions (locked in this chat — 2026-05-08)

41. **`email_clients.source` column distinguishes real email customers from portal anchor rows.** Three values: `'aws_domain'` (auto-pulled from AWS verified domains, or matched to one when manually added), `'manual'` (operator-created with a name that's not a verified SES domain — vanishingly rare), `'portal'` (auto-created as a portal anchor when a LinkedIn-only customer like Cube6 had a portal enabled — these rows have no email-sending purpose). The Email Campaigns Customers list filters `WHERE source IN ('aws_domain', 'manual')` so portal anchors don't pollute the list. Migration on first boot classified the 13 existing rows: any `portal_enabled=1` row with no email activity (no lists, subs, campaigns, or inboxes) → `'portal'`; everything else → `'aws_domain'`. Self-healing: if a portal-only customer ever becomes a real email-sending customer the activity check would flip them automatically — but this would be rare enough that the operator would just manually update the row.

42. **Logo compositor uses one unified path for every customer — always-white panel, no detection branching.** Earlier code tried to detect when a logo had its own white background (e.g. Tower Leasing) and skip the contrast panel, plus a separate dark-corners override for JPEG-as-PNG logos that forced white panel colour. Both branches are gone. **Every logo now goes through the same path: resize the (already-trimmed) canonical logo → place on a fully-opaque pure-white panel with rounded corners (`rgba(255,255,255,1.0)`) and proportional padding (20% of the logo's smaller dimension after resize, floor 8px).** White-bg logos blend into the panel — no harm. The image's bottom-right brightness is no longer sampled to decide patch colour either, because that heuristic produced dark panels on Sweetbyte's regen (orange-band image bottom-right → dark panel → dark-on-dark logo, illegible). Predictability beats cleverness. The Gemini fallback model order was also reordered so the working `gemini-2.5-flash-image` is tried first (~150ms saved per image). **Known limitation:** white-on-transparent logos (light wordmark designed for dark backgrounds) would render invisibly on the white panel — none of our current customers have one, but if a future customer does the answer is "ask them for a darker version", not "re-introduce detection branching". Two specifics later changed in the next session: (a) trim moved out of the compositor entirely — see #54 for upload-time trim; (b) padding became proportional rather than fixed-20 — see #55 for the trade-off across Tower's wide wordmark vs Cube6's compact logo.

43. **Phase 4 multi-step sequence editor is fully built, not "schema only".** The blueprint-as-of-2026-05-07 said "Sequence editor UI in CampaignModal — mockup first" was still missing. Wrong. The campaign modal has tab-strip UI ("1st contact / 2nd contact / 3rd contact / + Add contact") with per-step body editors and a delay-days input that shows on every step after the 1st. Steps are saved via `PUT /api/email/campaigns/:id/steps`; they're sent by the drip ticker which extends the candidate list with follow-ups whose previous step's send timestamp is `delay_days` ago. Up to 10 steps per campaign. Step 1's `html_body` is mirrored to `email_campaigns.html_body` for legacy code paths. Step removal warns if the step has already been sent to anyone.

44. **Subject line is per-step, not campaign-wide.** Each follow-up in a sequence can have its own subject — e.g. "Quick question, {{first_name}}" → "Re: my last note" → "One more thought". Stored as `email_campaign_steps.subject TEXT` (NULL means "fall back to the campaign-wide subject at send time"). Step 1's subject is required and is mirrored to `email_campaigns.subject` so test sends, previews, and the legacy single-step send path keep working unchanged. Steps 2+ subjects are optional. The `sendCampaign` function in `services/ses.js` accepts a `subjectOverride` parameter mirroring the existing `bodyOverride`; the drip ticker passes the step's subject through. The campaign modal moved the subject input out of the campaign-level header and into the per-step tab panel above the body editor. Step 1's tab labels it "Email subject *"; later tabs label it "Email subject for this contact (leave blank to reuse the 1st contact subject)".

45. **Rich-text editor defaults to Verdana 12pt; operator can change via toolbar.** Was Arial 14pt. Toolbar gained a font-family picker (8 email-safe fonts: Verdana, Arial, Helvetica, Tahoma, Trebuchet MS, Georgia, Times New Roman, Courier New). The font-size picker also got 10px and 11px added to the smaller end. The recipient-side CSS in `services/ses.js` `wrapBodyWithEmailCss` was updated to match — defaults to Verdana 12 — so the body of a new campaign authored in the editor renders identically in Gmail/Outlook. Existing campaigns that had inline `<span style="font-family:Arial; font-size:14px">` from previous authoring keep those styles (inline overrides the wrapper CSS), so no visual change to in-flight or already-sent drafts.

### Customer-portal further decisions (locked in this chat — 2026-05-08, second session)

46. **Customer-portal post action buttons are solid TGA-green with white text.** All four (Edit text / Rewrite post / New image / Approve) share one style now — solid `#0F6E56` background, white text, no border, 11px, weight 500, 7px vertical padding. Disabled state dims to 55% opacity. Approved state uses the lighter green tone (`TGA_GREEN_LO`) to show "✓ Approved" while staying on-brand. The operator explicitly chose "all four exactly the same" over giving Approve visual prominence. Trade-off accepted: customers may occasionally click a regen button thinking it's the confirm action. Blueprint should NOT relitigate this without re-asking.

47. **Three customer-portal post action endpoints had a number-vs-string ID bug latent since shipping.** Posts in `posts_json` have **numeric** id fields (Claude's generation prompt assigns `"id": 1, "id": 2, ...`). Express URL params are always strings. The original `findPostForPortalUser` lookup used strict equality (`p.id === postId`), which was always `1 === "1"` → false. Every per-post action route (Edit / Approve / Rewrite / New image) returned 404 "Post not found" for any campaign with numeric IDs. Cube6 didn't surface this because the customer skipped straight to Approve all (a different helper, `findCampaignForPortalUser`, which was correct). Tower Leasing was the first case that hit it. **Fix shipped:** `String(p.id) === String(postId)` plus a tolerance for the synthesised `post_<N>` form for posts that genuinely lack an id. Keep this string-coercion convention for any future code that compares post ids from URL params.

48. **`findPostForPortalUser` SELECT must include every column its callers use.** Same helper had a second bug discovered immediately after fixing the first — `SELECT id, stage, posts_json, created_at` omitted `client_id`. The regen routes then tried `campaign.client_id` to look up the LinkedIn `clients` row, found undefined, returned "Linked LinkedIn client not found". **Lesson, written down for future helpers: when a SQL helper returns a row to multiple downstream consumers, list every column the consumers need explicitly, or use `SELECT *`. Partial selects are a foot-gun across helper boundaries.** The companion helper `findCampaignForPortalUser` was already correct (it includes `client_id`).

49. **`email_clients.source` filter in admin routes hides portal-anchor rows from Email Campaigns customer list.** Decision #41 added the `source` column. The actual filtering ships in `routes/email.js` `GET /clients` with `WHERE source IS NULL OR source IN ('aws_domain', 'manual')`. The defensive `IS NULL` clause exists for race safety on a fresh deploy where the migration hasn't yet run on a row. Three real LinkedIn-only customers (Cube6, Mansons, Tower Leasing) now disappear from the Email Campaigns list while their portals continue to work. The classification on first boot was confirmed by a Render-shell diagnostic before code shipped — `portal=3, aws_domain=10` matched expectations exactly.

50. **Default-sender fields are inline-editable on the customer detail page header, not buried in the Edit Client modal.** Database columns `default_from_email` and `default_from_name` already existed pre-this-session — they auto-populated new campaigns and lists, but were only editable from inside the Edit Client modal. Now they live next to the Test send address on the customer detail header (`CampaignQueue` component), each with debounced 800ms auto-save to `PUT /api/email/clients/:id`. When the operator switches between customers in the left sidebar, all three fields re-sync from the new customer's row. New campaigns now also default Reply-to to `default_from_email` (was previously left blank) — operator confirmed reply-to should always equal the From address.

### Customer-portal further decisions (locked in this chat — 2026-05-08, third session)

51. **Inline-editable header fields update the parent's in-memory clients list, not just local state.** Decision #50 introduced the inline header fields with debounced auto-save, but the parent (`EmailSection`) held the customers array from a single `loadAll()` at page mount and never refreshed it after a save. Result: the operator typed values that DID save to the DB, but switching customers and back showed the old empty values (component remount re-initialised local state from the stale prop), and the New Campaign / List modals read the stale prop too — so neither auto-filled. Fix: the save handlers now read the response from `PUT /api/email/clients/:id` and call a new `updateClientLocally(updated)` helper on the parent which splices the updated fields into the in-memory `clients` array (preserving `list_count` / `subscriber_count` / `campaign_count` which the GET adds via subqueries but the PUT response doesn't include — explicit preserve so the sidebar list never flashes "undefined lists · undefined subs"). Same fix also narrowed the resync `useEffect` deps from `[id, test_email, default_from_name, default_from_email]` down to `[id]` only — otherwise updating the parent's array after a save would re-fire the effect mid-typing and clobber what the user was still entering. Header inputs also lost their placeholder text ("John Wicks", "hello@…") because operator wanted them genuinely blank when unset — placeholder was reading as a real saved value.

52. **Auto-unsubscribe gated on `matched_campaign_id`.** The classifier in `services/classify-replies.js` runs on every email in the customer's INBOX (so every row has a sensible badge), but the auto-unsubscribe path was firing on any `hard_negative`/`soft_negative` classification regardless of whether the inbound was a campaign reply. Random vendor mail with "remove me" footers (e.g. `cole@formspree.io`) was getting classified negative and auto-unsubscribing any subscriber on any of the customer's lists with a matching sender address — even though they'd never replied to a campaign. **Fix:** add `reply.matched_campaign_id &&` to the trigger condition in `applyClassification`. Lines up with the customer-portal "Normal email" badge logic (`PortalApp.jsx`'s `ClassifyBadge` already used `!reply.campaign_title` to neutralise the badge for non-campaign mail). If we can't tell which campaign a reply belongs to, we shouldn't act on its classification. Existing badly-unsubscribed rows from before this fix are NOT cleaned up — operator decided historical mess is fine.

53. **Gemini "BOTTOM RIGHT CORNER" prompt rule softened.** Earlier wording asked Gemini to "leave the bottom-right 320x140px area relatively clear (no text, no important design elements)" so our composited logo wouldn't cover important content. Side effect: Gemini interpreted the instruction as a positive design brief and rendered a visibly LIGHTER, cleaner-looking patch in that exact rectangle. On busy/dirty backgrounds (Cube6's factory-floor scene was the surfacing case) the lighter patch was clearly visible as a faint halo extending up and left of the actual white logo panel — looked like a "second white box" floating above the logo. **New wording:** `"Don't place text or the main subject in the bottom-right corner — a small logo will be added there in post-processing."` Drops the "320×140px clear area" framing that was producing the halo, keeps the protection against Gemini placing important content where the panel will land. The panel is opaque so anything underneath is hidden anyway — the only thing actually needed from Gemini is "don't put your main subject there."

54. **Logo trim moved from compositor to upload time.** Previously `compositeLogoBottomRight` in `services/gemini.js` ran Sharp's `.trim({ threshold: 30 })` inline on every regen. Sharp's trim is data-dependent — the same source file fetched twice could produce slightly different post-trim dimensions due to anti-aliasing, compression noise, or floating-point rounding. On Tower Leasing this surfaced as visible variance: Post 1's white logo panel was 320×116 pixels, Post 2's was 280×80, despite identical source logo and identical 1024×1024 underlying canvas. Diagnosed by Python script measuring the saved files. **Fix shipped:** new `services/logo-prep.js` module with `prepareLogoForStorage(buffer, mimetype)` that does the trim once at upload time. Same logic that used to live in the compositor — branch on alpha (transparent → keep alpha after trim; opaque → trim then `.flatten({ background: '#ffffff' })`), threshold 30, output as PNG (because trim can introduce alpha). SVGs pass through untouched (vector trim is meaningless). On trim failure (extreme edge case — entire image one colour) we fall back to the original buffer so the upload doesn't fail. Both upload routes (`routes/clients.js` POST `/:id/logo` and `routes/portal-admin.js` POST `/customers/:id/logo`) now call `prepareLogoForStorage` before the R2 PutObject. The shared `uploadLogoToR2` helper in `portal-admin.js` does it inline; `clients.js` mirrors the same pattern. The compositor in `gemini.js` no longer has any trim logic — it just resizes and places. Trim now happens once per upload, never per regen — every post gets identical bytes, identical dimensions, identical panel size. Two new DB columns track which rows have been processed: `clients.logo_processed_at TEXT` and `email_clients.logo_processed_at TEXT`. Both upload routes stamp this column on success. The cross-sync UPDATE that mirrors a LinkedIn-side logo across to linked email_clients rows now stamps the column on both sides simultaneously.

55. **Logo panel padding is proportional, not fixed-pixel.** First attempt after #54 bumped the compositor's PADDING constant from 20 to 40 because the operator wanted Tower Leasing's "Post 1" look (the bigger 320×116 panel) applied to every customer. That was a misread: the 320×116 size was just what 20px padding produced around Tower's natural ~280×76 wordmark — the variance was only between regens of the same customer, not between different customers. Bumping to 40 broke Cube6 — Cube6's compact logo + tagline composite is only ~100×100 after resize, so 40px on each side proportionally added an entire extra logo's width of empty white per axis. **Final answer: padding = 20% of the logo's smaller dimension after resize, with a floor of 8px.** Tower's ~280×76 wordmark gets ~15px padding (smaller dim = 76, 20% = 15); Cube6's ~100×100 logo gets ~20px padding. Different shapes, proportionally similar comfort. Floor of 8px guarantees breathing room for hypothetical tiny logos that resize to 30×30 or smaller. **Lesson:** when tuning a "looks right" pixel value across customers with different logo proportions, fixed pixels will always pull in the wrong direction for SOMEONE — proportional scaling is the only stable answer.

56. **Boot-time logo backfill in `services/logo-backfill.js`.** Every existing customer logo predates the trim-at-upload pipeline (#54), so they're sitting in R2 in their raw uploaded form. The backfill module walks every `clients` and `email_clients` row where `logo_url IS NOT NULL AND logo_processed_at IS NULL`, fetches the file from R2's public URL, runs it through `prepareLogoForStorage`, uploads the trimmed version to a NEW R2 key under the same prefix shape, and updates the DB row to point at the new URL with `logo_processed_at` stamped. The OLD raw file is left in place in R2 — we don't delete it, so a stale frontend tab loaded mid-deploy doesn't 404. The cross-sync between `clients` and any linked `email_clients` rows happens in the same pass, so processing a LinkedIn-side row also stamps + repoints any portal-mirror rows it's linked to. Idempotent: rows with `logo_processed_at` set are skipped, so re-running on subsequent boots is a no-op. Fire-and-forget: kicked off from `server/index.js` after `app.listen` returns, with a top-level catch so any failure logs but doesn't crash the process. Logs progress per-row as `[logo-backfill] clients[<id>] processed → <new url>` and a summary `Succeeded=N Failed=M Skipped(cross-synced)=K` at the end.

### Decisions from the 2026-05-12 chat session

57. **The Email Campaigns customer list is AWS-verified-domain-only. The `+ New client` button is removed.** Email customers are auto-pulled from the AWS SES verified-domains list by `loadAll()` in `EmailSection.jsx` every time the page opens — that's the only path to creating an email customer now. The `+ New client` button at the bottom of the customer-list sidebar (which routed to a manual-add modal) is gone. The empty-state fallback button (`Add your first email client`, shown when `clients.length===0`) is left in place but is unreachable on the live database which always has at least one customer. The existing `'manual'` source value on `email_clients.source` (decision #41) is now effectively dead — no code path produces it any more. Migration left in place because (a) historical rows from before this rule may exist and (b) the GET /clients filter `WHERE source IS NULL OR source IN ('aws_domain', 'manual')` keeps any legacy `'manual'` rows visible until manually reclassified. **Lesson driving this:** on 2026-05-12 a stray `'Mansons'` row was discovered in the Email Campaigns list — it had been created via `+ New client` early in the project, classified as `'aws_domain'` by the migration in db.js because it had a (single, empty) email_list attached to it (the migration's `hasActivity` check counts list rows, see decision #41). The list was never used for actual sending. A single Render-shell SQL update flipped `source` to `'portal'` and deleted the empty list; the row stayed (Mansons is a real LinkedIn-side portal customer that needs the `email_clients` anchor for its portal). With the button removed, that accidental-create path is closed. **Don't reintroduce a manual-add UI.** If a future customer genuinely needs to send from a non-AWS-verified domain, the answer is to add the domain to AWS SES first — at which point the auto-sync will pick it up. The architectural invariant is: **rows in the Email Campaigns customer list ↔ AWS SES verified domains, no other source.**

58. **Campaign log terminal no longer auto-scrolls the page.** The `useEffect` in `CampaignProgress.jsx` that called `logsEndRef.current?.scrollIntoView({ behavior: 'smooth' })` on every `logs` change was dragging the entire page to the bottom on every regen action. The original intent had been "keep the log terminal scrolled to its newest line during campaign generation" — but `scrollIntoView` walks UP the DOM and scrolls every scrollable ancestor, including the page, so the side effect was always present, just unnoticeable during initial generation when the user was already looking at the log. Spotted after the user clicked "New image" on a post and the page yanked to the bottom every time. **Fix:** the entire `useEffect` was removed along with the now-unused `logsEndRef` ref, the `useRef` import, and the dead marker `<div ref={logsEndRef}/>` at the bottom of the log terminal. A short comment was left in place explaining what was removed and why. The log terminal is now manually scrollable; new lines still appear, the page just doesn't move. Alternative considered: contain the scroll to the log terminal element only via `element.scrollTop = element.scrollHeight`. Not shipped because the user genuinely doesn't watch the log terminal during regen actions — they're watching the post card.

59. **IDYQ admin embed via signed-ticket bridge — reusable pattern for App integrations.** A new sidebar section `App` was added under the existing `Customer Portal` section, with one entry: `IDYQ` (the user's separate IdoYourQuotes product). Clicking it mounts a new component `src/components/apps/IDYQAdmin.jsx` inside the admin chrome. That component fetches a one-time URL from `GET /api/idyq-bridge/url` on the Studio backend; the backend mints a 60-second HMAC-SHA256-signed ticket `<expiry>.<nonce>.<sig>` using a shared secret `IDYQ_BRIDGE_SECRET`, returns a URL `https://idoyourquotes.com/admin-bridge?ticket=<signed>`. The component drops that URL into an iframe `src`. On the IDYQ side, a matching `/admin-bridge` endpoint verifies the ticket with the same secret (env-named `STUDIO_BRIDGE_SECRET` on IDYQ's side — same value, different env-var name to keep the directions clear), sets an IDYQ session cookie for a dedicated bridge admin user `studio-bridge@idoyourquotes.com` (created once via SQL on IDYQ's Render shell with a random bcrypt hash; this user can never log in via the IDYQ login form because no human knows the password), then 302-redirects to IDYQ's admin panel `/manage-7k9x2m4q8r`. End state: the iframe lands on IDYQ's admin panel already signed in, no double-login. **Architectural notes:** the shared secret + short-lived signed ticket is a clean, dependency-free pattern that can be reused for ANY future external admin embedded under the Studio sidebar (calendar, quoting, billing, etc.). Each integration gets its own secret pair, its own bridge admin user, its own `/admin-bridge` endpoint on the target side, and its own `/url` endpoint under `/api/<service>-bridge/` on Studio. The pattern handles three known failure modes with distinct visual signatures: (a) Studio chrome with yellow error card → Studio backend problem, most likely the secret env var isn't set; (b) blank iframe with no error → frame-blocking headers (X-Frame-Options, frame-ancestors CSP) on the target's edge — fix is one line of CSP add on the target side; (c) iframe loads to IDYQ's red Access Denied card → cookie didn't stick, either Safari/Chrome cookie partitioning (add `partitioned: true` to cookieOptions) or the bridge admin user was never created. IDYQ also explicitly chose to render an inline "Access Denied" card in its AdminPanel rather than redirect to login when session is missing — better failure mode for the embed than a blank screen. The integration shipped 2026-05-12 with the user confirming all three verification checks pre-deploy: no frame-blocking headers in IDYQ codebase, cookie is `SameSite=None; Secure` (matching every other IDYQ login path), and AdminPanel's behaviour on missing session is the inline card (no redirect).

60. **Admin reset-password supports admin-chosen passwords with random fallback.** Previously `POST /api/portal-admin/users/:id/reset-password` always generated a random temporary password via `genTempPassword()` and returned it to the admin. After a user request "I want the ability to choose the password," the endpoint now accepts an optional `{ new_password: string }` body field. If supplied, the admin-chosen value is used as the new password (validated server-side at `<8` chars matching `portal-auth.js`'s existing rule). If absent, the original random-generation behaviour fires. The response now includes a `kind: 'admin_chosen' | 'random'` field for any future consumer that wants to log the distinction (no UI uses it currently). The frontend in `PortalAdmin.jsx` `UserRow` now opens a new `ResetPasswordModal` on click (replacing the old `confirm()` dialog), with a password input + show/hide toggle, an inline "Generate random instead" button, Cancel, and primary Set password buttons. Enter submits, validation errors render inline (not via `alert()`). The existing `TempPasswordModal` is reused after success to show the new password once with copy buttons — same UX whether the password was admin-chosen or random-generated. The TempPasswordModal's "This is the only time you'll see this password" banner stays accurate in both cases: the password is bcrypt-hashed on save and there is no decrypt path. **Lesson driving this:** the user initially asked for a Render shell command to *reveal* an existing portal user's password — answered with the explanation that bcrypt hashing means the system literally cannot, plus the alternative "let me choose the password they get reset to" which became this feature. Worth recording: **customer-portal passwords are bcrypt-hashed and unrecoverable by design — never write a code path that stores or returns plaintext beyond the one-time return at creation/reset.** This is also a business asset, not just a security practice: being able to honestly say "we can't see your customers' passwords" is a stronger trust signal than "we try not to."

61. **"Refine my posts" — customer-facing post override rules with two-layer enforcement.** Customers were complaining that generated LinkedIn posts strayed onto topics they didn't want (e.g. a workshop with anxious clientele didn't want posts mentioning "stress"; an industrial customer didn't want references to machinery breakdowns). RAG-document edits weren't reliably preventing this — the LinkedIn algorithm research and the model's own judgment routinely overrode RAG nuance. **Feature shipped:** new `clients.content_rules TEXT` column (JSON-encoded array of `{id, text}`), new `Refine my posts` button in the portal header (next to the customer logo, only shown when LinkedIn is enabled), new modal `RefinePostsModal` in `PortalApp.jsx` with numbered list of rules — each row is an inline-editable textarea, delete per row, single Save button at the bottom. New endpoints `GET /api/portal/content-rules` and `PUT /api/portal/content-rules` on the portal side. Server-enforced limits: max 50 rules, max 500 chars per rule, dedup by trimmed-lowercase text, whitespace stripped. **AI enforcement is two-layer:** (a) **Prompt injection** — rules are rendered as a non-cached prompt block by `services/content-rules.js`'s `renderRulesPromptBlock(rules, { failedRules })`, inserted into both bulk generation and single-post regen prompts AFTER the cached RAG block but BEFORE the cached system prompt's continuation. The block is deliberately NOT wrapped in `cache_control: ephemeral` — this is the cache-invalidation fix for the user's explicit requirement "if they edit or delete a refine my post line then all future posts must obey by the delete/edit and not use cached info from before." Because the rules block is non-cached, every API call reads the latest `content_rules` from the client row and Anthropic re-tokenises it every time. (b) **Validation pass** — `validatePostAgainstRules(postText, rules)` in `services/content-rules.js` sends the generated post + rules to Claude Haiku 4.5 with a strict yes/no JSON-output prompt, returns `{ ok, violations: [{ruleId, ruleText, reason}] }`. Bulk generation validates all 12 posts in parallel; any failures get one retry via `regenerateSinglePost(post, client, { failedRules })` which calls out the violated rules specifically with extra emphasis. If the retry still fails, the post is kept with a `validation_warnings` array attached. Portal-side single-post regen (both `regenerate-text` and full `regenerate`) does the same validation but no retry (the customer can click Rewrite again themselves). **UI feedback** — `projectPost` in `routes/portal.js` exposes `validation_warnings` to the frontend. `PostCard` renders an amber card above the post body listing each violated rule and the AI's reason for flagging — so the customer can either edit their rules to clarify, or click Rewrite post to try again. Both decisions explicitly chosen by the user: (i) per-post regen picks up LATEST rules, not the rules at original generation time; (ii) flagged posts are visibly flagged so the customer knows what to amend. **Validation pass fails open** — any error from Haiku (network, parse, timeout) returns `{ ok: true, violations: [] }` rather than blocking the post. Validation is a safety net, not a hard gate. **Cost** — Haiku 4.5 at ~$1/MTok input / $5/MTok output, ~$0.0025 worst case per post = ~$0.030 per 12-post campaign. Negligible against the ~$0.50-1.50 a Sonnet generation costs. **Architectural notes for next chat:** the non-cached-prompt-block pattern (a separate user-message block with no `cache_control`) is the canonical way to inject content into a Claude API call that must NEVER be stale — keep this pattern in mind for any future feature where "the customer can change X and the change must apply immediately." The `MAX_VALIDATION_ATTEMPTS = 2` constant in `content-rules.js` is the cap on retries during bulk generation — raising it would multiply Sonnet token cost and is not recommended without strong evidence the second retry materially helps. The legacy `services/claude.js` is now dead code (no imports anywhere in the server) — `services/openai.js` is the sole generation path; leaving claude.js in place because changing dead code only introduces risk.

62. **Dark-background logo detection in `logo-prep.js` — escape hatch for non-light-background logos.** The trim pipeline (decision #54) assumes the logo's background is light/transparent so the corner pixel represents "background to crop." That assumption broke on The Manson Group's logo upload (2026-05-12): a JPEG with deep navy-blue background, white "THE MANSON" wordmark with subtle gradient/vignette. Sharp's `.trim({ threshold: 30 })` saw the dark corner, treated it as the reference colour, and the threshold-of-30 ended up matching irregular gradient pixels around the wordmark — cropping the "GROUP" subtitle off the bottom. Then `.flatten({ background: '#ffffff' })` painted what was left onto white, destroying the customer's branding. **Fix shipped:** new helper `medianCornerLuminance(buffer, meta)` samples the four corner pixels of opaque uploads (transparent PNGs are unaffected — their corners are by definition alpha, which is what the existing trim correctly handles), computes Rec. 601 luma per corner (0.299R + 0.587G + 0.114B), takes the MEDIAN of the four. If median luma < 120 (`DARK_BG_LUMINANCE_THRESHOLD`), the file is classified as dark-background and the trim path is SKIPPED ENTIRELY — the uploaded buffer is returned unchanged. Verified on the actual Manson file: top corners RGB(0,62,95)→luma 47, bottom corners RGB(0,42,66)→luma 32, median 39.7, well below 120. Light backgrounds read 220+; mid-grey reads ~128, so 120 cleanly separates "needs trim" from "leave alone." The decision log line `[logo-prep] Dark background detected (median corner luminance N < 120) — skipping trim, preserving original.` prints the exact median value so if a future logo lands in the 100-140 grey zone and gets the wrong classification, the right threshold is always inferable from Render logs. **Why median, not mean:** if a logo has three light corners and one dark corner (foreground element touching a corner), three-of-four lights should dominate the classification. Median-of-four = average of the two middle values, robust against a single odd corner. **Re-upload required after deploy:** the existing R2 file for any pre-fix dark-bg customer is already the wonky trimmed version. The fix prevents future damage but doesn't repair past damage — the customer logo must be re-uploaded via the admin Brand logo button to pick up the new code path. **Future enhancement (not shipped):** the compositor still places the (now untrimmed) dark-bg logo onto a WHITE panel in generated posts. If a customer specifically asks "I want my posts to use my dark brand colour for the panel instead of white," that's the "Option 3" change from the design discussion — bigger compositor change, deferred until a real complaint surfaces.

63. **Robust JSON parsing + automatic retry in `openai.js`.** A campaign generation for The Manson Group on 2026-05-12 failed with `Claude did not return valid JSON` despite the Render log showing `stop_reason: end_turn` (model thought it finished). Inspection of the response showed valid JSON with a stray non-JSON word — the literal string `Menu` — hallucinated between two key-value separators (`"angle": "...", Menu "buyer_segment": "..."`). One-off model anomaly; the JSON was otherwise complete. **Fix shipped:** three-layer defence at the module-scope helper `tryParseClaudeJSON(raw)` in `openai.js`. (1) Direct `JSON.parse`. (2) Greedy outer-object regex match `/\{[\s\S]*\}/` + parse. (3) **Repair pass** — strips stray non-JSON tokens via `match[0].replace(/,(\s*)[^"{}\[\],\s][^",{}\[\]]*?(\s+)"/g, ',$1$2"')`. The regex matches: between `,` and the next `"key":`, any non-quote/non-brace/non-bracket/non-comma token sequence, and replaces with the comma + whitespace + key opener. Deliberately conservative — only fires in JSON-structural positions where a key opener should follow a comma, so legitimate string values containing commas cannot be damaged. Critically, valid JSON parses on the first strategy so the repair never even sees its bytes — only broken JSON enters the repair path. Verified on the actual Manson failure case: the stray `Menu` is stripped and the JSON parses cleanly. Plus tested against safety cases (already-valid JSON, comma-inside-string, multiple stray tokens) — all behave correctly. **Plus automatic retry:** if all three parse strategies fail, the generation API call retries once with an appended user-message instruction: `IMPORTANT: your previous response was not valid JSON. Respond ONLY with the JSON object specified above. Do not include any other text, words, prose, or comments — not before the JSON, not after it, and not anywhere inside the JSON structure. Every character must be part of valid JSON.` Same cached context (RAG, system prompt, customer rules) — only the user-side instruction is strengthened. Most retry cases are transient model anomalies that don't repeat on a fresh call. **Plus full raw response logged on failure** — the previous error message only included `raw.slice(0, 300)`. The new code logs the FULL raw response (`console.warn` with `${raw.length} chars` prefix) every time parsing fails, so if a new anomaly type appears we can see exactly what the model produced and tune the repair regex. The same `tryParseClaudeJSON` helper is also used by `regenerateSinglePost` so per-post regen gets the same protection. The API-call helper `callClaudeForPosts(extraInstruction)` was extracted to allow clean second-attempt invocation without duplicating the streaming-with-web-search dance.

### Decisions from the 2026-05-19 chat session

64. **Per-customer Brand Panel (admin-side) — three new `clients` columns.** The Manson Group has a dark-blue logo with a built-in background, which fights the always-white panel mandated by decision #42. Rather than re-introduce auto-detection (explicitly rejected by #39/#42 — "predictability beats cleverness"), the panel is now **operator-controlled per customer**. Three additive columns on the `clients` table via boot migrations: `logo_position` (default `'bottom-right'`), `logo_panel` (default `'white'`), `logo_size` (default `'small'`). Every column defaults to the exact pre-existing behaviour, so every customer that existed before this migration is visually unchanged. The admin LinkedIn customer page ("Overview" tab, Supergrow card) gained a "Brand panel" section: three dropdowns (Position / Background / Size) with debounced 1-second auto-save and a "Saving… / ✓ Saved" indicator. `compositeLogo` in `gemini.js` reads the three settings off the client row: Position = 4 corners; Panel = white | none; Size = small (≈280×100) / medium (≈480×160) / large (≈640×220, "2× small"). The Gemini prompt's corner-clearance rule is keyed to `logo_position` so the model keeps the chosen corner clear. **This is a deliberate, operator-requested step OFF decision #42's "always-white panel, one unified path."** #42's reasoning (one input must not produce multiple outputs) still holds — the difference is the panel colour is now a stored per-customer setting, not a per-render heuristic, so it remains fully predictable: the same customer always gets the same panel. This is the deferred "dark-background logo" / "Option 3" work from decision #62's future-enhancement note and the blueprint §6.6 backlog item, picked up because a real customer (Manson) asked. Files: `server/db.js`, `server/routes/clients.js`, `server/services/gemini.js`, `src/components/ClientDetail.jsx`. **Operator-verified working** — earlier "it didn't save" reports were the operator navigating away before the 1-second debounce fired; the save itself is correct. Verification caveat recorded as lesson #62.

65. **Per-post logo controls (customer portal) — two-stage image generation + recomposite endpoint.** Building on #64, customers can now reposition/resize/re-panel the logo on an individual post WITHOUT regenerating the image (no Gemini call, no AI cost, ~½ second, does NOT count against the 30/day regen cap). To make this possible, `generateImage` in `gemini.js` was split into two stages: it now returns BOTH the pre-logo image (raw Gemini output, before any compositing) AND the final composited image. All four image-generation callers (`campaigns.js` ×2, `portal.js` ×2) upload the pre-logo bytes to R2 alongside the final image under a `-prelogo` key suffix; that upload is non-fatal if it fails. New endpoint `POST /api/portal/posts/:id/recomposite-logo` re-runs ONLY the compositor on the stored pre-logo image with per-post Position/Size/Background overrides. Returns `400 {error:'pre_logo_unavailable'}` for posts generated before this feature shipped (no stored pre-logo image — those posts must use "New image" to get one). `projectPost` exposes per-post overrides `logo_position`/`logo_size`/`logo_panel` (null = use customer default), `logo_controls_enabled` (bool), and the customer-level defaults `default_logo_position`/`default_logo_size`/`default_logo_panel`. Customer portal post card gained a 3-column dropdown row (Position / Size / Background) above the four green action buttons, dashed amber separator, "Logo on this image" label, disabled with a "Click New image to enable" hint when there's no stored pre-logo image. The image-spinner overlay is reused for the recomposite ("Updating logo… Repositioning logo — no charge."). **Every full regen path (campaign batch, single-post regen, legacy combined regen, admin per-post regen) resets all three per-post overrides to null** so a fresh image starts from the customer default, not a stale per-post override. Files: `server/services/gemini.js`, `server/routes/portal.js`, `server/routes/campaigns.js`, `src/components/customer-portal/PortalApp.jsx`. **Pending end-to-end verification** (see §"what to do first"): the recomposite flow start-to-finish, the disabled state on pre-deploy posts, the dropdowns initialising from customer-level defaults.

66. **Gemini prompt: explicit blanket ban on AI-drawn brand marks (customers WITH a logo).** Gemini was drawing a fake second brand mark (e.g. an invented "MANSON" wordmark bottom-right) that collided with the real composited logo, producing two logos per image. For every customer WITH a logo file the prompt rule was strengthened from "don't put text/subject in the [corner]" to an explicit blanket ban: do NOT include any logo, watermark, company name, brand signature, or text reading "[brand]" anywhere in the image — not in any corner, not as a decorative element, not as part of any sign, screen, label, or background. Customers WITHOUT a logo file keep the old "draw the brand name bottom-right" rule unchanged. File: `server/services/gemini.js`. **Gemini compliance is probabilistic** — if a fake mark still slips through occasionally that's a model quirk, not a rule error; a post-processing detection step would be the next-line defence (bigger build, not done). Does NOT fix existing posts — a baked-in fake mark requires a "New image" full regen to clear. **Pending verification:** confirm a freshly-generated Manson image has no AI-drawn fake brand mark.

67. **AI output sanitiser — strips stray non-Latin characters from generated post text.** A Manson post came back with stray CJK glyphs spliced mid-sentence (same family of model anomaly as the decision #63 stray-token bug — rare contamination of long structured output). New `sanitizeAiText()` helper in `openai.js` with a deliberately generous whitelist: tab/LF/CR, printable ASCII, Latin-1 Supplement + Latin Extended-A/B (é à ü ñ ç ø etc. for European customer/place names), a General Punctuation subset (em-dash, en-dash, ellipsis, curly quotes, bullet — Claude's normal style), and Currency Symbols (£ €). Everything else is stripped (CJK, Cyrillic, Arabic, Hebrew, Thai, zero-width chars, accidental emoji). Recurses through arrays and objects so a whole post object passes through. Logs a count + hex sample on strip (`[sanitize] stripped N non-Latin char(s) …`). Applied at three return points — `generatePosts`, `regenerateSinglePost`, `fixPost` — running AFTER the Haiku validation pass and just before return, so the customer never sees dirty text but validation still sees the original output. Smoke-tested with 11 cases (accented names / currency / curly quotes / ellipsis kept; Chinese / Cyrillic / Arabic / zero-width / emoji stripped) — all passed. Does NOT clean existing posts or operator/customer manual edits (only AI output going forward — manual edits are intentionally not sanitised). **Pending verification:** watch Render logs for `[sanitize]` lines after a few campaigns — frequent lines would mean the contamination is widespread and worth deeper investigation.

68. **Decision #61's customer-visible validation warning is HIDDEN but INTACT (dormant, not removed).** The yellow "This post may conflict with one of your rules" box in the customer portal is hidden via `{false && Array.isArray(post.validation_warnings) …}` in `PortalApp.jsx`. The render logic and a clear comment block are deliberately LEFT IN PLACE — re-enabling is a one-word change (delete the `false &&`). **The server-side validation pass STILL runs, STILL stores `validation_warnings`, STILL audits — only the UI surface is hidden.** Operator decision (2026-05-19), made with full pushback on record: the warnings were firing on ~90% of Manson posts because Haiku interprets rules thematically rather than literally, AND the Manson content strategy is inherently problem/pain-point driven so most posts legitimately mention a problem somewhere. So decision #61's customer-facing half is now dormant; its server-side half is unchanged. Treat #61 as "two-layer enforcement, customer-visible layer currently hidden by operator choice" — not "removed."

69. **Refine-my-posts modal: auto-growing rule textareas.** `RefineRuleRow`'s textarea now auto-grows to fit its full content (ref + useEffect: collapse to `auto` first so it can shrink, then set height to `scrollHeight`). `resize:'none'`, `overflow:'hidden'`, `rows={1}` baseline. `useRef` was added to the React import in `PortalApp.jsx`. This fixes a real bug, not just polish: long rules were clipping inside their own fixed-height textarea, so the rules-list container never overflowed and its existing `overflow:'auto'` scrollbar never engaged. With auto-grow, long rules push the list past the modal's 80vh and the single outer scrollbar engages correctly (one scrollbar for the whole list, not one per row). File: `src/components/customer-portal/PortalApp.jsx`. **Pending verification:** confirm the outer scrollbar appears with many rules and that long rules render in full.

70. **Admin Brand-panel dropdown label cleanup.** Dropped misleading sub-labels in `ClientDetail.jsx`: "No panel (logo directly on image)" → "No panel"; "Small (default)" → "Small". The "(default)" suffix was making the operator think it indicated the *current saved* value rather than the *factory default*. "Large (≈ 2× small)" was kept — it's genuinely informative about relative size, not misleading about saved state. File: `src/components/ClientDetail.jsx`.

### Decisions from the 2026-05-19 chat session (continued — Supergrow image bug + two-button deploy)

71. **Supergrow image attachment was silently broken since forever — fixed with the real media-upload flow.** `supergrow.js`'s `queuePost`/`createDraft` passed `image_urls: [...]` to Supergrow's `create_post`/`queue_post`. The confirmed Supergrow MCP tool schema (pulled live via `listTools` this session) has **no `image_urls` property** and sets `additionalProperties: false` — so Supergrow silently dropped the argument on every call. **Consequence: every post ever sent to Supergrow, on BOTH the admin draft path and the customer-portal approve path, arrived text-only — the generated image never attached.** Not introduced by any recent change; latent from the original integration. The earlier kanban screenshot (text-only cards) is consistent with this. **Fix shipped:** new `uploadImageToSupergrow()` helper in `supergrow.js` implementing Supergrow's real documented 3-step flow — (1) `create_media` returns a presigned S3/MinIO multipart-POST target `{ media_item:{id}, upload:{url,fields} }`; (2) POST the image bytes as `multipart/form-data` with every `upload.fields` entry first then the `file` field LAST (standard presigned-POST ordering, NOT a plain PUT); (3) `confirm_media` to make the `media_id` usable. The post is then created with `media_ids: [mediaId]`. `post.image_url` is a public R2 URL (`${R2_PUBLIC_URL}/${key}`) so the helper fetches those bytes directly. Node 22 has global `fetch`/`FormData`/`Blob` so NO new dependency (operator can't `npm install`). Image failure is non-fatal and loudly logged at every step (fetch, create_media, presigned POST status, confirm) — a null media_id means "post text-only" rather than blocking the post, but unlike the old silent bug it's visible in Render logs. **Signature change:** `queuePost`/`createDraft` params changed from `imageUrls: []` (array) to `imageUrl: null` (single string). All three callers updated: `campaigns.js` ×2 (main + retry in `deployToDrafts`), `portal.js` ×1 (approve-all). The `image_urls` at `campaigns.js:592` is NOT a caller — it's a human-readable JSON export artifact, deliberately left alone. **The false "approval_mode" comments were also corrected** — there is NO `approval_mode` argument anywhere in Supergrow's schema; `queue_post` auto-schedules by definition (per its own tool description: "Create a post and auto-schedule it in the next available time slot"), `create_post` makes a draft. **CRITICAL — NOT YET VERIFIED IN PRODUCTION.** The presigned-upload step was built exactly to Supergrow's documented spec but could not be tested from the build environment (no Supergrow credentials, sandbox can't reach mcp.supergrow.ai). Operator was asked to push these three files, run ONE normal admin deploy (still goes to Drafts — that path unchanged), and check whether posts in Supergrow's Drafts column now carry their images. If not, the per-step Render logs show exactly which step failed. Do NOT build the two-button feature (decision #72) until this is confirmed working — stacking auto-publish on an unverified image upload is the exact "built blind, broke in prod" failure this project's whole handoff exists to prevent (lesson #18, #40, #63).

72. **Two-button admin deploy — BUILT 2026-05-19 (after #71 confirmed working in production by operator).** Operator confirmed the #71 image fix works (posts now carry images in Supergrow), so this was built same session. **Shipped behaviour:** the single admin "Send drafts to Supergrow" button in `CampaignProgress.jsx` (shown at `stage='awaiting_approval'`) is replaced by TWO buttons. **Button 1 "Send to customer for approval"** → `POST /api/campaigns/:id/send-to-customer` (new route, `campaigns.js`): does NOT call Supergrow at all. The campaign is already visible in the customer portal at `awaiting_approval` (portal reads it directly). The route just stamps `campaigns.sent_to_customer_at` (new column, additive boot migration in `db.js` modelled on the `deployed_by` one) and leaves stage at `awaiting_approval` so the customer can still approve and Button 2 stays available. The admin UI switches to a "waiting on customer" banner when `sent_to_customer_at` is set. When the customer approves via the existing portal approve-all path, posts queue to Supergrow scheduled (with images, via #71). **Button 2 "Push to Supergrow scheduled"** → `POST /api/campaigns/:id/deploy` (same path kept so nothing else breaks; now calls new `deployToQueue` instead of `deployToDrafts`). `deployToQueue` mirrors `deployToDrafts` exactly (sequential to preserve post order, one retry per post, app_url written back, terminal `stage='done'`/`status='completed'`/`deployed_by='admin'`) but calls `queuePost` → Supergrow "scheduled" column → auto-publishes on Supergrow's calendar slots. Has a browser `confirm()` because it skips customer review. Customer sees it in their portal as completed history (same end state as portal approve-all). Button 2 stays available in the "waiting on customer" state too (operator pushes on customers' behalf — explicit operator choice). **`deployToDrafts` is intentionally KEPT IN PLACE, unused, NOT deleted** — documented fallback until `deployToQueue` is confirmed in production (same caution as #71; `create_post`/Drafts is now dead as a destination but the code stays one more cycle). The `/deploy` route now also hard-checks `client.supergrow_workspace_id`/`supergrow_api_key` before starting (Button 2 publishes for real, so fail fast). **Files shipped:** `server/routes/campaigns.js` (queuePost import, two routes, new `deployToQueue`), `server/db.js` (sent_to_customer_at migration), `src/components/CampaignProgress.jsx` (handleSendToCustomer, sentToCustomer flag, two-button + waiting-state banners — built to the approved mockup). All five touched files parse-checked clean. **Known limitation (unchanged, surface to customers later):** Supergrow's API has NO explicit post date/time — `queue_post` auto-assigns the next free calendar slot, so each post's `suggested_day`/`suggested_time` in the portal will NOT match actual publish time. Deferred content/UX decision: hide suggested-time for scheduled campaigns or add a "Supergrow decides timing" note. **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-19):** run a real campaign, click Button 2, confirm 12 posts land in Supergrow's Scheduled column with images and the campaign card shows the done/admin state; separately click Button 1 on another campaign and confirm the banner switches to "waiting on customer" with no Supergrow call, and that customer portal approve-all still works end-to-end.

73. **Admin post-card parity with the customer card — built 2026-05-19 (Path A).** Operator asked for the customer-portal per-post controls to exist on the admin review screen too. Decision: **Path A — visually identical card, admin keeps its own plumbing** (NOT a shared component; the blueprint repeatedly warns that refactoring `PortalApp.jsx` causes regressions — lessons #17/#19/#68, so the customer portal was left completely untouched). Per-post Approve was **deliberately excluded** (operator choice): the two-button deploy from #72 is the admin approval model; a per-post Approve on admin would create a competing/confusing second approval path. **What shipped:** (a) new admin endpoint `POST /api/campaigns/:id/recomposite-logo/:postIndex` in `campaigns.js` — an exact mirror of the customer-portal `/recomposite-logo` (decision #65): same Position/Size/Panel validation, same deployed-stage lock, same `pre_logo_unavailable` 400 for pre-feature posts, and it calls the SAME shared `recompositeLogoFromUrl` service in `gemini.js` (no duplicate compositor). Synchronous response returns the updated post (no SSE — recomposite is ~½ sec, no AI cost). (b) `CampaignProgress.jsx` admin post card rebuilt to match the customer card: three logo dropdowns (Position/Size/Background) in the amber-dashed "Logo on this image" section, greying out with the identical "click New image to enable" hint when `post.pre_logo_image_url` is absent; the old faint "New image" overlay button on the image corner was REMOVED and promoted to a proper labelled green button in the action row alongside "Edit text" and "Rewrite post" (now three buttons, no Approve). New `handleRecompositeLogo(postIndex, patch)` + `recompLogo` busy state; the image spinner now also covers recomposite ("Updating logo… Repositioning logo — no charge."). Dropdown initial values fall back post-override → customer-level default → hardcoded default, same precedence as the customer `LogoControls`. **Files shipped:** `server/routes/campaigns.js` (recompositeLogoFromUrl import, new endpoint), `src/components/CampaignProgress.jsx` (handler, busy state, logo dropdowns, restructured button row). Both parse-checked clean. Mockup shown and approved by operator before code. **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-19):** on a fresh campaign confirm the three dropdowns appear, changing one re-places the logo within ~1s with no AI cost, older posts show greyed dropdowns + the New-image hint, and the customer portal card is visually unchanged (Path A means it was not touched — confirm no regression there anyway).

74. **Failed campaigns now show their error reason on revisit — fixed 2026-05-19.** Operator asked: if post generation fails while they've navigated away, do they see the error when they come back? Investigation found: the error reason WAS already saved (`campaigns.error_log`) and a red "Campaign failed" banner already existed reading `campaign.error_log` — but `CampaignProgress.jsx`'s mount effect only populated campaign state from the SSE live feed, which for an already-finished/failed campaign can open-then-close with no `status` frame, leaving the banner blank or the screen stale. **Fix:** added an unconditional `fetch('/api/campaigns/:id')` on mount, independent of the SSE feed, that loads the saved campaign state (the GET endpoint already `SELECT *`s so `error_log` is included), sets it, and if `status==='failed'` pushes the `error_log` into the visible log terminal too. Now reopening a failed campaign always shows the actual failure reason, not just "failed". Frontend-only change, one file (`CampaignProgress.jsx`), no backend change (the data and banner already existed — only the load path was unreliable). Parse-checked clean. **Note for next chat:** this means the Manson JSON-parse failure (priority #0) will now surface its reason in the UI on revisit, not only in Render logs — but the underlying parse bug itself is still unfixed; this only improves *visibility* of failures, it does not fix any failure.
75. **Admin post-card logo dropdowns now reflect the operator's Brand-panel defaults — bug fixed 2026-05-19.** Operator reported (with screenshots): the Manson client's Brand panel is set to Position=Top right, Size=Medium, Background=No panel, but the admin post-card dropdowns showed Bottom right / Small / White panel — AND the dropdown was effectively stuck (couldn't pick "Bottom right" because it falsely already displayed it). **Root cause:** the admin frontend's dropdown value derivation (`post.logo_position || post.default_logo_position || 'bottom-right'`) was correct, but admin posts were served raw from `posts_json` and NEVER carried the `default_logo_*` fields. So the fallback always hit the hardcoded factory default. The customer portal was fine — its `projectPost` already injects `default_logo_position/_size/_panel` from the client row (decision #65, portal.js lines 137-139); decision #73 (admin parity) copied the frontend component but missed that the admin backend never supplies those fields. **Fix (backend only, `campaigns.js`):** two helpers — `withLogoDefaults(campaign)` (enriches every post in posts_json) and `postWithLogoDefaults(post, clientId)` (single post) — both read the client's `logo_position/size/panel` and attach them as `default_logo_*`, mirroring `projectPost` exactly. Applied at ALL SIX places the admin frontend receives posts: GET `/:id`, the SSE `status` snapshot on connect, both `post_updated` SSE events (image + text regen), and both single-post `res.json` returns (recomposite + regenerate-post). Per-post overrides still take precedence (same as customer side). Both helpers never throw — on any error they return the original unchanged (worst case = old factory-default behaviour, never a crash). The "stuck dropdown" was purely a symptom of the wrong default showing; once the correct default displays, `onChange` fires normally — no separate frontend change needed. Customer portal deliberately NOT touched (verified already-correct). Parse-checked clean. **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-19):** open a Manson campaign, confirm the three dropdowns now read Top right / Medium / No panel, and that changing one to any other value re-composites correctly.

### Decisions from the 2026-05-20 chat session (CRM Hot Prospects + inbox-modal UX fixes)

76. **CRM "Hot Prospects" feature shipped end-to-end. Per-customer scoping, NOT shared across admin/portal sides.** Operator asked for a new CRM section on both admin and customer portal with a "Hot Prospects" list that captures prospect details + email-thread history from inbox emails. Locked-in design after several rounds of pushback-and-clarify: **one list per customer** (Manson's list separate from Cube6's etc., admin switches between them via badges, customer portal only sees its own — matches the rest of the per-customer architecture). **No temperature levels / hot/warm tiers** — operator initially asked for levels then decided against ("they're already in Hot Prospects"). Each row stores prospect_email + prospect_name + optional follow_up_date + free-text notes + added_by + added_at. **Email-thread history is NOT stored on the row** — it's built live by joining `email_replies` (inbound from that address) and `email_outbound` (sent to that address) at read time, so new mail with the prospect auto-appears in the thread without any sync work. Operator explicitly chose this "auto-update and append to what is already in the list item" over a snapshot copy. **Repeat adds upsert, not duplicate** — same (customer, prospect_email) pair is a single row; the unique index on `hot_prospects(email_client_id, prospect_email)` is the hard guarantee, with an `INSERT ... ON CONFLICT DO UPDATE` upsert at the route layer. POST returns `was_new: true|false` so the frontend can render "✓ Added" vs "Already on the list" banners.

77. **Backend storage + endpoints — shipped in step 1 of the build.** New table `hot_prospects(id, email_client_id, prospect_email, prospect_name, follow_up_date, notes, added_by, added_at, updated_at)` with UNIQUE index on `(email_client_id, prospect_email)`, lookup indexes on `(email_client_id, added_at DESC)` and `(email_client_id, follow_up_date)` partial. Migration is purely additive at the end of `db.js`. New admin route file `server/routes/hot-prospects.js` mounted at `/api/email/hot-prospects` (BEFORE `/api/email` in `server/index.js` so the more specific path matches first; verified no catch-all in `email.js` could intercept). Six endpoints: `GET /` (list per customer), `GET /customers` (drives the admin customer-switcher — see #78), `POST /` (upsert, returns was_new), `PUT /:id` (update follow-up or notes), `DELETE /:id`, `GET /:id/thread`. Five mirror endpoints on `server/routes/portal.js` at `/api/portal/hot-prospects/*` for the customer-portal side (no `/customers` endpoint since single-tenant). `added_by` is `'admin'` for admin-side adds and `'portal:<client_user_id>'` for portal-side adds, auditable forever. `email_outbound` joins use `LOWER(to_address)` and `email_replies` join uses `LOWER(from_address)` since the prospect_email itself is stored lowercased — normalisation done at every read+write site. Migration smoke-tested against real SQLite via Python's sqlite3 (sandbox can't compile better-sqlite3 from source); confirmed insert + upsert + cross-customer-same-email scenarios all behave correctly. **`source_reply_id` is an optional POST body field** — when supplied, the endpoint lifts `from_name` off `email_replies` to auto-fill `prospect_name` if the caller didn't provide one. This is the ONLY auto-fill — phone numbers and other signature parsing was deliberately rejected (operator preferred "just copy the email thread contents" — the thread is the data).

78. **Admin CRM customer-switcher shows ALL portal-enabled customers (not just AWS-verified email senders).** The Email Campaigns customers list filters to `source IS NULL OR source IN ('aws_domain', 'manual')` (decision #57). The CRM's customer-switcher endpoint deliberately broadens that to `source IS NULL OR source IN ('aws_domain', 'manual') OR portal_enabled = 1` so LinkedIn-only-portal customers like Cube6/Mansons/Tower Leasing appear in the badges too. Reasoning: admin CRM is the operator's mission control across the whole customer roster — a LinkedIn-only customer can still have a mailbox connected (Rob at Cube6 has one) and prospects flagged on that mailbox should be visible to admin. Operator-confirmed 2026-05-20 (option B vs option A). This is a deliberate divergence from the Email Campaigns filter; the same `email_clients` table serves two audiences with different filters, same pattern as #57 documents.

79. **New admin CRM screen `src/components/CrmHotProspects.jsx` + sidebar entry.** New "CRM" sidebar section in `Sidebar.jsx` between Customer Portal and App, with one item "Hot Prospects" and a flame icon (hand-rolled single-path SVG matching the project's other sidebar icons). New view-router branch in `Dashboard.jsx`. The component is self-contained — own colour vars, own avatar helpers, own follow-up-date formatter with overdue/today/tomorrow colouring (red/amber/text), own modal shell. **Customer-switcher badges** are the top row: selected = solid TGA-green pill, others = white outlined; each shows initials + name + prospect-count. The selected customer id is persisted to `localStorage` under key `studio.crm.hot_prospects.last_customer_id` so the operator returns to the last-viewed customer on next visit. **Prospect list table** has columns Name (with avatar) / Email / Added (relative time) / Follow up (with the urgent colouring) / chevron. Click a row → detail modal with editable follow-up date (HTML date input + Clear button) and editable notes textarea — both auto-save with a 1-second debounce, status indicator shows Saving… / ✓ Saved / Save failed. Detail modal also shows the email-history thread, oldest-first, with inbound/outbound arrows + per-message timestamps. **Remove from list** button on the detail modal with a confirm() gate. **Critical detail on the auto-save debounce:** uses an `initialLoadRef` to skip the first effect run (which would otherwise no-op-save the values we just loaded into state from the fetch); also updates the local `data.prospect` snapshot after each save so the change-detect on the next keystroke compares against the saved value, not the original-load value.

80. **Admin Reply button in the inbox + admin-side `POST /api/email/replies/:id/send` endpoint.** The admin's `ReplyDetailModal` in `EmailSection.jsx` previously had no Reply button — the customer portal had one but admin didn't. Shipped a green primary "Reply" button as the leftmost action (matches customer-portal layout per decision #20's "admin mirrors customer" principle). Clicking it swaps the detail modal out for a new `AdminComposeReplyModal` (CC field optional, body textarea, Cancel returns to detail, Send sends + closes). The new admin endpoint mirrors the customer-portal one (`server/routes/portal.js` `/replies/:id/send`) exactly — resolves the reply + its mailbox via JOIN, builds RFC 5322 `In-Reply-To` + `References` headers, persists an `email_outbound` row up front (so the body is never lost even if SES fails), calls SES, stamps the MessageId on success or the error on failure. **Differences from the portal version:** no `client_user_id` (admin Bearer auth, no portal user — so `client_user_id` stays NULL), audit-log entry tagged `actor='admin'` not `'user'`, and from-name uses `email_clients.default_from_name` (falling back to the email_client's display name) so the recipient sees a consistent sender. Operator chose "from the customer's mailbox (same as customer portal does)" — keeps threading intact and the prospect sees a familiar address. State pattern: the swap-detail-for-compose state lives INSIDE `ReplyDetailModal` (a `composing` boolean) rather than being lifted to the parent. Smaller diff than the customer-portal pattern, keeps the change scoped — the parent state-lift pattern was explicitly chosen against here because the blueprint warns about big refactors of `EmailSection.jsx`.

81. **"Send to Hot Prospects" button + success banner + Open-in-CRM handoff.** Amber 🔥 button in the inbox detail modal, second-leftmost after Reply. POSTs to `/api/email/hot-prospects` with `email_client_id` + `prospect_email` + `source_reply_id`. Response carries `was_new` which drives the banner variant: green "✓ Added to Hot Prospects" or blue "Already on the list" — both include an underlined "Open in CRM →" link. **Cross-component navigation pattern (admin side):** the link writes the prospect's email_client_id to `studio.crm.hot_prospects.last_customer_id` localStorage, writes the prospect id to a NEW one-shot key `studio.crm.hot_prospects.open_prospect_id`, then dispatches a custom DOM event `studio:navigate` with `{detail:{view:'crm-hot-prospects'}}`. `Dashboard.jsx` has a `useEffect` listener that calls `handleNavigate(target)` on receiving the event. The CRM screen reads + immediately clears the one-shot key on mount, opening the matching prospect's detail modal. Cleanly de-couples EmailSection from Dashboard's view state — neither has to know about the other. Operator decisions baked in: "Already on the list" shows a blue banner (not silently succeed, not block) — informs the operator no duplicate was created. After a successful add the operator stays in the inbox modal (not auto-jumped to CRM) — preserves the triage flow; the "Open in CRM" link gives them the option without forcing it. **localStorage writes/reads are try/catch-wrapped** since private-browsing modes can throw; worst case the navigation still works but lands on the default customer instead of the right one.

82. **Inbox modal: action buttons pinned to the top, body scrolls below.** Operator reported (with screenshot of a long email) that the action buttons "get lost on big emails." Root cause: the shared `Modal` primitive in `EmailSection.jsx` puts ALL children inside one `overflow:auto` container, so the action row at the bottom scrolls off-screen with the body. **Fix:** the admin `ReplyDetailModal` and the customer-portal `ReplyDetailModal` BOTH stopped using their respective `Modal` primitives — each renders its own overlay + flex-column card with a **non-scrolling header** (close button + sender/subject/meta + ALL action buttons) and a **scrolling body** (classification reasoning, campaign context, Hot-Prospects banner, email body). Buttons always visible regardless of email length. The shared `Modal` primitives were left untouched because they're used by lots of other modals across the file and any change would risk regressions. The Hot-Prospects success banner moved INTO the scrolling body section (above the email content) since it relates to the customer's reading flow, not the action triggers. **Conditional buttons preserved unchanged:** Classify only shows if `!reply.classification`, Mark as handled only if `classification==='positive' && !handled_at`, Unsubscribe only if `!auto_unsubscribed`. The customer-portal version only has Reply + Send to Hot Prospects + Close (no classify/handle/unsubscribe since those are operator-only actions); same pinned-header restructure.

83. **Mailbox sidebar selection bug — closure capture in 30-second poller.** Operator reported: "the page refreshes and re-directs me to the domain at the top" — clicking a mailbox other than the top one and waiting 30 seconds would snap back to the first mailbox. **Root cause:** `useEffect(() => { loadInboxes(); const t = setInterval(loadInboxes, 30_000); return () => clearInterval(t); }, [])` with empty dep array captures the INITIAL `selectedId` (which is `null`) in the closure. Every 30 seconds the timer fires `loadInboxes`, and inside the function `if (!selectedId && d.length > 0) setSelectedId(d[0].id)` — but the `selectedId` it sees is the captured-null, not the current state. So it re-runs the "auto-select first" branch every poll. **Fix:** switched to the functional setter form: `setSelectedId(prev => prev || d[0].id)`. The `prev` closure-side variable is always the current value at setter-time, so the auto-select only fires when there's genuinely no selection. One-line change. Customer portal doesn't have multiple mailboxes so no equivalent bug there. **Lesson recorded:** when a `useEffect` interval reads state, always use the functional setter form OR include the state in the dep array (which restarts the timer); empty-array intervals are a known closure-capture trap.

84. **Customer-portal CRM mirror — `PortalApp.jsx` additions.** Same feature on the customer side, scoped to the signed-in customer's data (no customer-switcher). New "CRM" nav section in the portal sidebar with one item "Hot Prospects" (between Email and Account sections, not gated by ServiceGate since Hot Prospects is decoupled from email-service subscription state). New `PortalHotProspects` component + `PortalProspectList` + `PortalProspectRow` + `PortalProspectDetailModal` + `PortalProspectDetailBody` + `PortalThreadList` + `PortalThreadMessage` — six new helper components, all inline in `PortalApp.jsx` (single-file portal convention preserved). The customer-portal `ReplyDetailModal` gained the amber 🔥 Send to Hot Prospects button + the same success/already-on-list banner with Open-in-CRM. **Cross-component navigation pattern (portal side) is different from admin:** because everything lives inside one component tree (`PortalApp.jsx`), state is lifted to `PortalChrome` instead of using localStorage + custom events. New state `crmOpenProspectId` + a `navigateToCrmWithProspect(id)` function set the prospect id and flip `page` to `'hot_prospects'` in one go. `PortalHotProspects` accepts `autoOpenProspectId` + `onAutoOpenConsumed` props; the consume-and-clear happens via the callback. Cleaner than localStorage for in-tree navigation. **Wiring path:** `navigateToCrmWithProspect` → passed to `PortalInbox` as `onNavigateToCrm` → passed to `ReplyDetailModal` as `onNavigateToCrm` → invoked from the banner's Open-in-CRM link. Same auto-save / debounce / Remove behaviour as the admin detail modal. Avatar helpers + relative-time + follow-up colouring are duplicated in `PortalApp.jsx` (prefixed `portal*`) rather than imported from `CrmHotProspects.jsx` — keeps the portal self-contained, no cross-import to an admin component.

### Decisions from the 2026-05-20 chat session (continued — CRM linked-customers + follow-up alerts + Mark-as-converted)

85. **CRM hot-prospects expanded to the customer's linked email rows (read-side fix to a latent bug in #76).** Decision #76 keyed hot_prospects ownership to a single `email_client_id`, which broke for customers whose portal anchor is a different `email_clients` row from the row that owns their inbox (e.g. Cube 6's portal anchor row vs `mail.engineeringsolutions.co.uk` — joined via `customer_services` with `service_key='email'` and `linked_external_id`). Prospects flagged from a reply on the linked inbox row were saved against that row's id and the portal — which reads against the anchor's id — couldn't see them. **Fix shipped (decision-#46/#48 family — "a helper that doesn't read the column its callers need"):** new `resolveLinkedSet(emailClientId)` helper in `routes/hot-prospects.js` (mirrored as `_resolveProspectLinkedSet` in `routes/portal.js`) — returns the full set of email_clients ids that belong to the same real customer (anchor + linked inbox row, deduped, self-link exempt). Every hot-prospects read path that previously filtered `WHERE email_client_id = ?` now does `WHERE email_client_id IN (?, ?)` against the linked set. Five admin endpoints touched (GET list, GET customers, POST RETURNING extended, PUT, DELETE, GET thread) and five portal mirrors. **Admin switcher:** the `GET /customers` query now HIDES rows that are linked-TO by some portal customer's 'email' service (self-links exempt — same `email_client_id != linked_external_id` clause `db.js:1001` already uses), and the per-card `prospect_count` aggregates via correlated subquery across the linked set. Result: `mail.engineeringsolutions.co.uk` disappears from the switcher (its prospects roll into Cube 6's card). **Portal POST write path (Option B chosen by operator):** Rob writes against `resolveEmailClientId(clientId)` — the inbox-owning row — not the portal anchor. Matches admin write behaviour so the source-inbox label is meaningful on both sides. **POST response carries `crm_customer_id`** — the switcher-card id (anchor for linked customers, self id otherwise). `EmailSection.jsx`'s "Open in CRM" link uses this to set `last_customer_id` correctly — previously it set the source row's id and would land on a now-hidden card. **UI changes:** new "Inbox" column on the prospect list (between Email and Added), rendered ONLY when the customer's linked set has > 1 row — operator-chosen "Choice A — show only when meaningful" over the simpler "always show" alternative. New `has_linked_inboxes` boolean on the list response drives the column visibility; per-row `source_inbox_name` (looked up in one batched query, no N+1) drives each pill. Detail modal also shows a "From `<inbox>`" subtitle under the prospect email when has_linked_inboxes. Same UI on both portals. **No migration** — existing prospect rows on the linked inbox just become visible to the portal on next deploy. **Smoke-tested against real SQLite** before delivery (sandbox can't compile better-sqlite3 from source; Python's sqlite3 was used to confirm: cross-link customer's portal sees prospects from the linked row, self-linked customer's switcher card stays visible with correct count, resolveLinkedSet is symmetric from either direction). **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-20):** Cube 6's portal now shows prospects flagged from `mail.engineeringsolutions.co.uk`; admin switcher hides the domain card; Inbox column appears for Cube 6 but not Manson (single-inbox); single-customer flow (admin Send-to-Hot-Prospects → success banner → Open-in-CRM) lands on the right card. Files: `server/routes/hot-prospects.js`, `server/routes/portal.js`, `src/components/EmailSection.jsx`, `src/components/CrmHotProspects.jsx`, `src/components/customer-portal/PortalApp.jsx`.

86. **Follow-up alerts + "Mark as converted" — sidebar badge + top panel + closed_at state.** Operator asked for a way to be alerted when a follow-up date came due AND a way for the badge count to NOT increase forever (i.e. a definition of "done" for a prospect). Resolved together as one cohesive feature. **Two terminal-state design (Option 2 chosen):** new `hot_prospects.closed_at` (ISO datetime) + `closed_by` ('admin' or 'portal:<client_user_id>') columns via additive boot-migration ALTER pattern + partial index `idx_hot_prospects_closed_at ON (email_client_id, closed_at) WHERE closed_at IS NOT NULL`. NULL = active, populated = converted. Reversible: a separate `/reopen` endpoint clears both columns. **Sidebar badge (admin):** new `GET /api/email/hot-prospects/due-counts` returns `{ overdue, due_today, total }` aggregated across ALL customers (operator-chosen "total across roster, not per selected customer" — the badge is a "you have something to do somewhere" nudge; per-customer counts already exist on the switcher cards). Red-pill badge on the "Hot Prospects" SubItem in `Sidebar.jsx`; new `badgeUrgent` prop on `SubItem` toggles red instead of the existing green. 30-second poll + listens for an in-app event `studio:hot-prospects-changed` so the badge drops instantly when the CRM screen mutates anything. **Sidebar badge (portal):** mirror endpoint `GET /api/portal/hot-prospects/due-counts` scoped to the customer's linked set (single-tenant, no aggregation). `PortalChrome` polls + listens for the same event. Same red-pill UI on the "Hot Prospects" NavItem (new `badge` prop). **Top-of-CRM follow-up panel:** new `FollowUpPanel` component in `CrmHotProspects.jsx` (and `PortalFollowUpPanel` mirror in `PortalApp.jsx`) renders above the prospect list when total > 0. Red background if anything overdue, amber if only-today. Hidden entirely when total === 0 — zero visual weight when there's nothing to action. **List ordering moved to the server:** new `ORDER BY` with a CASE bucket — 0=overdue, 1=today, 2=other-active, 3=converted — then by follow_up_date ASC within the urgent buckets and added_at DESC otherwise. Replaces the old `ORDER BY added_at DESC`. Same SQL on both portal and admin endpoints (including search-filtered variants). **List divider + green pill:** frontend renders a "Converted (N)" pseudo-row between the last active row and the first converted row, detected by `prospects.findIndex(p => p.closed_at)`. Converted rows get a faint `#f9faf7` background tint + an inline green "✓ Converted" pill next to the prospect name (kept inline on the existing Name column rather than as a new Status column to avoid grid bloat when the Inbox column is also visible). **Detail modal — Mark/Reopen buttons + banner:** new green `✓ Mark as converted` button next to the existing red Remove. On click → POST `/:id/mark-converted` immediately (no confirm() — operator chose "no friction; reopen handles mis-clicks"). Modal swaps to a converted state: green banner at top showing `✓ Converted on <date+time> by <admin|customer>`, button replaced with outlined-green "Reopen as active". **Idempotency** baked into both ends: server uses `COALESCE(closed_at, ?)` so calling mark-converted on an already-closed row preserves the original timestamp + actor. **Customer-portal `closed_by` rendered as "you"** in the banner when the actor matches `portal:*`, "admin" otherwise — friendlier voice for self-actions. **prospect_count subquery on switcher** now also filters `hp.closed_at IS NULL` so the per-customer card badges drop when prospects are marked converted, matching the sidebar behaviour. **Cross-portal event dispatch:** the CRM screen's `refreshAll` (called after any mutation) dispatches `window.dispatchEvent(new CustomEvent('studio:hot-prospects-changed'))`. Both sidebars listen and refetch their due-counts immediately. Try/catch-wrapped — fails silently on browsers that block CustomEvent (none in production but defensive). **Files shipped:** `server/db.js` (closed_at + closed_by + partial index migration), `server/routes/hot-prospects.js` (mark-converted + reopen + due-counts endpoints, list ORDER BY, switcher count filter), `server/routes/portal.js` (mirror endpoints + ordering), `src/components/Sidebar.jsx` (badge state + polling + badgeUrgent prop), `src/components/CrmHotProspects.jsx` (FollowUpPanel + Converted divider + green pill + Mark/Reopen buttons + banner + dueCounts state), `src/components/customer-portal/PortalApp.jsx` (PortalChrome badge state + PortalFollowUpPanel + same list/modal additions + badge prop on NavItem). All six parse-checked clean; SQL smoke-tested against real SQLite (ordering matches expected urgency-then-converted sequence; due-counts drops correctly on mark-converted; COALESCE idempotency holds; reopen NULLs both columns). **Pending verification (operator to confirm post-deploy):** badge appears on both portals when prospects are due; top panel appears/hides correctly; converted divider appears at the right index; mark-as-converted updates the banner/button without page refresh; reopen reverses it; per-customer switcher count drops alongside the sidebar badge.

### Decisions from the 2026-05-20 chat session (continued — customer-portal Email Campaigns redesign, backend foundation)

87. **Customer-portal Email Campaigns redesign — BACKEND ONLY this session, frontend deferred to next.** Operator observed that the portal's current Email Campaigns view is information-thin (4 KPI tiles + one-row table with sent/opens/clicks/replies counts) — read-only by design (decision #36 family, no customer controls) but too sparse to communicate value. Designed a richer redesign over four mockup rounds and locked four UX choices: **(1)** clicking a campaign row expands an inline accordion panel below it (NOT a side drawer or full-page detail — operator picked Design 1; drawer/full-page rejected because Cube6 et al. have 1–5 campaigns each, so the drawer's "keep list visible while reading detail" benefit doesn't apply and the cramped 420px detail width hurts the email-preview rendering). **(2)** Tracking status shown BOTH as row-level pill ("Tracking: smart" / "off" / "full") AND per-stat denominator label ("50 / 120 tracked") — operator chose Both over either alone because customers need to see the campaign-level mode at-a-glance AND understand each rate's denominator. **(3)** Multi-step drip campaigns shown as tabs inside the expanded panel ("1st contact / 2nd contact / 3rd contact"), matching admin's pattern — operator chose tabs over nested-rows or grouped-rows because the admin already uses tabs and customers benefit from a familiar pattern. **(4)** Stats roll-up shown as BOTH combined headline (one Sent/Open/Reply set across the whole drip on the table row) AND per-step breakdown (in the expanded panel tabs) — operator chose Both because the table needs at-a-glance numbers AND the expanded panel needs the drill-down. Plus a dismissable amber explainer banner above the KPI tiles about what tracking means and why some emails aren't tracked (operator chose "always show + dismissable" over either-end alternative).

   **Critical honesty decision baked into the data model — per-recipient tracking flag.** The mockup shows "X tracked / Y untracked" per campaign. Pre-feature, the per-recipient tracking decision (computed by `shouldTrackRecipient()` in `services/touch-count.js` based on smart/off/all mode + touch count + always-warm list flag) was used to decide whether to inject the open pixel + click links at send time but was NOT stored anywhere — so "how many of these 369 sends got tracked" was unanswerable for any campaign without re-running the touch-count math against today's data (which gives different answers as time passes, because touch counts grow with every new send). Operator confirmed the honest choice: **add a new `email_sends.tracked` column (INTEGER DEFAULT 0)**, populate it at send time from the already-computed `trackThis` boolean, accept that historic campaigns will show `tracked=0` for all rows. To avoid the misleading "0 tracked" claim on a historic campaign that genuinely WAS sent with tracking on, the portal endpoint returns a `tracking_split_available` boolean — `false` when the campaign's `tracking_mode` is on but combined tracked count is 0 (pre-feature data), `true` otherwise. Frontend (next session) will hide the per-stat denominators on historic campaigns and just show the campaign-level "Tracking: smart" pill; new campaigns going forward get the full split. This is the same family as lesson #18 / #63 — measure before tuning, surface the honest answer rather than fabricate the missing data.

   **Three honesty caveats acknowledged by the frontend design.** The current data model does NOT support: (a) hard vs soft bounce split (only one `bounced_at` timestamp, no type column) — frontend shows a single "Bounces: N" number; (b) unsubscribe source breakdown (auto-detect vs link-click — `subscribers.unsubscribed_at` is a single timestamp, no source column) — frontend shows a single "Unsubs: N" number; (c) per-step reply attribution — `email_replies.matched_campaign_id` points at the campaign, not the specific step that triggered the reply — frontend shows reply count at campaign level only. The expanded-panel mockup originally showed all three; corrected before code so the build matches the data.

   **Backend changes shipped (three files):**

   - **`server/db.js`** — additive ALTER TABLE migration adding `tracked INTEGER DEFAULT 0` to `email_sends`. Slotted into the existing `if (!sendCols.includes('tracked'))` block alongside the `message_id` / `open_count` / `click_count` migrations (section "10a"). Defaults to 0 for all historic rows. **Migration ran clean in production on first boot** (operator-verified: log line `[db] migration: added tracked to email_sends` appeared once on Render).

   - **`server/services/ses.js`** — `insertSend` prepared statement now writes the `tracked` column (1 or 0) from the already-computed `trackThis` boolean. Two-line change: added `tracked` to the column list + value placeholder in the INSERT, added `trackThis ? 1 : 0` as the final arg to `insertSend.run(...)`. Drip ticker reuses `sendCampaign` so all send paths covered by this one change.

   - **`server/routes/portal.js`** — `GET /api/portal/campaigns` rewritten + new sibling `GET /api/portal/campaigns/:id`.

     The list endpoint now returns per campaign: identity (id, title, subject, started_at), customer-friendly status (`scheduled` / `sending` / `sent` / `failed` / new `in_progress` value for mid-drip), drip awareness (`is_drip`, `steps_total`, `steps_sent`, `drip_status` = `'completed'|'in_progress'|'not_started'`), tracking flags (`tracking_mode`, `tracking_split_available`), combined headline totals across all steps (`sent`, `tracked`, `untracked`, `opens`, `clicks`, `bounces`, `unsubs`, `replies`), AND a `steps[]` array with per-step rollup (no HTML body — that's in the detail endpoint, keeps the list response fast). Per-step rollup uses `GROUP BY campaign_id, step_number` on `email_sends` with `CASE WHEN status NOT IN ('failed','bounced')` for the sent count (consistent with the touch-count logic that excludes failed/bounced) and `tracked = 1` for the tracked count. The "in_progress" status is computed only when `is_drip && steps_sent < steps_total` — single-send campaigns never get this status. Pre-step-table campaigns get a synthesised single step from the campaign itself so the frontend always has at least one entry to render.

     The new detail endpoint at `GET /campaigns/:id` returns everything the list returns PLUS step HTML bodies (for the email preview pane), per-step `from_name`/`from_email` (so the recipient view matches what was actually sent), and the last 30 `email_replies` matched to this campaign with classification + a 160-char body snippet (newlines collapsed). 404 (not 403) on cross-tenant lookups, per the project's standing rule.

     **Tracking-split availability logic** (see header comment in the route): `tracking_split_available = (tracking_mode === 'off') || (combined.tracked > 0)`. When `false`, frontend should hide the X/Y denominators and just show the campaign-level mode pill — never claim "0 tracked" when tracking was actually on. Three test cases verified in Python's sqlite3 against the real query: historic-tracking-on-but-no-data → `false` (correct), new-campaign-with-real-data → `true`, tracking-off → `true` (because "0 tracked" IS the honest answer when tracking is off).

   **What did NOT ship this session:** the customer-portal frontend (`PortalApp.jsx`) is unchanged. The existing UI is backward-compatible with the new endpoint shape because all the new fields are additive — old fields (`sent`, `opens`, `clicks`, `bounces`, `unsubs`, `replies`, `status`, `started_at`) still present with the same names and types. Operator verified post-deploy: portal Campaigns view loads visually identically; DevTools Network response on `/api/portal/campaigns` includes the new `is_drip`/`steps_total`/`tracking_split_available`/`steps` fields. The actual UI rebuild (inline accordion + drip tabs + tracking pill + dismissable banner + in-progress amber pill + timeline strip showing sent/scheduled/upcoming step dates) is the next session's work — one file: `src/components/customer-portal/PortalApp.jsx`. The four mockups built during this session are the locked design — see "Pending verification" below for what to confirm before frontend.

   **Files shipped:** `server/db.js`, `server/services/ses.js`, `server/routes/portal.js`. All three parse-checked clean. SQL smoke-tested against real SQLite with both an in-progress drip campaign (3 steps, only step 1 sent, 60/40 tracked split, 5 bounced, 12 replies) and a historic single-send tracking-off campaign (369 sent, 0 tracked) — all per-step / combined / split-availability values correct. **DEPLOYED 2026-05-20; migration confirmed run; per-send `tracked` write path PENDING end-to-end verification with a real `tracking_mode='smart'` send.**

### Decisions from the 2026-05-20 chat session (continued — portal frontend + Formspree autoflag + reply-matching bugfix)

88. **Customer-portal Email Campaigns frontend rebuild — SHIPPED 2026-05-20 fourth session.** Decision #87's locked design implemented in one file: `src/components/customer-portal/PortalApp.jsx`. The `PortalCampaigns` function (~300 lines, replacing the previous 156-line read-only table) renders the full design: dismissable amber explainer banner (localStorage-backed dismiss state under `studio.portal.campaigns.banner_dismissed`), four KPI tiles (Total campaigns / Total sent / Replies received / In progress — fourth tile renamed from "Currently sending" so it captures mid-drip campaigns too), campaigns table with per-row Tracking pill (Off/Smart/All), Status pill including the amber `in_progress · N/M` pill for mid-drip, per-stat "of N tracked" denominator labels (hidden when `tracking_split_available=false`), inline accordion expand on row click (Design 1 — no drawer, no full-page), lazy-loaded detail panel via `GET /api/portal/campaigns/:id` cached per-campaign in component state, in-panel combined-stats strip (Sent/Opens/Clicks/Replies/Bounces/Unsubs), timeline strip with green-filled dots for sent steps + dashed-grey for upcoming steps + actual send dates or "+Nd after previous", drip step tabs ("1st contact / 2nd contact / 3rd contact" matching admin's pattern), per-tab subject/from/delay header + per-step stats grid + email body preview rendered as the recipient saw it (dangerouslySetInnerHTML from steps[].html_body, scoped via isolation:'isolate' wrapper), and a Recent Replies list (last 30, with subject/snippet/classification chip/relative time). **Honesty caveats honoured throughout:** single Bounces number (no hard/soft split), single Unsubscribes number (no source breakdown), reply count shown at campaign level only (no per-step attribution). All in one file per the project's standing rule against refactoring PortalApp.jsx into separate components (lessons #17/#19/#68). Parse-checked clean. **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-20):** Cube6's portal Campaigns page loads with the new look, the SAMPLE campaign expanding shows the in-progress amber pill correctly, timeline strip + drip tabs render with proper sent/upcoming distinction, email body preview displays. **Per-send `tracked` write path STILL pending verification** — Cube6's existing campaign sent before the migration so `tracked=0` on all 369 rows, which correctly suppresses the per-stat denominators via `tracking_split_available=false` (the honest behaviour). Once step 3 of Cube6's drip fires (~+2 days from step 2's 18 May send), the new rows will be written with `tracked=1` (when smart mode says they should be) and the "of N tracked" sublabels will appear on the row + per-step stats — that's the final confirmation point for the whole #87 backend write path.

89. **Formspree auto-flag to Hot Prospects — SHIPPED 2026-05-20 fourth session, KNOWN ISSUES PENDING POLISH.** Customer ask: form submissions landing in connected mailboxes (every customer has Formspree-backed website forms) should auto-create Hot Prospects rows so the customer + admin can act on them without clicking through. Studio-wide, auto-flag (not one-click), same priority as any other hot prospect. **Architecture:** new `server/services/formspree-flagger.js` module exposing `processFormspreeLead(parsed, emailClientId)` — pure function tested via 15-case logic sweep before shipping (catalogue download / website enquiry / Formspree newsletter ignored / random sender ignored / HTML body parsing / local-part name fallback / infrastructure-email filter / empty body / name-with-html stripping all pass). Detection: sender must match `@formspree\.io$` AND subject must contain a lead keyword (`catalogue download` / `website enquiry` / `contact form` / `form submission` / `new lead` / `new enquiry` etc — conservative list, extend as new templates surface). Newsletter subjects like "Enrich Your Leads with Formspree" fail the keyword test and get ignored. **Body parsing** — pass 1 looks for `label: value` pairs in the plain-text body with `EMAIL_LABEL_RE` and `NAME_LABEL_RE`, pass 2 falls back to "first email-shaped string in the body that isn't @formspree.io or noreply@anywhere." Name resolution: parsed name → local-part title-cased ("craig.willett@…" → "Craig Willett") → null. **Hook in `imap-poller.js`** — `processFormspreeLead` is called right after every `email_replies` INSERT, inside the same per-message try-catch so any throw is logged but never breaks the poller. **Upsert** mirrors the existing `/api/email/hot-prospects` POST logic exactly: `INSERT ON CONFLICT(email_client_id, prospect_email) DO UPDATE` with `COALESCE` on name so re-flagging doesn't overwrite richer manual data. `added_by='auto:formspree'` is a new actor value — the audit trail will distinguish auto-flagged rows from human-clicked ones. **Backfill script** `server/scripts/backfill-formspree-prospects.js` runs once on Render shell to sweep existing `email_replies` rows from @formspree.io and upsert them; idempotent (won't overwrite manual notes). **Files shipped:** `server/services/formspree-flagger.js` (new, 327 lines), `server/services/imap-poller.js` (hook added), `server/scripts/backfill-formspree-prospects.js` (new). All parse-checked. **Backfill run by operator 2026-05-20:** 28 @formspree.io rows scanned, 7 prospects added, 4 refreshed, 17 newsletter-style skipped, 0 no-email-found. **KNOWN PROBLEMS pending polish (operator surfaced):** (a) **5 junk rows landed in Hot Prospects** — `kevin@mail.edphandles.com` (own mailbox; the EDP website-enquiry form template uses `mailbox: kevin@...` to label the destination address, the parser's email-label regex matches "mailbox" loosely or the bare-email fallback grabs it), `admin@thegreenagents.com` (own admin), `kevinsmith@edpgroup.co.uk` (probably internal test), `sdfgsd@sdfg.com` (form spam test), `westley@sweetbyte.co.uk` (cross-client duplicate against the wrong email_client_id). (b) **Bare-email fallback is too permissive** — when no labelled `email:` field is found, we grab the first email-shaped string in the body, which on the EDP website-enquiry template grabs the customer's own mailbox address. The catalogue-download template (which has proper `name:` + `email:` + `_replyto:` labels) works correctly. (c) **The actual website-enquiry body format hasn't been seen yet** — we should pull a raw `body_text` of an EDP website-enquiry email and add labels for whatever fields Formspree uses on that template before the parser will work properly on it. **Pending polish (next session if operator asks):** drop the bare-email fallback entirely (require a properly-labelled email field — better to miss a row than add a wrong one), add an own-mailbox + own-domain filter (skip any prospect_email matching an `email_inboxes.email_address` row OR matching the customer's own SES verified domain — neither should ever be flagged as a prospect), supply cleanup SQL for the 5 existing junk rows.

90. **Reply-matching threading bug — FIXED 2026-05-20 fourth session, latent since Phase 3.1.** Operator surfaced via inbox screenshots: every "Automatic reply: SAMPLE" landed in Cube6's mailbox shows as **"Normal email"** in the customer portal (not Out of office), and the admin campaign report shows **0 replies** despite 10+ obvious auto-replies sitting in the inbox. **Root cause** (diagnosed via SQL diagnostic on Render shell, not guessed): inbound replies carry the full RFC 5322 Message-ID form `<0110019e44d3eec4-dd60a681-...-000000@eu-north-1.amazonses.com>`, but `email_sends.message_id` stores SES's local id WITHOUT the `@domain` suffix: `0110019e44d3eec4-dd60a681-...-000000`. The matcher in `imap-poller.js` was stripping angle brackets (`<>`) but never the `@domain` suffix, so the lookup against `email_sends WHERE message_id = ?` always failed. Diagnostic confirmed: 10 of 10 SAMPLE replies had `matched_camp = 0` despite both `in_reply_to` and `references_header` being PRESENT — pure data-shape mismatch on the lookup key. **Fix shipped:** new local helper `lookupSendByMessageId(rawId)` in `imap-poller.js` with three-shot lookup per id: (1) strip `<>` + split on `@` to get the prefix, try exact match — handles the common case; (2) try the original-stripped form as-is (defence in case any code path DOES store the `@domain` variant); (3) prefix `LIKE 'prefix%'` as defence-in-depth. Most-specific lookup wins. Backward-compatible — sends stored without the `@domain` (the current state) still match via path 1; if SES ever changes how it returns the id, path 2 or 3 will catch it. **Second fix in same round** — the sender-fallback block (catches replies where threading headers were stripped by gateways) was setting `matched_subscriber_id` but NOT `matched_campaign_id`. Result: even when we found a subscriber, the customer portal couldn't tell which campaign the reply belonged to, so it showed "Normal email" (because `ClassifyBadge` keys off `reply.campaign_title` derived from `matched_campaign_id`). Now stamps the subscriber's MOST RECENT campaign send as the match. **Backfill SQL** ran on Render shell to retroactively match existing 10 SAMPLE rows; one-shot UPDATE using the same `substr(... instr('@')...)` trick — confirmed all 10 rows now show `matched_camp = 1` with correct classifications attached (8 auto_reply, 2 soft_negative). **Files shipped:** `server/services/imap-poller.js` (matcher rewrite + sender-fallback fix). Parse-checked clean. **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-20):** SAMPLE campaign reply count went from 0 to 10 in admin view; customer portal Cube6 inbox now shows "OOO" badges on auto-replies (not "Normal email"); soft_negative replies (Allison Lynch, Andrew Hawkes) display correctly. **Critical lesson recorded:** the threading bug was latent since Phase 3.1 shipped — every campaign reply ever sent that came back as an auto-reply landed in `email_replies` but never matched its campaign, which means historical campaign reply counts across the entire deployment are understated. The backfill SQL covered SAMPLE/"your Services"; running the same pattern against ALL `email_replies WHERE in_reply_to IS NOT NULL AND matched_campaign_id IS NULL` would retroactively repair every campaign. Operator decision deferred: do the broader backfill if/when admin campaign reply counts come up as wrong-looking. The SQL is documented here for future use.

91. **Formspree classifier gate — SHIPPED 2026-05-20 fourth session.** Same root cause family as #89: the reply classifier in `services/classify-replies.js` was running Haiku on Formspree form-submission bodies and labelling them `soft_negative` or `hard_negative` (Haiku interpreting "submitted your form" / "looking for" kind of language as negative-sentiment outreach). The EDP Formspree test submission landed with classification=`hard_negative` in admin, despite being a customer's own website form. **Fix shipped:** pre-pass in the classifier's main loop — if `from_address` ends in `@formspree.io`, set classification to `neutral` immediately with reason "Formspree mail — not a campaign reply (skipped classification)" and skip the regex/heuristic/AI passes. Counts as a regex-tier bypass for stats. **Why this matters beyond cosmetics:** the auto-unsubscribe gate (decision #52) requires `matched_campaign_id` to fire, which Formspree mail won't have — so an inappropriate auto-unsub wasn't actually a risk. But a future Formspree email accidentally getting attached to a campaign (e.g. via the sender-fallback in the poller if Formspree's noreply address ever ended up on a subscriber list) could have. The classifier gate is belt-and-braces. **Cleanup SQL** also ran on Render shell to fix the 1 pre-fix EDP Formspree row that was already classified `hard_negative`: `UPDATE email_replies SET classification='neutral' WHERE LOWER(from_address) LIKE '%@formspree.io' AND classification IN ('hard_negative', 'soft_negative')`. **Files shipped:** `server/services/classify-replies.js` (pre-pass added). Parse-checked clean. **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-20):** EDP Formspree row now shows "Neutral" in admin; future Formspree mail will be set to neutral automatically without invoking Haiku.

### Decisions from the 2026-05-20 chat session (FIFTH session — Formspree polish + badge parity)

92. **Inbox badge parity — admin and customer portal now show the SAME labels for the SAME data, with three deliberate divergences. Shipped 2026-05-20 fifth session.** Operator surfaced via screenshots: admin showed "Out of office" for a row; the customer portal showed "OOO" for the same row. Admin showed "Soft negative" on an Allison Lynch reply; portal showed "Neutral". The portal was ALSO showing "Normal email" on rows that should have been visibly flagged as campaign auto-replies. The operator's ask: "i want both portals giving the same results, no admin does this and customer portal does that." This amends what an earlier session had baked in as if it were operator-locked design — it wasn't. **The earlier collapse of soft_negative/hard_negative/forwarding into a generic "Neutral" on the customer portal was never operator-requested; it was a previous chat's inferred-on-the-fly judgement and got formalised into lesson #69's wording without an explicit operator decision behind it.** This session corrects the record: there is no operator policy against customers seeing negative labels.

**What shipped (two files only):**

- **Admin `EmailSection.jsx` `classifyBadge`** — rewrote with three layers in priority order. (1) Formspree website-form leads (sender `@formspree.io` AND subject contains one of the lead keywords — same gate as `isFormspreeLead` in `formspree-flagger.js`) get **"Website Prospect"** in green, bypassing the classifier entirely; (2) campaign-matched rows get a `Campaign · ` prefix in front of the existing label (e.g. "Campaign · Soft negative", "Campaign · Out of office", "Campaign · New prospect", "Campaign · Auto-unsubscribed"); (3) non-campaign mail keeps the bare classifier label (ops detail, e.g. a vendor's OOO still reads "Out of office" on admin). Colour palette unchanged — same hex codes per classification as before, just relabelled and prefixed.

- **Customer portal `PortalApp.jsx` `ClassifyBadge`** — rewrote to mirror admin exactly with **two deliberate divergences** (both explicitly operator-approved this session): (a) non-campaign mail still collapses to **"Normal email"** on the portal because a vendor's OOO truly is meaningless to the customer (admin keeps the bare label for ops triage); (b) missing/null classification (transient state, resolves in ~10s as the classifier runs) shows **"Classifying…"** on the portal instead of admin's "Unclassified" — "Unclassified" reads as a failure to a customer, "Classifying…" reads as in-progress. Campaign-matched rows on the portal now show the FULL admin label set with `Campaign · ` prefix: "Campaign · New prospect" / "Campaign · Soft negative" / "Campaign · Negative" / "Campaign · Out of office" / "Campaign · Forwarded" / "Campaign · Neutral" / "Campaign · Auto-unsubscribed". Formspree leads on the portal also show "Website Prospect" in green (same as admin).

**Both badge functions were smoke-tested logic-only (13 cases) before shipping: Formspree website enquiry / catalogue download / newsletter-not-a-lead, all six campaign classifications, auto_unsubscribed override, non-campaign Google security alert, non-campaign vendor OOO, transient unclassified state. All 13 produced the exact label expected on both surfaces.** Both files parse-checked clean.

**Why "Website Prospect" not "Hot Prospect":** decision-time wording chosen by operator. "Hot Prospect" is the CRM section name; using it on the inbox badge would conflate "you should look at this" (the badge) with "this is on the Hot Prospects list" (the CRM section). The auto-flagger continues to add Formspree leads to the Hot Prospects list (decision #89 unchanged) — that part is automatic; the badge is the inbox-row signal that the email IS a website form submission.

**Why the prefix not two separate badges:** operator chose option (a) "Campaign · Soft negative" over option (b) "two separate pills." Less screen real estate, same information.

**Files shipped:** `src/components/EmailSection.jsx`, `src/components/customer-portal/PortalApp.jsx`. **PENDING OPERATOR VERIFICATION:** real Cube6/EDP inbox rows should show the new labels after deploy. Specifically: every "Automatic reply: SAMPLE" row on the portal should now read "Campaign · Out of office" (was "OOO"); Allison Lynch should read "Campaign · Soft negative" on BOTH admin and portal (portal previously said "Neutral"); the existing EDP Formspree website-enquiry rows should read "Website Prospect" on both surfaces. Non-campaign Google security alert rows stay "Normal email" on portal, "Neutral" on admin (deliberate).

**Decision-record impact:** lesson #69's framing ("the divergence is by design — feature not bug") is no longer accurate and has been rewritten in the lessons section this session. Lesson #69's *generic guidance* ("when two surfaces show different labels, document the mapping clearly") still holds and is worth keeping — but the EXAMPLE it cites (ClassifyBadge) is now an example of a divergence that should have been removed, not preserved. See lesson #71 (this session's new lesson) for the corrected generic guidance.

93. **Formspree parser polish — SHIPPED 2026-05-20 fifth session, after pulling a real EDP website-enquiry body from Render shell.** Decision #89 left a known-defective parser that landed 5 junk Hot Prospects rows. Per lesson #70, the diagnostic step came first: operator ran `SELECT body_text FROM email_replies WHERE from_address LIKE '%@formspree.io' AND subject LIKE '%enquiry%' ORDER BY received_at DESC LIMIT 1` on Render shell BEFORE any code was written. The body revealed something the previous session had assumed but not verified: **the EDP website-enquiry template puts the label on one line and the value on the NEXT line** (`email:\nwestley@sweetbyte.co.uk`), not in the same-line `email: westley@...` form the parser was built for. The catalogue-download template uses same-line form. So the previous parser's labelled-scan never matched anything on website-enquiry templates and fell straight through to the bare-email fallback, which grabbed the first email-shaped string in the body — `kevin@mail.edphandles.com` from the `mailbox:` label, which IS the customer's own destination address, not the submitter. **Fix shipped (three things together):** (a) parser now handles BOTH same-line `label: value` AND two-line label-then-value formats — `parseFormspreeBody` walks lines once, tries same-line split first, falls through to "this line is a label, next line is the value" otherwise; (b) the bare-email fallback is **REMOVED** — labelled-only, no recall-over-precision compromise (lesson #68 — "the bare-fallback is where junk creeps in"). If a future template doesn't carry a properly-labelled email field, the row is skipped + logged + we add label support in a follow-up; (c) new `isOwnAddress(emailClientId, prospectEmail)` second-defence filter — even if the parser ever picks the wrong field again, we won't write a junk row when the email matches a connected `email_inboxes.email_address` OR a domain the customer sends from (host portion of any `email_brands.from_email` or `email_lists.from_email` tied to that email_client_id). Wired into both the live poller path AND the backfill script. **10-case smoke test** covers the real EDP website-enquiry body, real EDP catalogue body, infrastructure senders, `mailbox:`-only-no-other-labels case, HTML-only body fallback, isFormspreeLead detection, localPartToName edge cases — all 10 pass. **Cleanup SQL** ran on Render shell for the 5 existing junk rows: `kevin@mail.edphandles.com`, `admin@thegreenagents.com`, `kevinsmith@edpgroup.co.uk`, `sdfgsd@sdfg.com` deleted across all customers, plus the wrong-client copy of `westley@sweetbyte.co.uk` on the `clearerpaths.co.uk` row (kept the legitimate `mail.edphandles.com` copy). Verified: 0 junk remaining, 1 westley row remaining. **Files shipped:** `server/services/formspree-flagger.js` (parser rewrite + `isOwnAddress`), `server/services/backfill-formspree-prospects.js` (own-address filter wired in). Both parse-checked clean. **DEPLOY WAS BLOCKED:** at end of fifth session Render's spend cap was hit, deploy didn't go through. Bundle pushed during sixth session once cap was raised (2026-05-21). **NOW CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-21).** **Important caveat for future Formspree work:** the parser is now labelled-only by design. If new customer form templates surface that don't use one of the recognised labels, the row will be skipped and logged — pull a sample body via Render-shell SELECT before extending label support. Don't guess.

### Decisions from the 2026-05-21 chat session (SIXTH session — Formspree thread pinning + Hot Prospects pipeline status)

94. **Hot Prospects thread shows the source Formspree email even when from_address doesn't match the prospect's address — SHIPPED 2026-05-21 sixth session.** Operator opened "Mr Ian mccreery" hot prospect (ian@starplanfurniture.net) and saw "No messages found" in the Email History panel — despite his record having been auto-created from a Formspree submission. Deep-dive revealed why: the thread query in `routes/hot-prospects.js` and `routes/portal.js` matches on `LOWER(from_address) = prospect.prospect_email`. Formspree submissions arrive with `from_address = noreply@formspree.io`; the actual prospect's email lives in the BODY of that message. Address-match never returns the source row, so the thread is correctly empty by its own logic — but operator-wrong because the form data is right there. **Fix shipped — three parts:** (a) new column `hot_prospects.source_reply_id TEXT` (FK to `email_replies.id`, nullable) pins the original `email_replies` row that flagged this prospect. (b) Both thread endpoints (admin + portal) now pull the pinned reply in addition to the address-matched rows, dedup against the existing list, merge as if it were any other inbound reply. Linked-set tenant filter still applies so a stale id can't leak in a foreign customer's reply. (c) Three write paths populate the column: the IMAP poller captures the just-inserted reply id and passes it to `processFormspreeLead(parsed, emailClientId, sourceReplyId)`; the manual-admin POST and portal POST both accept an optional `source_reply_id` body field (the EmailSection.jsx Send-to-Hot-Prospects button was already sending it but the column didn't exist). COALESCE on upsert so the first source wins — re-flagging from a different (newer) Formspree submission doesn't overwrite the existing pin. **Backfill SQL** ran on Render shell to retroactively pair existing 4 auto-flagged Formspree prospects with their source rows (matching `added_by='auto:formspree'` against `email_replies` where `from_address='noreply@formspree.io'` AND `body_text LIKE '%' || prospect_email || '%'`, most-recent wins). Verified all 4 pairs are correct: Mr Ian mccreery → COSMA EXPRESS RANGE, Rhiannon Phenis → TELA Glass Doors Systems, Westley → EDP website enquiry, Craig → COSMA EXPRESS RANGE. After UPDATE: 0 unmatched. **Q1 of the design conversation locked option (i):** the source row appears IDENTICAL to a regular inbound reply in the thread (so it shows as "Formspree <noreply@formspree.io>" — operator accepted this trade-off explicitly). Option (ii) would have been a labelled "How this lead came in" wrapper; operator declined. **Files shipped:** `server/db.js` (migration), `server/services/imap-poller.js` (capture replyId), `server/services/formspree-flagger.js` (accept sourceReplyId + COALESCE on update), `server/routes/hot-prospects.js` (RETURNING + thread fetch), `server/routes/portal.js` (RETURNING + thread fetch). All five parse-checked. **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-21):** Mr Ian mccreery's modal now shows the COSMA EXPRESS RANGE catalogue download form contents in the Email History panel.

95. **Hot Prospects pipeline status + tag colour + per-side viewed tracking — SHIPPED 2026-05-21 sixth session.** Operator asked for a way to organise prospects as the list grows ("contacted yes/no, requires follow up, did not answer") and a NEW pill + sidebar unread count on the customer portal. Design conversation explicitly amended an earlier design: initial proposal was free-colour-only (no status), Claude pushed back that colours without semantic meaning aren't filterable/searchable, operator chose option (c) **fixed statuses + free tag colour combined**. **Status enum:** three values — `new` (default for any new prospect, including Formspree auto-flagged), `contacted` (operator/customer has reached out), `no_response` (chased, got nothing back). Stored as TEXT NOT NULL DEFAULT 'new'. Each status has a default pill colour (blue/green/grey). **Tag colour:** customer-chosen override, stored as a colour NAME (not hex) from a bounded 8-colour palette (red/orange/yellow/green/blue/purple/pink/grey) plus NULL for "no override — use status default". Free-hex was considered and rejected because nothing prevents an operator picking an unreadable colour pair. **Custom tag colour wins** for the row pill background when set; the status LABEL stays whatever the status enum says (no relabelling on colour change). **Per-side viewed tracking:** two new columns — `admin_first_viewed_at` and `portal_first_viewed_at`, both TEXT nullable. Each side tracks its own (Q3 = separate, explicit operator decision). Stamped on first modal open via fire-and-forget `POST /:id/mark-viewed` (idempotent — repeated calls on already-viewed rows are no-ops). Drives the per-row red "NEW" pill on both lists. **Portal sidebar unread count:** new `GET /api/portal/hot-prospects/unread-count` returns `{ count: N }` where N is active prospects with `portal_first_viewed_at IS NULL`. Wired into the existing NavItem alongside the existing red urgent badge — two distinct pills (green unread + red urgent) because they signal different things (Q4 = portal-only). **Edit UX:** new Status dropdown + 9-option Tag colour picker (the 8 colours + ∅ default) in BOTH admin (`CrmHotProspects.jsx`) and portal (`PortalApp.jsx`) detail modals. Both fields save via the existing 1-second debounced PUT pipeline — same "Saving… / ✓ Saved" indicator covers all four fields (follow-up, notes, status, tag colour). PUT endpoint validation: server rejects status values outside the three-element enum and tag_color values outside the palette (or null). Both surfaces disable the status dropdown + colour picker when the prospect is converted (closed_at != null). **Migration:** four ALTER TABLE ADD COLUMN calls (idempotent), one CREATE INDEX for the unread-count query (partial index on `portal_first_viewed_at IS NULL AND closed_at IS NULL`), one backfill UPDATE for existing rows where status didn't default. **9-case smoke test** for the chrome resolver: status defaults (new/contacted/no_response), tag override wins, null/undefined/unknown defensive fallbacks — all 9 pass. **Existing-rows operator decision:** when the migration ran, all 7 existing prospects became `admin_first_viewed_at IS NULL AND portal_first_viewed_at IS NULL` — would have shown NEW pills on every row + an unread count of 7. Operator opted for the cleanup-SQL approach over either "live with it" or mass-set-status. SQL ran on Render shell: `UPDATE hot_prospects SET admin_first_viewed_at = added_at, portal_first_viewed_at = added_at WHERE admin_first_viewed_at IS NULL OR portal_first_viewed_at IS NULL` — verified 0 unviewed remaining on both sides. **Files shipped:** `server/db.js` (4 ALTER + 1 INDEX + 1 UPDATE), `server/routes/hot-prospects.js` (extended PUT, new mark-viewed POST, RETURNING/SELECTs updated), `server/routes/portal.js` (same + new GET unread-count), `src/components/CrmHotProspects.jsx` (Status column, NEW pill, dropdown + picker in modal, mark-viewed on open), `src/components/customer-portal/PortalApp.jsx` (same + unreadBadge on NavItem + unread-count fetch in chrome). All five parse-checked. **PENDING OPERATOR VERIFICATION:** real end-to-end test — change a status to Contacted in the admin modal, save, close, reopen, confirm status persists; pick a tag colour, confirm row pill background changes; on the portal sidebar after adding a new prospect somewhere, confirm green unread count appears.


### Decisions from the 2026-05-21 chat session (SEVENTH session — inbox row badges + Unsubscribe/Re-subscribe across the app)

96. **Inbox row action-taken badges — SHIPPED 2026-05-21 seventh session, CONFIRMED WORKING IN PRODUCTION.** Operator wanted to see at a glance, on every email row in the inbox list AND in the open-email modal, whether action had already been taken on the sender — specifically "is this person already on the Hot Prospects list?" and "have they been removed from the mailing list?" Two new secondary badges now sit next to the existing classification badge (`Campaign · New prospect` / `Website Prospect` / etc) on both surfaces. **(a) "On Hot Prospects list"** — amber pill, **clickable**, jumps straight to that prospect's detail modal in the CRM view via the existing `studio:navigate` + `localStorage` handoff (admin) or the in-tree `onNavigateToCrm` chain (portal). Shows whenever the sender's email matches a `hot_prospects` row under the customer's linked set (Cube6's [Cube6, mail.eng] or Manson's [Manson] etc — uses the canonical `resolveLinkedSet`). **(b) "Unsubscribed"** — grey pill, NOT clickable (end-state, no destination). Shows when the sender is unsubscribed from at least one of the customer's mailing lists — covers operator unsubscribes via the link click, classifier auto-unsubscribes, or manual flips, by reading `email_subscribers.status='unsubscribed' OR unsubscribed_at IS NOT NULL`. **Linked-set scoping for BOTH lookups** — critical for linked customers like Cube6, whose mailing lists actually live under the Cube6 anchor not the mail.eng inbox row. The first cut of this code scoped only to the inbox row's `email_client_id` — smoke-tested against a realistic Cube6 scenario which caught the bug pre-deploy. Fix: subscribers JOIN now spans the full linked set, matching what Hot Prospects already does (decision #85). Both badges render the same on admin AND customer portal per #92's "same labels by default" rule. Backend: `resolveLinkedSet` + `resolveCrmCustomerId` exported from `hot-prospects.js`; new `attachInboxBadges()` helpers in `email.js` (admin) and `portal.js` (portal) decorate inbox rows + the detail-modal response with `hot_prospect_id`, `hot_prospect_crm_customer_id`, and `contact_unsubscribed`. Frontend: new `ActionBadges` component in `EmailSection.jsx` and `PortalActionBadges` in `PortalApp.jsx`. **No backfill or migration required** — both signals are computed live at read-time off existing tables. **Files shipped:** `server/routes/hot-prospects.js`, `server/routes/email.js`, `server/routes/portal.js`, `src/components/EmailSection.jsx`, `src/components/customer-portal/PortalApp.jsx`. All five parse-checked. **CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-21):** Ian at IDM Design's row shows "Campaign · New prospect" + "On Hot Prospects list"; Harvey Jones's auto-unsub row shows "Campaign · Auto-unsubscribed" + "Unsubscribed"; both badges render in the open-email modal too. Operator did need a moment to look at the customer portal — initial screenshot was admin only, where the row layout stacks the two pills slightly differently to the portal.

97. **Unsubscribe + Re-subscribe everywhere they make sense — SHIPPED 2026-05-21 seventh session, PENDING FULL OPERATOR VERIFICATION.** Built in two halves of the same session: per-reply (modal level) first, then Hot Prospect (record level) second. **Backend shape — three-response idiom used across ALL four new/replaced endpoints:** every flip endpoint returns one of `{ok: true, lists_affected: N}` (the success case), `{ok: false, code: 'not_on_lists', message: '...'}` (sender's email isn't on any subscriber row across the customer's linked set — honest refusal, no silent no-op), or `{ok: false, code: 'already_unsubscribed' | 'already_subscribed', message: '...'}` (state is already what the operator asked for). The three-shape pattern was a deliberate design choice over the alternative of always returning `ok: true` — silent no-ops would obscure the Q3 "what if they're not on any list?" case the operator explicitly wanted surfaced. **Per-reply modal (both admin and portal):** existing admin Unsubscribe button gated condition CHANGED from `!reply.auto_unsubscribed` (per-reply audit flag) to `!reply.contact_unsubscribed` (real subscriber state — the new field from #96). When `contact_unsubscribed` is true: button row shows **"✓ Already unsubscribed"** grey pill + a Re-subscribe button next to it. After either button is clicked, an inline status line appears below the action row (success green / info italic grey / error red) that stays visible until the next click — important for the "not on any list" case which is information, not a failure. Modal does NOT close on success — refetches the reply so the badges + button row swap state in place. Portal modal gained an Unsubscribe button it never had before. **Hot Prospect detail modal (both admin and portal):** added a Re-subscribe banner first round (amber, "🚫 Unsubscribed from your mailing list" + Re-subscribe button), shown only when `contact_unsubscribed` is true. Operator then asked for the symmetric Unsubscribe direction too — second round added the red Unsubscribe banner ("📬 On your mailing list" + Unsubscribe button), shown only when contact_on_list AND !contact_unsubscribed. **THIRD STATE introduced:** the modal needed to distinguish "on list + subscribed" (red banner), "on list + unsubscribed" (amber banner), "not on list" (no banner). Adding a `contact_on_list` boolean alongside `contact_unsubscribed` was cleaner than overloading `contact_unsubscribed=false` into two meanings (it can mean both "subscribed" AND "not on any list"). **All linked-set scoped** — Cube6's mail.eng + Cube6 anchor both checked in every direction. The existing per-reply admin endpoint (`/replies/:id/manual-unsubscribe`) had been single-id scoped since Phase 3 — this session rewrote it to use the linked set, which is the right behaviour for linked customers (was a latent bug for them). **One Render-blocker mistake caught mid-build:** initial portal route attempted `INSERT INTO email_audit_log (... client_user_id, ...)` — that column doesn't exist on the table. Caught by re-reading the schema (lesson #73 family — accept-and-discard / write-to-nonexistent-column is the same anti-pattern). Fixed before deploy: portal-side audit rows now use `actor='portal:<user-id>'` + `metadata.source='portal'` for the same effective audit trail. **Files shipped (six total):** `server/routes/hot-prospects.js` (named exports for `resolveLinkedSet`/`resolveCrmCustomerId`; new `computeUnsubscribedAddresses` + `computeOnListAddresses` helpers; rewrote `/replies/:id/manual-unsubscribe` for linked-set + three-shape response; new `/replies/:id/manual-resubscribe`; new `/:id/unsubscribe` and `/:id/resubscribe` for hot prospects; thread + list responses now decorated with `contact_unsubscribed` + `contact_on_list`), `server/routes/email.js` (admin uses the new helpers; admin replies endpoints attach the new fields), `server/routes/portal.js` (mirror endpoints; portal helpers `computePortalUnsubAddresses` + `computePortalOnListAddresses`; thread + list responses decorated; new portal `/hot-prospects/:id/unsubscribe` + `/:id/resubscribe` + `/replies/:id/manual-unsubscribe` + `/replies/:id/manual-resubscribe`), `src/components/EmailSection.jsx` (admin button condition changed + Re-subscribe button added + inline status line), `src/components/customer-portal/PortalApp.jsx` (portal modal got both buttons + status; portal Hot Prospect modal got the dual banner), `src/components/CrmHotProspects.jsx` (admin Hot Prospect modal got the dual banner). All six parse-checked. **Smoke tests run:** six SQL cases covering both directions of the toggle + the not-on-list case all produced the expected responses. **Honest design caveat:** the existing per-reply Unsubscribe button changed condition (from `!auto_unsubscribed` to `!contact_unsubscribed`). For legacy data where `auto_unsubscribed=1` but the subscriber row was never actually flipped (historical inconsistency), the button will now appear and clicking it will correctly flip the subscriber row — the right outcome, just a behaviour change worth knowing. **PENDING OPERATOR VERIFICATION:** (1) per-reply modal — pick any reply where the sender IS on a list and currently subscribed → red Unsubscribe button → click → "✓ Unsubscribed from N lists" status → "✓ Already unsubscribed" + Re-subscribe button replace it. (2) Same again, click Re-subscribe → back to Unsubscribe. (3) Open Mr Ian McCreery's prospect modal → confirm NO banner (Formspree lead not on any list). (4) Open a prospect whose email IS on a list and currently subscribed → confirm red Unsubscribe banner. (5) Click it, flip → confirm amber Re-subscribe banner appears in place. (6) Customer portal mirror of (1)–(5). **Ian at IDM Design SQL check ran during this session** showed he's not on any list at all (`SELECT s.email, s.status FROM email_subscribers s JOIN email_lists l ON l.id = s.list_id WHERE LOWER(s.email)='ian@idmdesign.co.uk'` returns empty) — confirms that part of the Q3 behaviour is correct: the modal stays banner-free for him.

### Decisions from the 2026-06-08 chat session (NINTH session — "Remove from list" + SES SNS bounce wiring + future-feature stubs)

99. **"Remove from list" — soft-hide for the Email Campaigns customer list — SHIPPED 2026-06-08 ninth session, IN PRODUCTION (operator verified `mail.idoyourquotes.com` removal worked correctly).** Operator wanted a way to clean up customer rows that had been removed from AWS SES verified domains but were lingering in Studio's Customers list (the auto-sync only ADDS rows from AWS, never removes — decision #57 design). **Honest design conversation before code:** considered cascade-delete with typed-name confirm vs soft-hide; operator initially asked for cascade ("just delete it") then changed mind to soft-hide ("in case the customer comes back to us several months later and wants to carry on a new campaign, we will have the history then so can continue from the last campaigns"). Soft-hide is the right call — preserves all data (lists, subscribers, campaigns, replies, hot prospects, audit log) and means a re-verification in AWS auto-unhides the row with full history intact. **Mechanism:** new `hidden_at TEXT` column on `email_clients`, added via additive migration (NULL = visible; populated = hidden). `GET /clients` filter excludes hidden rows. `DELETE /clients/:id` keeps URL compatibility with the previous endpoint at that path but its BEHAVIOUR is now soft-hide (sets `hidden_at = datetime('now')`). **Auto-unhide via AWS re-verification:** `POST /clients` checks `WHERE LOWER(name) = LOWER(?) AND hidden_at IS NOT NULL LIMIT 1` BEFORE inserting — if the AWS sync's `loadAll` tries to "create" a row whose name matches a hidden row, the endpoint clears `hidden_at` and returns the existing row instead. Symmetric with #57: "AWS is the source of truth for what's visible" — remove from AWS → row hides; re-verify in AWS → row reappears with all history. Case-insensitive name match. **UI:** Red "Remove from list" `Btn variant="danger"` in the bottom-left of the Edit Client modal, separated from Save/Cancel (right) so it can't be mis-clicked. Only shown in edit mode (`editing` flag), never on the New Client modal. Confirm dialog wording reflects the soft nature: "Lists, subscribers, campaigns, and reply history are kept. If you re-verify the domain in AWS later, this customer will reappear here automatically with all their history intact." **Parent wires `onDeleted` prop:** edit-modal call site in `EmailSection.jsx` passes `onDeleted={()=>{setSelected(null);loadAll();}}` so the parent deselects the now-stale `selectedClient` and refreshes the customer list. **Smoke testing before deploy (lesson #77):** Python+SQLite test with 8 scenarios — baseline visible list / hide idoyourquotes (orphan with no data) / hide mail.fundingclarity (4651 subscribers preserved across hide) / re-POST idoyourquotes triggers auto-unhide branch / case-insensitive match unhides / hiding mail.eng doesn't break Cube6's portal customer_services link / re-verifying mail.eng restores Cube6 email feature / hiding an already-hidden row is idempotent. All 8 pass. **Files shipped (3 total):** `server/db.js` (hidden_at additive migration), `server/routes/email.js` (filter in GET, auto-unhide in POST, soft-hide in DELETE), `src/components/EmailSection.jsx` (Remove button + onDeleted prop + wire-up). All parse-checked. **Open question NOT decided (don't pre-decide):** no admin UI exists to see/unhide a hidden row directly — recovery is via re-verifying in AWS. If a customer comes back but operator can't / doesn't want to re-verify in AWS, there's currently no way to unhide. Wait for operator to surface the need before building a hidden-domains admin view.

100. **SES SNS bounce/complaint wiring — CONFIGURED 2026-06-08 ninth session, IN PRODUCTION (end-to-end verified on mail.engineersolutions.co.uk; other 13 domains require operator action in AWS console).** Studio's SNS bounce/complaint handlers at `/api/email/sns/bounces` and `/api/email/sns/complaints` (decision in Phase 2 + handler at email.js line 190 / 214) had been correctly coded since Phase 2 but were never actually being called — `email_sns_events` table was empty across 9,763 historical sends, 0 bounces marked. The campaign report (mail.engineersolutions.co.uk Wave 2) showed `0.00% Bounced / 0 hard bounces` while Gmail Spam folder for david@mail.engineersolutions.co.uk contained 78 MAILER-DAEMON Delivery Status Notification messages. **Root cause:** SES per-identity notifications were never wired to the existing `bounces` / `complaints` SNS topics. The SNS topics already existed (created originally for Sendy — only Sendy's HTTPS endpoint was subscribed). Studio was simply not in the loop. **Honest pushback NOT to chase code fixes** (the IMAP poller only opens INBOX so it can't see Gmail Spam; pushed back on adding Spam-folder polling as a fragile workaround — SNS is the correct mechanism, never an IMAP workaround). **AWS-console configuration done in this session:** (1) Added Studio HTTPS subscription `https://studio.thegreenagents.com/api/email/sns/bounces` to existing `arn:aws:sns:eu-north-1:262023811768:bounces` SNS topic — auto-confirmed by Studio's `SubscriptionConfirmation` handler within seconds. (2) Same for `complaints` topic and `/api/email/sns/complaints` endpoint. Sendy's existing subscription left untouched — SNS fans out to all subscribers, both Sendy and Studio now receive the same events. (3) Wired SES identity `mail.engineersolutions.co.uk` Notifications tab: Bounce feedback → bounces topic + Include original headers ON; Complaint feedback → complaints topic + headers ON; Delivery feedback DELIBERATELY LEFT OFF (delivery events would flood Studio with one event per successful send — Studio doesn't use them). **End-to-end test:** sent `bounce@simulator.amazonses.com` test from the campaign Test button. SNS event landed in `email_sns_events` within 3 seconds as `type='bounces:Notification'`, `notificationType='Bounce'`, `bounceType='Permanent'`. `handleBounce` was called, parsed correctly, but did NOT update `email_sends` because **test sends deliberately don't write to `email_sends`** (line 1448 — see lesson #78). This is the correct behaviour for test sends. The bounce handler's same path runs on real sends. Operator trusted the test as proven enough. **OPERATOR ACTION STILL OUTSTANDING:** repeat the SES Notifications wiring for the other 13 verified identities in the eu-north-1 SES console (sweetbyte.co.uk, mail.wedoyourquotes.com, mail.weprintcatalogues.com, mail.fundingclarity.co.uk, clear-a-way.co.uk, itcloudpros.uk, thegreenagents.com, mail.edphandles.com, syncsure.cloud + whatever else is on page 2 of identities). Same per-identity steps for each: Identities → click domain → Notifications tab → Edit on "Feedback notifications" → Bounce: bounces topic + headers ON, Complaint: complaints topic + headers ON, Delivery: leave No SNS Topic. ~90 seconds per domain. NO further testing needed per-domain — engineersolutions proved the wiring works end-to-end. **Until operator finishes the other 13:** bounces on those domains will continue to be invisible to Studio (no auto-flip of `email_subscribers.status='bounced'`, no auto-flip of `email_sends.status='bounced'`, campaign report continues to show 0% bounced). Email-feedback-forwarding (the MAILER-DAEMON messages back to From address) is unaffected by this and continues to deliver to Gmail's Spam. **Note on the second AWS account (clearerpaths.co.uk + future TGA marketing domains):** when Stage 1 of the AWS-account split lands, SNS wiring will need to be done independently for the TGA account using TGA-side SNS topics + a separate set of endpoint routes (or shared if topics can cross-account, which we'd need to design — likely they can't and we'll need separate `/api/email/sns/tga-bounces` etc). Park this design until session 1 of the split is being built. **No code shipped in this session for SNS — it was a pure configuration unlock.** Existing Studio code from Phase 2 worked correctly the moment SNS started delivering events.

101. **DEFERRED future-feature stubs locked in 2026-06-08 ninth session (no code yet, blueprint-only).**

- **(a) Reply-count over-counting in campaign report.** SQL in `server/routes/email.js` around line 1609 joins `email_replies` to `email_sends` on `(email_client_id, lower(from_address)=lower(s.email), reply_received_at >= send_sent_at)` — but does NOT gate by `matched_campaign_id = es.campaign_id`. So any reply from a subscriber on this campaign's list, sent after their step send, counts — including replies attributable to OTHER campaigns the contact is on, or general business mail to the same `From` address. For heavy long-running customers like mail.engineersolutions (multiple campaigns, overlapping recipient lists) the over-count is significant: Wave 2 report shows "17 replies on 1st contact" + "5 replies on 2nd" which operator cannot find in the actual mailbox view (because they don't exist as replies to THIS campaign — they're real replies, just to other campaigns). **One-line SQL fix:** add `AND r.matched_campaign_id = es.campaign_id` to the JOIN. **Honest caveat:** this will REDUCE the reported reply numbers visibly. Operator should expect the numbers to drop, this is the over-count being corrected. Mailbox view's per-reply campaign attribution (which uses `matched_campaign_id` correctly already) is the source of truth — the campaign report should match it. **Status:** deferred to next session to keep scope of ninth session tight. Safe one-file fix when picked up.

- **(b) Click-tracking inflated by corporate mail security scanners.** Spotted via Wave 2 recipient "Wil at sinclair-intl.com" showing 36 clicks. SQL diagnostic on `email_link_clicks` showed pattern is mixed: some clicks within seconds of each other (multiple identical URL clicks within 1 second — bot/scanner pattern; 4 clicks to engineersolutions.co.uk all at 12:06:00 confirmed), interspersed with single clicks hours apart (real human). Corporate inboxes with active link-scanning (Mimecast, Proofpoint, Microsoft Defender, etc.) pre-fetch every link in incoming email. Studio's `email_link_clicks` table has no `ip_address` or `user_agent` column so we can't definitively distinguish bot from human — only timestamp clustering. **This is an industry-wide problem, not Wil-specific** — every recipient on a corporate domain with link-scanning has inflated click counts. **Four options considered, not decided:** (A) burst dedup — collapse N clicks within X seconds for same (subscriber, url) into 1 click — lowest risk, kills ~95% of scanner noise; (B) first-click-only per (subscriber, url) pair — simpler, more aggressive, loses re-engagement data; (C) identify-and-flag — don't change counts, surface "x clicks (probably scanner)" in UI; (D) add ip_address + user_agent capture to `email_link_clicks` for future definitive distinguishing. **Status:** deferred. Don't pick up unless operator surfaces it again. If picked up, option (A) is the lean answer — but a small migration to add ip_address + user_agent columns first (option D) would future-proof and lets us go back to dedup with confidence later.

- **(c) Bounce-rate auto-pause campaign protection.** AWS SES already auto-pauses at ~10% rolling hard-bounce rate (with email warning at ~5%). By the time AWS pauses, sender reputation is already damaged. Operator wants Studio to pause campaigns EARLIER as self-defence. **Open design questions (don't pre-decide):** threshold (5%? 3%?), per-campaign vs per-customer scope, window (rolling 24h? "minimum 100 sent before evaluation"?), scope of pause (this campaign only? all campaigns for this customer? globally?), recovery (manual unpause? auto-resume when rate drops?), alerting (email john@thegreenagents.com? dashboard banner? Slack?). **Status:** deferred — pre-requisite is decision #100's SNS wiring to be done across ALL identities, because without complete bounce coverage, the bounce-rate calculation has blind spots. Pick up after operator finishes the 13-domain SES Notifications grind.

98. **Per-campaign unsubscribe panel + drip-ticker gate — SHIPPED 2026-05-22 eighth session, CONFIRMED WORKING IN PRODUCTION end-to-end.** Operator wanted per-campaign opt-out tickboxes shown identically in (a) the inbox open-email modal and (b) the Hot Prospect detail modal, on both admin and customer portal, with instant lockstep between surfaces. Plus an auto-stop when someone is added to Hot Prospects so the source campaign's drip stops continuing to them (operator's concern: "looks ridiculous" to send drip step 3 to someone you're already in real conversation with). **Design conversation locked four key choices BEFORE any code:** (Q1) added-to-Hot-Prospects auto-stops the source campaign's drip — but introduces a NEW state "paused (excluded from drips) but NOT unsubscribed", separate from genuine unsubscribed (preserves ability to re-enrol in future campaigns); (Q2) tickboxes are PER-CAMPAIGN, not per-list — operator explicitly clarified after my initial per-list assumption; (Q3) instant lockstep across admin↔portal; (Q4) panel shows EVERY campaign at the customer (not filtered to those the contact's email is on the list for) — so future campaigns auto-appear in everyone's panel and the "Unsubscribe from ALL — includes future" tick covers them; archived campaigns kept in panel and labelled. **Underlying mechanism (two NEW tables, both linked-set scoped):** `campaign_unsubscribes(id, email_client_id, contact_email, campaign_id, unsubscribed_at, source)` for per-campaign rows (`source` is `'manual'` or `'hot_prospect_auto'`; UNIQUE(email_client_id, contact_email, campaign_id) prevents dupes via INSERT OR IGNORE); `contact_unsubscribed_all(email_client_id, contact_email, unsubscribed_at, source)` for the master "Unsubscribe from ALL" tick. **Two separate tables, not one with nullable campaign_id** — same separation-of-meanings principle as `contact_on_list` vs `contact_unsubscribed` from #97 (don't overload one column with two semantics). **Drip ticker gate (closes blueprint backlog priority 3 — open across multiple sessions):** new `resolveLinkedSet()` helper in `drip-ticker.js` (DUPLICATED from `hot-prospects.js` — service layer can't depend on routes layer, same convention as `resolveLinkedSet` duplicated portal-side); two new NOT EXISTS clauses applied to step-1, follow-up, AND still-pending queries. Still-pending gate is critical — without it, unsubscribed contacts keep campaign in 'scheduled' forever and it never flips to 'sent'. Per-campaign gate uses `campaign_id` directly (no linked-set lookup — convention is writes always land under the campaign's owning `email_client_id`); master gate uses linked-set IN clause (master flag can be written under either side of a linked customer like Cube6+mail.eng). **Auto-tick on Hot Prospect add:** existing POST `/api/email/hot-prospects/` extended (and portal mirror) to look up `matched_campaign_id` from the `source_reply_id`, then INSERT OR IGNORE into `campaign_unsubscribes` with `source='hot_prospect_auto'`. Only fires when `was_new=true`. Best-effort, wrapped in try/catch — a failed lookup never blocks the prospect-add itself. Existing prospects added BEFORE this deploy do NOT get retroactively auto-ticked (additive principle — same as #95). **Two shared helpers exported from `hot-prospects.js`** for reuse: `buildSubscriptionsPanel(emailClientId, contactEmail, options)` builds the panel JSON state (campaigns × per-contact state, with N+1 query avoidance — one aggregate query per fact rather than per-campaign loops); `applySubscriptionsUpdate(emailClientId, contactEmail, body, audit)` applies a partial PUT payload. Same helpers called from all four endpoint pairs across admin + portal. **Four new endpoint pairs (eight endpoints total):** admin `GET/PUT /api/email/hot-prospects/:id/subscriptions`, admin `GET/PUT /api/email/replies/:id/subscriptions`, portal `GET/PUT /api/portal/hot-prospects/:id/subscriptions`, portal `GET/PUT /api/portal/replies/:id/subscriptions`. Portal-side ones are tenant-scoped via the existing patterns (`resolveEmailClientId` for replies; `_resolveProspectLinkedSet` + `_inClause` for prospects). All PUTs return fresh panel state in the same response — no follow-up GET needed. **Cross-customer security check:** `applySubscriptionsUpdate` verifies any `campaign_id` in the body is owned by the contact's linked set before writing — so even a malformed/malicious request can't tick a campaign owned by another customer. Belt-and-braces with the tenant scope on the route lookup. **Frontend — self-contained `CampaignSubscriptionsPanel` component, intentionally duplicated three times** (in `EmailSection.jsx`, `CrmHotProspects.jsx`, and `PortalApp.jsx` as `PortalCampaignSubscriptionsPanel` — same convention as `resolveLinkedSet` being duplicated portal-side; cross-React-tree imports get messy). Each panel: optimistic UI (tick reflects immediately, PUT in background, snaps to canonical state on error); master-on disables per-campaign rows visually (checked + disabled with italic hint "untick 'Unsubscribe from ALL' to control campaigns individually"); auto-ticked source campaign renders amber with "Auto" pill + sub-label "auto-stopped when added to Hot Prospects"; archived campaigns rendered faded (65% opacity), sorted to bottom. Sub-label per row composes 0-4 facts: archived flag, audience-list status, send progress ("3 of 5 sent" / "N steps planned"), auto-stop note. **Render-storage convention** matters for the gate: per-campaign rows ALWAYS written under the campaign's `email_client_id` (so the gate query needs no linked-set lookup); master flag written under whichever id the operator wrote from (linked-set lookup on read). Smoke-tested 2026-05-22 with explicit Cube6 cross-side case: master tick written under mail.eng correctly blocks campaign owned by cube6 anchor. **Smoke testing before deploy (lesson #77):** seven Python+SQLite scenarios covering Cube6 linked, Manson self-linked, auto-tick, untick, cross-side master writes/reads, INSERT OR IGNORE duplicate prevention, foreign-campaign security rejection. All seven pass before any code shipped. **Files shipped (eight total, one per delivery for staged deploy verification):** `server/db.js` (two new tables + three indexes — verified idempotent), `server/services/drip-ticker.js` (gate + duplicated linked-set helper), `server/routes/hot-prospects.js` (auto-tick on POST extension + two new helpers + two new endpoints + extended exports), `server/routes/email.js` (two thin endpoint wrappers + extended import), `server/routes/portal.js` (four thin endpoint wrappers — two for replies, two for hot prospects + extended import), `src/components/EmailSection.jsx` (panel component + one render site in inbox modal), `src/components/CrmHotProspects.jsx` (panel component + one render site in Hot Prospect modal), `src/components/customer-portal/PortalApp.jsx` (portal panel component + TWO render sites — portal inbox modal + portal Hot Prospect modal). All eight parse-checked clean. **Deploy verification at each step:** files 1–5 verified via Render shell `sqlite3` schema check + curl against real reply IDs (Chris @ UK Hose returned panel JSON correctly; Jack @ Custom Kitchens returned panel JSON correctly). One mid-build catch: operator forgot to deploy file 3 before testing — diagnosed via `find ~/project -name "hot-prospects.js"` + grep, redeploy fixed it (lesson #76 — "deploy failed" can mean "deploy never ran"). Files 6+7+8 verified via operator screenshots of admin inbox + admin Hot Prospect modal + customer portal showing identical panels and ticks reflecting correctly across both sides. **Honest coexistence with #97 (not retired):** the existing list-level Unsubscribe / Re-subscribe buttons (admin + portal) and the dual-banner Hot Prospect modal banners from #97 are STILL IN PLACE alongside the new panel. They operate at LIST level (kill subscriber row in `email_subscribers`); the new panel operates at CAMPAIGN level (rows in `campaign_unsubscribes`). Different layers, different purposes — NOT redundant. Future operator decision available: consolidate into one UI, move list-level into a collapsed "Advanced" section, rename for clarity ("Remove from mailing lists" vs "Stop campaigns"), or retire the list-level controls. Don't pre-decide — wait until the operator has lived with both in production. **Files shipped, all eight parse-checked, all eight deployed, CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-22):** ticking a campaign in admin inbox shows same state in admin Hot Prospect modal AND in customer portal inbox AND in customer portal Hot Prospect modal, all surfaces synced. **Open question for next session (not blocking):** decide whether to retire #97's list-level UI affordances now that the campaign-level panel covers most operator scenarios, OR leave them for the rarer "remove from list entirely" case.



### Supergrow MCP — confirmed facts (pulled live 2026-05-19, do not re-investigate)

- **Auth:** API key as URL query param `https://mcp.supergrow.ai/mcp?api_key=KEY`. One account key (`69e4894b5d…` prefix) is shared across ALL clients; routing is purely by `supergrow_workspace_id`, which is distinct per client and verified correct (The Manson Group's posts went to workspace `914be39a-f90d-4635-b216-6a2c7281abbd`, matching the operator's Supergrow screenshot URL). **Identical key prefixes across all clients is BY DESIGN, not a duplication bug — do not "fix" it.**
- **Kanban/status columns:** `draft, under_review, approved, needs_changes, scheduled, published` (from `get_kanban_column`). `list_posts` status enum: `draft, scheduled, published, failed, all`.
- **No scheduling-by-timestamp anywhere.** `create_post` → draft (no date field). `queue_post` → auto-schedules into next free calendar slot (no date field). `update_post` → edits a draft (no date field). There is NO `schedule_post` tool and NO way to post at an explicit chosen datetime via this API. Supergrow owns slot timing.
- **No `approval_mode` argument exists** on any tool — old code comments claiming `approval_mode="auto"`/`"draft"` were wrong and have been corrected.
- **Images:** ONLY via `create_media` → presigned multipart POST → `confirm_media` → pass `media_ids: [uuid]`. No `image_urls` field exists (see decision #71). `text` field has `maxLength: 3000`. Optional `linked_in_company_page_id`, `carousel_title`, `video_title`, `media_ids`.
- **Why posts were landing in Drafts (the original 2026-05-19 operator report):** operator pushed from the admin side, which is wired to `create_post`/`deployToDrafts` → Drafts BY DESIGN (decision #18). This was never a bug — it was the admin draft path working as built. The customer-portal approve-all path already uses `queue_post` → Scheduled. The two-button work (decision #72) is what makes admin-side direct-to-Scheduled a first-class action.

---

## Current state — what's deployed and working

### Phase 1: Core email sending (DONE, live)
- AWS SES via raw Signature V4
- Campaigns sent in batches with throttle (10/batch, 800ms gap)
- Subscribers + lists (per email_client)
- CSV import; one-shot send via `Send now`

### Phase 2: Bounce/complaint via SNS (DONE in code, **partially wired on AWS as of 2026-06-08 — 1 of 14 identities done, 13 still pending operator action — see decision #100**)
- SNS topics `bounces` and `complaints` already exist (both also subscribed to Sendy — both apps run in parallel; SNS fans out)
- Backend endpoints `/api/email/sns/bounces` and `/api/email/sns/complaints` ready since Phase 2; Studio HTTPS subscriptions added 2026-06-08 and auto-confirmed
- **`mail.engineersolutions.co.uk` identity Notifications wired 2026-06-08 — bounces + complaints flowing end-to-end, verified via `bounce@simulator.amazonses.com` test**
- **TO-DO for user (carries over to next session):** repeat SES identity Notifications wiring on the other 13 verified domains (sweetbyte.co.uk, mail.wedoyourquotes.com, mail.weprintcatalogues.com, mail.fundingclarity.co.uk, clear-a-way.co.uk, itcloudpros.uk, thegreenagents.com, mail.edphandles.com, syncsure.cloud, + whatever's on page 2 of identities). Same per-identity steps: Notifications tab → Edit Feedback notifications → Bounce: bounces topic + headers ON, Complaint: complaints topic + headers ON, Delivery: leave No SNS Topic. ~90 seconds per domain.

### Phase 2.5: Smart per-recipient tracking (DONE, live)
- Three modes per campaign: `off` / `smart` / `all`
- Per-list "always_warm" override; pre-send dialog shows recipient breakdown
- Cold campaigns default to `tracking_mode='off'`

### Phase 3.1 + 3.1.5: Mailbox connection + IMAP polling + full-inbox view (DONE, live)
- Per-domain Gmail/Workspace mailbox via app password, AES-256-GCM at rest
- 3-min poll, manual "Check now"
- 30-day backfill on first poll; subsequent polls use `uid > last_uid`
- Manual `Resync` (non-destructive: reset `last_uid=0`) and **destructive Resync** (also wipes `email_replies` for that inbox)
- Three-strategy reply matching: `In-Reply-To` → `References` → sender lookup
- **Timestamp display fix**: SQLite's `datetime('now')` returns no Z; `relTime()` in EmailSection.jsx detects and force-treats as UTC

### Phase 3.2: Reply classification + auto-unsubscribe (DONE, live)
- Three-pass classifier in `services/classify-replies.js`: regex → heuristic OOO → Haiku for the rest
- Cron every 60s, up to 25 unclassified per tick, max 3 parallel Haiku calls
- Strips quoted/forwarded content before running
- Auto-unsubscribe fires on `hard_negative` AND `soft_negative` — but ONLY when `reply.matched_campaign_id` is non-null (gated by decision #52). Random vendor mail with negative wording no longer poisons the unsubscribe list.
- Buttons: **"Classify pending"** (mailbox header), **"Classify with AI"** (reply detail)

### Phase 3.3: Per-recipient personalisation (DONE, live)
- `services/name-parser.js` rule + Haiku fallback
- Columns on `email_subscribers`: `first_name`, `first_name_source`, `first_name_reason`
- 16 unit tests pass for rule parser
- `{{first_name}}` placeholder + legacy `[Name]`
- **Preview Campaign modal** — flip through subscribers, edit parsed name, skip count surfaced

### Phase 3.4: Drip ticker (DONE, live)
- `services/drip-ticker.js` runs every 60s
- Drip columns on `email_campaigns`: `drip_send_days`, `drip_window_start/end`, `drip_timezone`, `drip_today_date`, `drip_today_sent`, `drip_last_tick_at`
- ±30% jitter ("Option B")
- Re-checks status before every send → Pause/Cancel takes effect within seconds
- Render restart mid-burst is safe (uses `email_sends` row existence)
- ScheduleControls UI in CampaignModal with live finish-date estimate
- Campaign queue shows drip progress

### Phase 3.5: Drip progress reporting + recipients view (DONE, live)
- `CampaignReport` works for in-flight drips and sent campaigns
- New `View progress` button for scheduled / sending / paused
- `CampaignRecipientsPanel` with filter pills, search, CSV exports

### Phase 4 helpers (DONE, live)
- **Default sender per client** — `email_clients.default_from_email`/`default_from_name`
- **Line-spacing fix Option A** — `wrapBodyWithEmailCss()` in ses.js wraps every body with a CSS reset before SES base64. Outlook default `<p>` margins are normalised; empty `<p><br></p>` placeholders hidden.

### Phase 4 multi-step sequences (BUILT and live, not schema-only)

The blueprint previously said this was schema-only. Wrong — corrected this chat.

**What's live:**
- `email_campaign_steps(id, campaign_id, step_number, subject, html_body, delay_days, created_at)` + UNIQUE on `(campaign_id, step_number)`
- `email_sends.step_number INTEGER NOT NULL DEFAULT 1` + index `(campaign_id, subscriber_id, step_number)`
- Backfill: every existing campaign auto-gets a step_1 row from its `html_body` on first boot
- Per-step subject (decision #44): `subject TEXT` column on `email_campaign_steps`. NULL = fall back to `email_campaigns.subject`.
- `sendCampaign()` accepts `stepNumber`, `bodyOverride`, and `subjectOverride` parameters
- **Drip-ticker extension is built and running.** `services/drip-ticker.js` queries the step list per campaign, finds subscribers due for step N+1 (where their last send was ≥ `delay_days` ago), and combines them with step-1 first-time candidates into the same daily burst budget. `_stepNumber` flows through the `toSendNow` array and back to `sendCampaign`.
- **Sequence editor UI is built and live.** `CampaignModal` in `EmailSection.jsx` has the tab strip ("1st contact / 2nd contact / + Add contact"), per-step body editor, per-step subject field, per-step delay-days input. Up to 10 steps, with a remove confirmation that warns if the step has already been sent to anyone.

**Still missing (real backlog for next chat):**
- **Stop-condition filter.** The drip ticker doesn't yet halt the sequence for subscribers who replied with `positive` / `soft_negative` / `hard_negative`. They keep getting follow-ups. Fix is small: extend the candidate query with `AND NOT EXISTS (SELECT 1 FROM email_replies WHERE matched_campaign_id = c.id AND from_subscriber_id = s.id AND classification IN ('positive','soft_negative','hard_negative'))`.
- **Per-step reporting in `CampaignRecipientsPanel`.** Recipients view doesn't currently show "last step sent". Useful for operators to see how far through the sequence each subscriber has reached.
- **Edit-warning toast for already-sent steps.** State is wired (sent_count > 0 flag), but the modal doesn't yet show a confirmation "this step has been sent to N people, your edit applies to the rest only" before save. Helper text shows on the step itself but doesn't gate the save.

---

### Customer Portal (FULLY BUILT, LIVE)

A per-customer portal at `/c/<slug>` where each portal customer's contact (e.g. Rob at Cube6) signs in to review LinkedIn posts, see email-campaign stats, read replies, manage their portal users.

**Architecture is split across two sides — admin-side management and customer-facing portal.** Each runs through its own routes and has its own UI section.

#### Admin side — Customer Portal management

**Sidebar location:** `Sidebar.jsx` has a new section "**CUSTOMER PORTAL**" at the bottom with one item: **Portal Customers**. Mounted by `Dashboard.jsx` when `view === 'portal-customers'`.

**File:** `src/components/PortalAdmin.jsx`. Self-contained, ~1200 lines.

**Pages:**
- **Customers list** — table of every `email_clients` row where `portal_enabled = 1`. Columns: name, slug, portal users count, then one column per service (LinkedIn, Facebook, Email, …). Each cell shows "✓ <name>" / "Not required" / "Coming soon". Two header buttons: "**+ New portal customer**" (creates a fresh row) and "**Enable existing customer**" (turns an existing email_client into a portal customer).
- **Manage panel** (per customer) — five sections, top to bottom: Services, Logo, Portal users, Portal URL, Hide-from-list (danger zone).

**Services section** — fully data-driven from the `services` catalogue table. One dropdown per service. For services with `link_table` set (e.g. LinkedIn → `clients`, Email → `email_clients`), the dropdown lists picker options from that table. For plain services (no `link_table`), the dropdown is just Enabled / Not required. For `state = 'coming_soon'` services, the dropdown is greyed out.

**Logo section** — file upload, image preview, replace + remove buttons. Uses `multer` + R2 (same setup as LinkedIn-side `clients.js`). Cross-syncs to the linked LinkedIn `clients` row's `logo_url` if any.

**Portal users section** — table with username, email, role, last sign-in, reset-password + remove buttons. "+ Add user" button opens a modal that creates a `client_users` row and shows a one-time temporary password (Wez copies it and gives to the customer out-of-band). Reset-password also shows the new temp password once.

**Portal URL section** — copy-to-clipboard button for `https://studio.thegreenagents.com/c/<slug>`.

**Backend file:** `server/routes/portal-admin.js`, mounted at `/api/portal-admin`. Goes through the existing admin Bearer-token middleware. **Endpoints:**
- `GET /services` — services catalogue
- `GET /service-options/:service_key` — picker rows for a service's `link_table` (with already-linked-elsewhere flag)
- `GET /customers` — portal customers list (filtered to `portal_enabled = 1`)
- `POST /customers` — create new portal customer; auto-slug; defaults email service off
- `GET /customers/:id` — single customer + users
- `PUT /customers/:id` — toggle `portal_enabled`
- `PUT /customers/:id/services` — body `{ services: { <key>: { subscribed, linked_external_id? } } }`. Validates against catalogue, transactionally upserts, syncs legacy columns
- `GET /customers/:id/users` — list portal users
- `POST /customers/:id/users` — create user, returns temp password ONCE
- `DELETE /users/:id` — remove user (cascades sessions, resets, login attempts)
- `POST /users/:id/reset-password` — admin reset, returns new temp password ONCE, kills sessions, resets `last_login_at = NULL` so the temporary-password banner reappears
- `GET /eligible-customers` — email_clients NOT yet portal-enabled (drives "Enable existing customer" picker)
- `POST /customers/:id/logo` — upload logo, cross-syncs to LinkedIn `clients.logo_url` if linked
- `DELETE /customers/:id/logo` — clear logo, cross-clears LinkedIn

**Cross-sync from LinkedIn side:** `server/routes/clients.js` `POST /:id/logo` was extended — after uploading to R2 and updating `clients.logo_url`, it also `UPDATE email_clients SET logo_url = ?` for any portal customer linked to this LinkedIn client (via `customer_services` OR the legacy `linkedin_client_id` column).

#### Customer side — the portal itself

**Frontend file:** `src/components/customer-portal/PortalApp.jsx`. Self-contained. Mounted by `App.jsx` based on URL pattern `/c/<slug>`. The fetch interceptor in App.jsx **deliberately excludes `/api/portal/*` from the admin Bearer token** — portal calls use their own session cookie.

**Pages (all in PortalApp.jsx):**
- **Login** — username + password form. Subtitle pulls customer name from `GET /api/portal/by-slug/:slug`. "Forgot password?" link opens a modal that posts to `/auth/forgot-password` (always returns 200 — never leaks whether email is registered).
- **Reset password** — mounted when URL has `?reset=<token>`. Two password fields → POST to `/auth/reset-password`.
- **Portal chrome** (after login) — sidebar with three sections (**Social posts** / **Email** / **Account**), workspace area on the right. Sidebar headings are bold uppercase with bright colour for clear visual hierarchy.
- **LinkedIn Posts** — full review grid mirroring admin layout exactly. Image at natural aspect, full body with "Read full post" expand, four buttons (Edit text / Rewrite post / New image / Approve), spinner overlays during regen. Below the live grid: "Past campaigns" section with read-only history of every `deployed` or `done` campaign for this client.
- **Inbox** — real `/api/portal/inbox` data. Click a row to open full reply detail (HTML body when present, text fallback) in an email-client-style modal. Reply button opens compose modal that sends via SES from the mailbox the original arrived on, with `In-Reply-To` and `References` for threading.
- **Campaigns** — real `/api/portal/campaigns` read-only list with sent/opens/clicks/replies counts. Aggregate stats strip at top. `tracking_off` campaigns show "—" for opens/clicks with explanatory note.
- **Settings** — change own password (real, working), organisation users (deliberately shows the signed-in user only with a "managed by The Green Agents" note — Wez's call, not a TODO).

**Header logo:** 44×44 box. Renders `client.logo_url` when set, falls back to initials on the brand colour. Logo arrives via `/api/portal/auth/check`.

**ServiceGate** — wraps each tab page. Reads `services.<key>` (one of `'enabled'` / `'not_required'` / `'coming_soon'`) and renders either the real tab contents OR a "Not required" / "Coming soon" panel. So a customer subscribed only to LinkedIn still sees the Inbox + Campaigns nav items but each shows a calm "Not required" panel instead of empty data.

**Auth backend file:** `server/routes/portal-auth.js`, mounted at `/api/portal`. Endpoints:
- `GET /by-slug/:slug` — public; returns `{ client_name, slug }`
- `POST /auth/login` — generic 401 on failure; lockout: 10 fails / 15 min per `(email_client_id, username)` triggers a 15-min cooldown
- `POST /auth/logout` — kills CURRENT session only
- `GET /auth/check` — validates cookie, bumps idle expiry, returns `{ user, client, services }`. The `services` payload is now a map of `{state, label, pitch}` per service, not bare strings. Read sites must use `svc.linkedin?.state`, etc. ServiceGate tolerates either shape for stale-cache safety.
- `POST /auth/change-password` — kills all OTHER sessions, keeps current; clears any pending reset tokens
- `POST /auth/forgot-password` — always returns 200; sends SES email from `studio@thegreenagents.com` if user exists; reset URL is `/c/<slug>?reset=<token>`
- `POST /auth/reset-password` — kills ALL sessions including current; user has to sign in fresh

**Data backend file:** `server/routes/portal.js`, also mounted at `/api/portal` (Express cascades through routers at the same prefix). All routes go through `requirePortalSession` middleware applied via `router.use()` at the top — the customer is always resolved from the cookie.

**Endpoints currently shipped (full chunk 3b complete):**

Posts (3a + 3b-i):
- `GET /posts` — most-recent `awaiting_approval` campaign for the linked LinkedIn client. Returns `{ posts, campaign, not_subscribed }`. Posts projected from `campaigns.posts_json` to portal-friendly fields including `topic`, `content_pillar`, `format`, `suggested_day`, `suggested_time` so the customer card mirrors the admin layout.
- `PUT /posts/:id` body `{ title, body }` — saves edits AND marks approved (single-click for the common "tweak then accept" flow).
- `POST /posts/:id/approve` — approve without editing.
- `POST /posts/:id/regenerate` — combined regen (text + image). Kept for safety but no longer wired to a UI.
- `POST /posts/:id/regenerate-text` — text-only regen via `regenerateSinglePost` (Anthropic). Drops approval, counts against 30/day cap.
- `POST /posts/:id/regenerate-image` — image-only regen via Gemini + R2 upload. Drops approval, counts against 30/day cap.
- `POST /campaigns/:id/posts/approve-all` — bulk approve. On full success: pushes posts sequentially to Supergrow as live `queue_post` calls in `posts_json` order, then sets `stage='done', status='completed', deployed_by='portal', completed_at=now()` so the campaign reaches the same terminal state as an admin push. On partial failure: stops at the first failure (no out-of-order queue), keeps stage at `awaiting_approval`, marks only the successful ones with `client_approved_at`, audits the partial state. Returns 207 with details.

History (3b-i):
- `GET /campaigns-history` — all campaigns where `stage = 'done'` for the linked LinkedIn client. `'done'` is now the single terminal stage; the legacy `'deployed'` value was migrated into `'done'+deployed_by='portal'` on next boot (decision #27). Returns full projected posts arrays so the frontend can expand a card without a second fetch.

Inbox (3b-ii):
- `GET /inbox` — last 100 replies for the customer's mailboxes (resolved via `customer_services` email link). Returns rows with `from_address`, `from_name`, snippet, classification, received_at, matched_campaign_title.
- `GET /replies/:id` — full reply detail including `body_html` and `body_text`. 404 (not 403) on cross-tenant lookups.
- `POST /replies/:id/send` body `{ cc, body }` — send via SES. From-address is the mailbox the original arrived on. Sets `In-Reply-To` and `References` headers (built from the inbound reply's `message_id` + `references_header`). Stores an `email_outbound` row regardless of SES outcome — `message_id` populated on success or `error` populated on failure.

Campaigns (3b-iii):
- `GET /campaigns` — read-only list of every email campaign for the linked email_client, newest first, with sent/opens/clicks/bounces/unsubs/replies counts. Reply count computed from `email_replies.matched_campaign_id` (no stored count column on email_campaigns). `tracking_off: true` on campaigns where both `track_opens` and `track_clicks` are false. Status normalised to four customer-friendly values: `scheduled` / `sending` / `sent` / `failed`.

**Critical security rule:** every portal route MUST resolve `email_client_id` from the session cookie via `req.portalClient.id`, then resolve linked-services ids via `resolveEmailClientId(...)` or `resolveLinkedinClientId(...)`. Never trust an `email_client_id`, `slug`, or `linked_external_id` passed in URL params or request body. Return **404** (not 403) for resources that exist but don't belong to the caller.

**Frontend state:** `PortalApp.jsx` is now fully wired — no `// TODO(backend chunk 3b)` markers remain. The mock data constants (`mockReplies`, `mockCampaigns`) are still declared at the top of the file but no longer referenced anywhere in the active render paths; left in place to avoid noise in the diff and can be cleaned up in a future polish chat.

#### Customer portal — generic services design

**Two tables drive the entire services system.** Adding a new service in future is a DB insert, not a code change.

**`services` table** — catalogue:
```
service_key      TEXT PRIMARY KEY
display_name     TEXT NOT NULL
description      TEXT                            -- admin-facing operator notes
customer_pitch   TEXT                            -- customer-facing sales copy
state            TEXT NOT NULL DEFAULT 'live'    -- 'live' | 'coming_soon' | 'retired'
link_table       TEXT                             -- SQL table for picker options, NULL for plain on/off
link_label       TEXT                             -- friendly label for the picker
sort_order       INTEGER NOT NULL DEFAULT 100
created_at       TEXT NOT NULL DEFAULT (datetime('now'))
```

Currently seeded with six rows (re-seeded every boot, idempotent — wording tweaks ship via deploy with no manual DB work):
- `linkedin` → `state='live'`, `link_table='clients'`, sort 10
- `facebook` → `state='coming_soon'`, sort 20, with sales pitch
- `instagram` → `state='coming_soon'`, sort 25, with sales pitch
- `tiktok` → `state='coming_soon'`, sort 28, with sales pitch
- `facebook_pixels` → `state='coming_soon'`, sort 29, with sales pitch
- `email` → `state='live'`, `link_table='email_clients'`, sort 30, with sales pitch

**To add SEO** in the future:
```sql
INSERT INTO services (service_key, display_name, description, state, sort_order)
VALUES ('seo', 'SEO', 'Search engine optimisation reports.', 'live', 40);
```
The admin UI immediately shows an SEO dropdown. The customer portal frontend would need a new `<NavItem>` for it — that's the only React change.

**To add Google Ads with picker:**
```sql
INSERT INTO services (service_key, display_name, ..., link_table, link_label, sort_order)
VALUES ('google_ads', 'Google Ads', ..., 'google_ad_accounts', 'Google Ads account', 50);
```
Plus add `'google_ad_accounts'` to `ALLOWED_LINK_TABLES` in `portal-admin.js` (whitelist for SQL safety).

**`customer_services` table** — subscriptions:
```
id                 INTEGER PRIMARY KEY
email_client_id    TEXT NOT NULL
service_key        TEXT NOT NULL
linked_external_id TEXT                       -- NULL for plain on/off services
enabled_at         TEXT NOT NULL DEFAULT (datetime('now'))
enabled_by         TEXT                       -- 'admin' | 'backfill' | etc.
```

Indexes:
- `UNIQUE (email_client_id, service_key)` — one subscription per service per customer
- `INDEX (service_key)` — for picker queries
- `UNIQUE (service_key, linked_external_id) WHERE linked_external_id IS NOT NULL AND email_client_id != linked_external_id` — partial index that prevents two different customers from claiming the same external record. **Self-links (e.g. an email_client linking to its own id as the email source) are exempt** — they represent "use my own data" defaults, not cross-customer claims.

**Legacy columns kept for backwards compat:** `email_clients.service_email_enabled` and `email_clients.linkedin_client_id` are kept and synced from `customer_services` via `syncLegacyColumns()` after every service change. They can be dropped in a future cleanup chat once nothing reads them.

#### Locked-in defaults (don't relitigate)

- Brute-force lockout: 10 failed login attempts within 15 min per `(email_client_id, username)` → 15-min lockout
- Password minimum: 8 characters, no other rules
- Session: idle 7d, absolute 30d
- Bcrypt cost factor: 12
- Session tokens: `crypto.randomBytes(32).toString('base64url')`
- Slugs: lowercase, ASCII only, non-alphanumeric runs → single dash, trim. On collision append `-2`, `-3`...
- New customer-user provisioning: admin sets temp password, portal shows "your password is temporary" banner
- Reset emails: SES, From `studio@thegreenagents.com`
- Customer "Approve all" → Supergrow live `queue_post` (NOT draft)

---

## What's NOT done (priorities for next chat)

### 1. Customer-portal smoke tests — what was verified this chat

Several end-to-end flows were tested and confirmed working in production:

- **Approve all → Supergrow live queue** ✅ Wez ran a 12-post campaign for Cube6 (and later Sweetbyte), customer clicked Approve all, posts landed in Supergrow's scheduled queue in order. Confirmed.
- **Branding tile on Settings** ✅ Was hardcoded "Tower Leasing" mock data — replaced with real client object from `/auth/check`.
- **Inbox badge for non-campaign emails** ✅ Was misleadingly applying classifier output to random INBOX traffic. Now shows neutral grey "Normal email" badge when `campaign_title` is null.
- **Customer portal grid layout** ✅ Was forced 2-column; matches admin's responsive auto-fill now.
- **Past-campaign history** ✅ Visible after deploy fix (legacy `'deployed'` rows migrated to `'done'`).
- **Customer-approved campaign read-only view** ✅ Admin opens a deployed campaign → blue banner, no regen buttons, no Re-send button. Header reads "Customer approved & sent to Supergrow."

Verified during the second 2026-05-08 session:

- **`email_clients.source` filter** ✅ After deploy and migration, Cube6/Mansons/Tower Leasing disappeared from Email Campaigns customers list while their portals continued working. Confirmed by Wez.
- **Phase 4 sequence editor** ✅ Already shipped pre-this-session — confirmed by reading the live code (the previous blueprint's "still missing" claim was stale).
- **Per-step subjects** ✅ Shipped this session; Wez has not yet sent a multi-step campaign to confirm in production but the plumbing is end-to-end (frontend → backend → DB → drip-ticker → SES `subjectOverride`).
- **Verdana 12 default** ✅ Shipped this session, not yet confirmed by recipient inspection — worth a test send to a personal address and Show original to confirm the wrap CSS is applying.
- **Customer portal post action button styling** ✅ All four buttons now solid TGA-green with white text. Shipped this session.
- **Customer portal post-action 404 bug (numeric ID + missing client_id)** ✅ Two fixes shipped this session in sequence; Wez confirmed Tower Leasing posts now respond to all four action buttons.
- **Default-sender fields on customer detail header** ✅ Shipped this session. Three fields (Default From name, Default From email, Test send address) now inline-editable with debounced auto-save. New campaigns also default Reply-to from `default_from_email`.

Still NOT confirmed end-to-end:

- **Inbox reply send via SES with threading.** Code shipped, but no verified test of a real reply landing back in Gmail/Outlook with proper `In-Reply-To` and `References` headers. Worth testing with `Show original` next chat.
- **Campaigns view with real numbers.** Code shipped, depends on the customer having real `email_campaigns` rows on their linked email_client.
- **Per-step subject end-to-end.** No production multi-step send has happened yet — drip-ticker path with `subjectOverride` is unverified by real recipient inspection.
- **Logo trim-at-upload + proportional padding + Gemini halo fix.** Shipped this session (decisions #54/#55/#53). Wez confirmed the original variance is gone after the trim moved to upload time. Tower Leasing's regenerated Cube6-style cleared zone halo confirmed visually fixed by softening the prompt rule. Padding tuning landed on proportional 20% scaling after a brief detour through fixed 40 (too generous on Cube6) and fixed 20. Final visual confirmation across multiple customers' regens still pending.
- **Default-sender persistence + auto-fill on New Campaign.** Shipped this session (decision #51). Wez confirmed both the persistence and the auto-fill behaviour after deploy.
- **Auto-unsubscribe gate on `matched_campaign_id`.** Shipped this session (decision #52). Behaviour change is silent — no UI surface — so confirmation requires watching `[classifier]` log lines on Render: lines that previously read `auto-unsubscribed N subscriber(s) matching <addr>` should now only appear for sender addresses that genuinely sent into a campaign reply.

### 2. Active polish backlog

Items raised across recent chats that are real bugs or improvements but not yet shipped:

- **Gmail spam folder isn't polled.** `imap-poller.js` opens `INBOX` only. Real campaign replies that Gmail's spam filter shoves into `[Gmail]/Spam` are invisible to the portal. Worth adding a second pass that polls Spam and surfaces matches with a "Gmail thinks this is spam — but it's a reply to your campaign" indicator.
- **Sweetbyte's logo file is a JPEG renamed to .png.** With the always-white-panel path (decision #42) and the upload-time trim+flatten (decision #54), output is now clean. Real fix is to ask the customer to upload a proper transparent PNG. Console warning shipped to surface this for future cases.
- **Mock data constants left declared.** `mockReplies`, `mockCampaigns`, `mockClient` at the top of `PortalApp.jsx` are no longer referenced — harmless unused JS but worth a small cleanup.
- **Orphan `src/components/PortalApp.jsx` (no subfolder).** The active portal lives in `src/components/customer-portal/PortalApp.jsx`. The orphan is the misplaced original from a build-failure fix and nothing imports it. Wez to delete locally on Windows.
- **Rich-text editor still uses inline padding.** Recipient-side rendering in Outlook still depends on `wrapBodyWithEmailCss`. Decision unchanged from previous chats.
- **Old un-trimmed R2 logo objects.** The boot-time backfill (#56) leaves the original raw logo files in R2 in place (it points the DB at the new trimmed file but doesn't `DeleteObject` the old one — avoids race-with-stale-frontend-tab). Storage cost is negligible at current scale; an R2 lifecycle policy could clean these up if it ever matters.

### 3. Phase 4 multi-step follow-up sequences — what's still missing

Schema, runtime, and editor UI are all live (see "Phase 4 multi-step sequences" under Current state). What's left:

**Stop-condition filter.** The drip ticker doesn't yet halt the sequence for subscribers who replied with `positive` / `soft_negative` / `hard_negative`. They keep getting follow-ups. Fix is small: extend the candidate query in `services/drip-ticker.js` with `AND NOT EXISTS (SELECT 1 FROM email_replies WHERE matched_campaign_id = c.id AND from_subscriber_id = s.id AND classification IN ('positive','soft_negative','hard_negative'))`. This is the most consequential remaining piece — without it customers keep getting nudged after they've already said no.

**Per-step reporting in `CampaignRecipientsPanel`.** Recipients view doesn't yet show "last step sent" per row. Useful for operators to see how far through the sequence each subscriber has reached. Schema supports it (`email_sends.step_number` is populated correctly).

**Edit-warning toast.** State is wired (sent_count > 0 flag flows from backend to UI per step), but the modal doesn't yet show a confirmation gate before saving an edit to a step that's already been sent. Helper text appears on the step itself but doesn't block.

### 4. SNS subscription wiring (USER ACTION ONLY, no code)

Endpoint code is done. User adds HTTPS subscriptions on AWS console.

### 5. Phase 5: Send & reply from inside the studio inbox (NOT STARTED)

Plain text v1. Use email_client name as from-name. Compose new + reply to existing.

**Build:**
- `npm install nodemailer`
- `services/smtp-sender.js` using `smtp.gmail.com:465` with the IMAP app password
- IMAP APPEND to Gmail Sent folder so user's Gmail shows the reply
- Don't reprocess our own sends as inbound (track sent message-ids)
- Compose modal with To/Subject/Body + Reply button

### 6. Phase 6: Postmaster Tools + SNDS (USER ACTION ONLY)

Direct user to set up `postmaster.google.com` and `sendersupport.olc.protection.outlook.com/snds/`.

### 7. Smaller backlog items

- **Drip starts immediately when scheduled today past window:** add a "fires next at..." line in the campaign queue
- **Reply matching for self-tests is broken** by design (Gmail rewrites threading between Workspace accounts)
- **Audit-log viewer** UI for `GET /api/email/audit-log`
- **`export default router` mid-file in routes/email.js** — cosmetic, not blocking
- **Drip reset on edit confirmation dialog** — currently changes apply tomorrow
- **Stale-brief warning** is currently 14 days. May want tuning based on observed cadence.
- **Reply send retry on transient SES failure.** Currently fails fast with a 502 + error banner. Could add 1 retry with backoff for transient throttle errors.
- **Inbox infinite scroll / search.** Capped at 100 most-recent replies. If a customer has thousands, older ones are invisible.
- **Visible feedback when a service is set to "Not required"** — subtle warning text on the Manage panel.
- **Banner at top of Manage panel when zero services are subscribed:** "This customer has no services enabled — they'll see 'Not Subscribed' on every tab."
- **Greater visual distinction for the Save Services button when dirty** — currently relies on opacity change which can be missed.

---

## Database schema (current)

Migrations run on every boot in `server/db.js`. SQLite at `/var/data/studio.db` on Render.

### Core tables (Phase 1-2)
- `email_clients` — top-level grouping. Columns: `id, name, color, slug, default_from_email, default_from_name, portal_enabled, service_email_enabled, linkedin_client_id, logo_url, logo_processed_at, source, ...`
  - `source` (decision #41): one of `'aws_domain'` (auto-pulled from / matched to AWS verified domain), `'manual'` (operator-created with non-domain name), `'portal'` (auto-anchor for a LinkedIn-only customer's portal). The Email Campaigns Customers list filters out `'portal'` rows. Migration on first boot classified all 13 existing rows.
  - `logo_processed_at` (decision #54/#56): NULL for rows where the logo file in R2 is the raw uploaded form. Non-NULL ISO timestamp once `services/logo-prep.js` has trimmed the file and the canonical pre-trimmed version is what `logo_url` points at. Boot-time backfill (#56) walks NULL rows and processes them. New uploads stamp it on success.
- `clients` — LinkedIn-side customers (separate table from `email_clients`, see decision #11). Columns include the same `logo_url` and `logo_processed_at` pair (#54/#56). Also `content_rules TEXT` (decision #61) — JSON-encoded array of `{id, text}` customer-defined override rules for post generation. NULL or empty array = no rules. Read fresh on every AI call (no caching) so edits/deletes apply immediately. Also `logo_position` (default `'bottom-right'`), `logo_panel` (default `'white'`), `logo_size` (default `'small'`) — the per-customer Brand-panel settings from decision #64, added via additive boot migrations; all default to pre-#64 behaviour so existing customers are unchanged. `compositeLogo` in `gemini.js` reads these; the per-post overrides (decision #65) are stored in `posts_json` and fall back to these customer-level defaults when null.
- `email_brands`, `email_lists`, `email_subscribers`, `email_campaigns`, `email_sends`, `email_link_clicks`, `email_campaign_links`, `email_sns_events`

### Phase 3 tables
- `email_inboxes` — Gmail mailboxes, AES-256-GCM `app_password_encrypted`, `last_uid` for IMAP poll
- `email_replies` — every email fetched, with classification fields
- `email_audit_log`

### Phase 4 — multi-step sequences (BUILT and live)
- `email_campaign_steps(id, campaign_id, step_number, subject, html_body, delay_days, created_at)` — UNIQUE on `(campaign_id, step_number)`. `subject` is NULL for steps that should fall back to `email_campaigns.subject` at send time (decision #44). Step 1 always has its subject mirrored to `email_campaigns.subject`.
- `email_sends.step_number INTEGER NOT NULL DEFAULT 1` + index `(campaign_id, subscriber_id, step_number)`

### Customer portal (FULLY LIVE)
- `client_users (id, email_client_id, username, email, password_hash, role, created_at, last_login_at)` — UNIQUE on `(email_client_id, username)`
- `client_sessions (id [token], client_user_id, expires_at, created_at)` — idle 7d, absolute 30d
- `password_resets (id [token], client_user_id, expires_at [+1h], used_at, created_at)`
- `client_login_attempts (id, email_client_id, username, attempted_at)` — for brute-force lockout
- `client_post_regens (id, email_client_id, client_user_id, campaign_id, post_id, created_at)` — for daily 30/customer regen cap
- `email_outbound (id, email_client_id, in_reply_to_reply_id, client_user_id, from_address, to_address, cc_address, subject, body_text, body_html, message_id, in_reply_to_header, references_header, sent_at, error)` — outbound SES sends from BOTH the portal reply form AND the admin reply form (decision #80). `client_user_id` is NULL for admin sends, populated with the client_users row for portal sends.
- `services (service_key, display_name, description, customer_pitch, state, link_table, link_label, sort_order, created_at)` — services catalogue, six rows seeded (linkedin/facebook/instagram/tiktok/facebook_pixels/email)
- `customer_services (id, email_client_id, service_key, linked_external_id, enabled_at, enabled_by)` — subscriptions, with two UNIQUE indexes (one full, one partial-and-self-link-exempt)
- `campaigns.deployed_by` column — `'admin'` or `'portal'`. Distinguishes admin-deployed campaigns from customer-portal-deployed ones. Drives the "Customer approved" pill + read-only banner in the admin UI.

### CRM — Hot Prospects (decisions #76-77, #85, #86, BUILT and live)
- `hot_prospects (id, email_client_id, prospect_email, prospect_name, follow_up_date, notes, added_by, added_at, updated_at, closed_at, closed_by)` — per-customer prospect rows. Indexes: UNIQUE on `(email_client_id, prospect_email)` (which makes "add the same prospect twice" an upsert at the route layer); `(email_client_id, added_at DESC)` for the newest-first list; partial index on `(email_client_id, follow_up_date) WHERE follow_up_date IS NOT NULL` for follow-up queries; partial index on `(email_client_id, closed_at) WHERE closed_at IS NOT NULL` for the converted-prospects filter (decision #86).
  - `prospect_email` is stored lowercased + trimmed. All reads against `email_replies.from_address` and `email_outbound.to_address` use `LOWER()` to match. The thread endpoint (`GET /:id/thread`) builds the live conversation by joining these two tables — no separate copy on the prospect row, so new mail auto-appears.
  - `follow_up_date` is `TEXT` in ISO `YYYY-MM-DD` format. `NULL` means "no follow-up set".
  - `added_by` is `'admin'` for admin-side adds, `'portal:<client_user_id>'` for portal-side adds, auditable forever.
  - `closed_at` is `TEXT` ISO datetime stamped when the prospect is marked converted (decision #86). `NULL` = active. `closed_by` is `'admin'` or `'portal:<client_user_id>'`. Converted prospects are excluded from the sidebar urgent badge and from the per-customer switcher count, but stay in the prospect list (pushed to the bottom under a "Converted (N)" divider, with a green pill). Reopening clears both columns and the prospect is active again.
  - **Ownership note (decision #85):** an `email_client_id` on a `hot_prospects` row identifies "the inbox the prospect was added against", NOT "the only customer who owns this row". Customers with linked email rows (anchor + inbox-owning row joined via `customer_services.service_key='email'.linked_external_id`) own the prospect collectively — every hot-prospects read path expands ownership via `resolveLinkedSet(emailClientId)` (in `routes/hot-prospects.js`) / `_resolveProspectLinkedSet()` (in `routes/portal.js`). Self-linked customers (e.g. Manson, where `linked_external_id = email_client_id`) are a 1-row set — no expansion needed. Use the helper for ANY future feature that filters or writes against `email_client_id` on this table.


### Per-post approval state (inside `posts_json`, not a table)
Each post object inside `campaigns.posts_json` may have:
- `client_approved_at` — ISO timestamp from customer portal approve action
- `client_approved_by_user_id` — which `client_users` row clicked approve

Decision locked: keeping in JSON beats a sidecar table because all other per-post state (`linkedin_post_text`, `image_url`, `app_url`, `image_error`) lives there too.

### Helper attached to db object
- `db._portalUniqueSlug(name, excludeId)` — slug helper. Called by `routes/email.js` POST /clients and `routes/portal-admin.js` POST /customers.

---

## Important env vars (Render)

| Var | Purpose |
|---|---|
| `STUDIO_PASSWORD` | Admin Bearer auth |
| `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` | SES + SNS |
| `AWS_SES_REGION` | `eu-north-1` |
| `SES_CONFIGURATION_SET` | `studio-no-tracking` |
| `MAILBOX_ENCRYPTION_KEY` | 32-byte base64 for IMAP password encryption — **never rotate without migration** |
| `PUBLIC_URL` | base URL for tracking + reset links |
| `DB_PATH` | `/var/data/studio.db` |
| `ANTHROPIC_API_KEY` | Haiku for name parser, reply classifier |
| `NAME_PARSER_MODEL` / `CLASSIFIER_MODEL` | override Haiku model id (defaults to `claude-haiku-4-5-20251001`) |
| `R2_ENDPOINT` / `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` / `R2_BUCKET_NAME` / `R2_PUBLIC_URL` | Cloudflare R2 for images and logos |
| `IDYQ_BRIDGE_SECRET` | shared secret for IDYQ admin embed (decision #59). Must match `STUDIO_BRIDGE_SECRET` on IDYQ's Render service. Long random hex. |
| `IDYQ_BASE_URL` | optional override for the IDYQ target URL — defaults to `https://idoyourquotes.com`. Useful for staging environments. |
| `NODE_ENV` | `production` on Render — drives Secure cookie flag for portal sessions |

If you ever see `[name-parser] AI fallback failed: ...model not found...` in Render logs, set `NAME_PARSER_MODEL` to whatever the current Haiku string is (check `https://docs.claude.com`). Same for `CLASSIFIER_MODEL`.

---

## File map (current)

```
server/
  index.js                        — boots app, mounts all routers, starts pollers/cron
                                    Mounts:
                                      /api/auth          → auth.js (admin login)
                                      /api/clients       → clients.js (LinkedIn/Supergrow)
                                      /api/campaigns     → campaigns.js (LinkedIn campaigns)
                                      /api/email         → email.js
                                      /api/algorithm     → algorithm.js
                                      /api/portal        → portal-auth.js (auth) + portal.js (data)
                                      /api/portal-admin  → portal-admin.js (admin-only)
                                      /api/idyq-bridge   → idyq-bridge.js (admin-only, App integration)
                                      /api/email/hot-prospects → hot-prospects.js (admin-only, CRM —
                                        mounted BEFORE /api/email so the more specific path matches first)
                                    Process-level uncaughtException/unhandledRejection
                                    handlers belt-and-braces against ImapFlow event
                                    emitter quirks that would otherwise terminate the
                                    process.
                                    After app.listen, kicks off backfillLogos() fire-
                                    and-forget so any pre-#54 raw logo files in R2
                                    get re-trimmed in the background (decision #56).
  db.js                           — schema + migrations (run every boot, additive only)
                                    Note: linkedin_settings UPDATE happens BEFORE its CREATE TABLE
                                    line; works on existing prod DBs because the table was
                                    created on a prior deploy. Would crash on a fresh DB.
                                    Not blocking but worth fixing in a cleanup chat.
  middleware/auth.js              — Bearer token check (admin)
  routes/
    auth.js                       — admin login
    clients.js                    — Supergrow clients (NOT email_clients).
                                    POST /:id/logo cross-syncs to email_clients.logo_url.
                                    Logo upload now trims via prepareLogoForStorage
                                    BEFORE the R2 PutObject (decision #54) and stamps
                                    logo_processed_at on both clients + linked
                                    email_clients rows.
    campaigns.js                  — Supergrow campaigns (NOT email_campaigns)
    email.js                      — ALL email-side routes. 2050+ lines. POST /clients
                                    auto-generates slug via db._portalUniqueSlug AND classifies
                                    new rows as 'aws_domain' (matches verified domain) or
                                    'manual'. GET /clients filters out source='portal' rows.
                                    PUT /clients/:id accepts partial-body updates and
                                    returns the full row — used by the inline-editable
                                    header fields on the customer detail page (#50/#51).
                                    PUT /campaigns/:id/steps now accepts per-step subjects.
                                    POST /replies/:id/send is the admin reply endpoint
                                    (decision #80) — mirrors the portal version, sends from
                                    the customer's mailbox with default_from_name, threads
                                    via In-Reply-To + References, persists email_outbound
                                    row up front + audit-logs as actor='admin'.
                                    NB: export default router appears mid-file with routes
                                    after it. Works (export-binding semantics) but confusing.
    algorithm.js                  — Supergrow algorithm
    portal-auth.js                — customer-portal auth + requirePortalSession middleware
                                    Exports requirePortalSession for use by portal.js.
    portal.js                     — customer-portal data routes. ALL OF CHUNK 3b SHIPPED.
                                    20 routes: GET /posts, PUT /posts/:id, POST /posts/:id/approve,
                                    POST /posts/:id/regenerate (legacy combined), POST /posts/:id/regenerate-text,
                                    POST /posts/:id/regenerate-image, POST /campaigns/:id/posts/approve-all,
                                    GET /campaigns-history, GET /inbox, GET /replies/:id,
                                    POST /replies/:id/send, GET /campaigns,
                                    GET /content-rules, PUT /content-rules (decision #61),
                                    plus 5 CRM endpoints (decision #76-77):
                                    GET /hot-prospects, POST /hot-prospects (returns was_new),
                                    PUT /hot-prospects/:id, DELETE /hot-prospects/:id,
                                    GET /hot-prospects/:id/thread (live-joined from
                                    email_replies + email_outbound).
                                    Both post-regen paths run validatePostAgainstRules after
                                    rewriting — flagged posts get validation_warnings
                                    attached for portal UI display.
    portal-admin.js               — admin-side customer-portal management. POST /customers
                                    inserts new portal anchor rows with source='portal'.
                                    The shared uploadLogoToR2 helper trims via
                                    prepareLogoForStorage before PutObject (#54). The
                                    /customers/:id/logo route stamps logo_processed_at
                                    on both email_clients + linked clients rows.
                                    POST /users/:id/reset-password accepts optional
                                    {new_password} body — admin-chosen if supplied,
                                    random generated otherwise (decision #60). Response
                                    includes kind: 'admin_chosen' | 'random'.
    idyq-bridge.js                — App integration. GET /url mints a 60-second HMAC-signed
                                    ticket using IDYQ_BRIDGE_SECRET, returns full URL
                                    pointing at IDYQ's /admin-bridge endpoint. Requires
                                    requireAuth (admin Bearer). Decision #59.
    hot-prospects.js              — CRM admin endpoints (decisions #76-78). Six routes:
                                    GET / (list per customer), GET /customers (drives the
                                    customer-switcher badges — broader filter than
                                    /api/email/clients per #78), POST / (upsert, returns
                                    was_new), PUT /:id, DELETE /:id, GET /:id/thread.
                                    Mounted at /api/email/hot-prospects BEFORE /api/email
                                    so the more specific path matches first.
  services/
    ses.js, tracking.js, touch-count.js, crypto-vault.js, imap-poller.js,
    name-parser.js,
    classify-replies.js          — three-pass reply classifier. Auto-unsubscribe
                                    path is now gated on reply.matched_campaign_id
                                    being non-null (decision #52) so non-campaign
                                    inbox traffic can never trigger it.
    drip-ticker.js               — extends candidate list with follow-up step sends; passes
                                    each step's subject + body through to sendCampaign as
                                    subjectOverride/bodyOverride.
    gemini.js                    — Nano Banana image gen + unified-panel logo compositor
                                    (decision #42). One panel path for every logo.
                                    No trim step — that lives in logo-prep.js (#54).
                                    Padding is proportional: 20% of the logo's
                                    smaller resized dimension, floor 8px (#55).
                                    The "BOTTOM RIGHT CORNER" prompt rule was
                                    softened to stop Gemini rendering a visible
                                    cleared zone behind the panel (#53).
    logo-prep.js                 — One-shot trim at upload time. Exported
                                    prepareLogoForStorage(buffer, mimetype) →
                                    {buffer, mimetype, ext}. Used by both
                                    upload routes BEFORE R2 PutObject. SVGs
                                    pass through untouched. Decision #54.
    logo-backfill.js             — Boot-time pass that re-trims every existing
                                    logo (clients + email_clients rows where
                                    logo_processed_at IS NULL). Idempotent.
                                    Fire-and-forget from server/index.js after
                                    app.listen. Decision #56.
    content-rules.js             — "Refine my posts" support. Three exports:
                                    parseRules(client), renderRulesPromptBlock(rules, {failedRules}),
                                    validatePostAgainstRules(postText, rules) → {ok, violations}.
                                    Uses Claude Haiku 4.5 for the validation pass.
                                    Fails open on errors. Decision #61.
    claude.js (DEAD — no imports), openai.js, supergrow.js, etc.
src/
  App.jsx                         — auth + dashboard mount; mounts PortalApp at /c/<slug>;
                                    fetch interceptor excludes /api/portal/* from admin Bearer
                                    AND adds Bearer to every /api/* request including /api/idyq-bridge/
  components/
    Sidebar.jsx                   — left nav. Sections: Social Media Posts, Email Campaigns,
                                    Customer Portal, CRM (decision #79), App (IDYQ — decision #59).
                                    Mailboxes badge polls every 30s.
    Dashboard.jsx                 — view router. Handles view='apps-idyq' by mounting
                                    components/apps/IDYQAdmin.jsx, view='crm-hot-prospects'
                                    by mounting CrmHotProspects.jsx. Listens for the
                                    'studio:navigate' custom event so other components
                                    (e.g. EmailSection's Open-in-CRM link) can switch
                                    the top-level view without lifting state. Coming-soon
                                    placeholder component AdminComingSoon renders FB/Insta/
                                    TikTok/FBPixels tabs until their admin UIs ship.
    apps/
      IDYQAdmin.jsx               — embeds idoyourquotes.com/admin via signed-ticket bridge.
                                    Fetches GET /api/idyq-bridge/url on mount, drops the
                                    returned URL into an iframe. Failure modes: yellow error
                                    card (Studio backend), blank iframe (frame-blocking on
                                    IDYQ side), IDYQ Access Denied card (cookie didn't stick).
                                    Decision #59.
    EmailSection.jsx              — THE BIG ONE. 3400+ lines. Email-related everything.
                                    CampaignModal contains the Phase 4 sequence editor:
                                    tab strip ("1st contact / 2nd contact / + Add contact"),
                                    per-step subject + body + delay-days. loadAll() syncs
                                    AWS verified domains into the customers list every time
                                    the page opens. ReplyDetailModal: pinned-header layout
                                    (decision #82) keeps Reply / Send-to-Hot-Prospects /
                                    Classify / Unsubscribe / Reclassify / Close always
                                    visible; body scrolls below. AdminComposeReplyModal
                                    sends replies via POST /api/email/replies/:id/send
                                    (decision #80). Send-to-Hot-Prospects flow + banner +
                                    Open-in-CRM handoff (decision #81) lives here too.
                                    Mailbox auto-select uses the functional setter form
                                    so the 30-second poll doesn't reset selection (#83).
    ClientDetail.jsx              — LinkedIn-side client detail. Logo upload here cross-syncs
                                    to portal customers.
    PortalAdmin.jsx               — admin Customer Portal management (~1200 lines)
    CrmHotProspects.jsx           — admin CRM screen (decision #79). Self-contained
                                    component, ~750 lines. Customer-switcher badges row
                                    (broader filter than Email Campaigns — includes
                                    portal-only customers per #78). Prospect list +
                                    detail modal with editable follow-up + notes (1s
                                    debounce auto-save) + live email-history thread.
                                    Reads two localStorage keys on mount —
                                    studio.crm.hot_prospects.last_customer_id (remembers
                                    which customer was last viewed) and
                                    studio.crm.hot_prospects.open_prospect_id (one-shot
                                    handoff from EmailSection's Open-in-CRM link, cleared
                                    immediately on read so it only triggers once).
    customer-portal/
      PortalApp.jsx               — full customer-facing portal (~3250 lines after the
                                    CRM additions in decision #84). Includes login, reset
                                    password screen, all five tabs (LinkedIn Posts, Inbox,
                                    Campaigns, CRM Hot Prospects, Settings), settings,
                                    modal forms. ReplyDetailModal has the same pinned-
                                    header layout as the admin one (decision #82) and
                                    the Send-to-Hot-Prospects flow scoped to the signed-in
                                    customer. Cross-page handoff between Inbox and CRM
                                    uses lifted state (crmOpenProspectId on PortalChrome
                                    + navigateToCrmWithProspect callback), NOT localStorage
                                    — everything lives in one component tree so callbacks
                                    are cleaner. PortalHotProspects + 6 helper components
                                    (PortalProspectList/Row/DetailModal/DetailBody/
                                    ThreadList/Message) are inline in this file per the
                                    single-file portal convention.
    RichTextEditor.jsx            — contentEditable wrapper. Defaults to Verdana 12pt
                                    (decision #45). Toolbar has font-family + font-size
                                    pickers; operator can override per selection.
```

---

## Things that have caused pain (lessons)

1. **Always parse-check before claiming success.** EmailSection.jsx is 3000+ lines, PortalApp.jsx is 2000+. str_replace can silently delete lines if context isn't unique. Path: `/home/claude/.npm-global/lib/node_modules/tsx/node_modules/esbuild/bin/esbuild --loader:.jsx=jsx --log-level=warning <file> > /dev/null && echo OK`. Same for Node files: `node --check <file>`.

2. **str_replace + similar code blocks** — when several modals all start the same way, str_replace refuses with "found multiple times". Add 2-3 lines of unique surrounding context to disambiguate.

3. **MIME encoding bugs are subtle** — declaring quoted-printable but not actually QP-encoding the body works for ASCII but corrupts URLs. Stick with base64.

4. **The user pushes via GitHub Desktop on Windows** — can't run `npm install` themselves. If you add a dep to package.json, Render auto-installs on deploy. Don't ask user to run commands.

5. **The user values honest pushback over agreement.** When a pre-decision turns out wrong once you're knee-deep (e.g. "no posts table exists, can't extend it"), surface it. Don't silently work around. The user explicitly asked for this throughout.

6. **Big design changes deserve mockups before code.** When user asks for new UI, build interactive HTML mockups first (`visualize:show_widget` tool). Confirm direction before writing React.

7. **Browser editor visual ≠ recipient visual.** RichTextEditor uses lineHeight 1.6 and `<p>` blocks. Recipients see Outlook's `<p>` margins on top of that. `wrapBodyWithEmailCss` in ses.js compensates server-side.

8. **SQLite timestamps need timezone treatment.** `datetime('now')` returns a UTC string with a space and no Z. Browsers parse that as local time. Fix: detect format, append `Z` before constructing Date.

9. **Tool reminders / yellow toast** in claude.ai sometimes fire after long tool calls. Conversation usually still completes correctly. Tell user to refresh if it blocks.

10. **Phantom `last_uid`** — mailboxes connected pre-3.1.5 had `last_uid` set to current. Resync button forces fresh 30-day backfill.

### Lessons specific to the customer-portal work (earlier chats)

11. **Don't confuse "admin can save it" with "deployed and tested."** When Wez saves a service config, the database changes — but the customer-side portal might be cached, signed in pre-config, etc. Always tell Wez to hard-refresh (Ctrl+F5) the customer side after admin changes, especially if testing in a different tab.

12. **The two `clients` tables are a fact, not a problem to solve.** `clients` (LinkedIn) and `email_clients` (cold email) are independent. The customer portal joins them via `customer_services.linked_external_id`. This is the correct design and should NOT be merged or "cleaned up" without a major refactor that's well outside the portal scope.

13. **Self-links on `customer_services` are normal.** When a cold-email customer's portal points at its own email_clients row, that's the natural default. Don't add UNIQUE constraints that block it. The fix shipped: partial UNIQUE with `email_client_id != linked_external_id`.

14. **Generic services beats per-service columns once you hit service #3.** Adding Facebook would have been "another column"; adding SEO, Google Ads, etc. would have multiplied the cost. The `services` + `customer_services` design is now the source of truth — adding a new service is a DB insert.

15. **Backfill self-links explicitly when migrating.** When we moved from `service_email_enabled` (bool) to `customer_services` (rows with picker), every existing email-enabled customer needed a `customer_services` row pointing at itself. Without that, they'd silently lose email service on the next save. The migration handles this with `INSERT OR IGNORE` + a one-shot UPDATE for any rows already inserted with NULL.

16. **Mounting two routers at the same prefix works in Express.** `app.use('/api/portal', authRouter)` and `app.use('/api/portal', dataRouter)` both apply at the same prefix; requests cascade through them and each only matches its declared routes. The middleware `requirePortalSession` on `dataRouter` only runs for requests routed to it, not auth routes. Correct mounting order: auth first, data second.

17. **Don't fake tool calls.** I made one mistake this chat where I wrote `[USl_replace]` and "Successfully replaced string" as plain text instead of actually calling tools. No edits happened. The user caught it. **Always use real tool calls; if you see plain-text "tool result" lines without a real tool call confirmation, redo the work.**

### Lessons specific to chunk 3b (earlier chat)

18. **Read the Render logs before guessing.** When the user reported HTTP 500 on GET /posts after chunk 3b-i deploy, I spent multiple turns inspecting code looking for an import error. The actual cause was a `SELECT id, title, ... FROM campaigns` — `title` doesn't exist as a column on the `campaigns` table — and was visible in the logs immediately. Lesson: when a 500 hits, **ask for the Render log first**, don't guess from the code.

19. **`useState` calls must be above all early returns.** When extending `PortalPosts` with new state for chunk 3b-i (busy map, expandedId, bulkBusy, etc.), I initially put the new `useState` calls below the loading/error/empty early returns. React's rules-of-hooks require hooks to run in the same order on every render — putting them after a `return` violates this. Fixed by moving all hook calls to the top of the function.

20. **`generatePosts` web search was the failure mode.** On 2026-05-07, Cube6's campaign failed with "Claude did not return valid JSON: I need to conduct the required research before generating the posts." The model decided to research first and emitted a text response instead of JSON. Web search has now been removed from `generatePosts` (decision #25). The LinkedIn algorithm context comes from the stored `algorithm_brief` instead. Both `generatePosts` and `regenerateSinglePost` also got automatic JSON-parse retry — up to 2 attempts.

21. **Sidebar colour consistency was a real issue, not just aesthetic.** Admin and customer portal sidebars used different greens (`#0F6E56` vs `#0e3b2d`). Customer noticed immediately when comparing screenshots. Lesson: "consistent branding" means literal colour values, not "they're both green-ish."

22. **Mirror-the-admin meant exactly that.** The customer-portal post card had been built independently with truncated text, force-cropped 1.91:1 images, two action buttons. The admin used full text + natural-aspect images + three action buttons. Customer's instruction was "mirror the admin exactly" — which surfaced a series of decisions (separate Rewrite/New image buttons, hide "View in Supergrow", combined or separate regen cap) that needed pre-confirmation before code. Building those mockups first, getting calls on each, then writing code in one shot was the right path.

### Lessons specific to this chat (campaign desync + logo handling + services expansion)

24. **Two stages doing the same job is one too many.** Customer-portal approve-all originally wrote `stage='deployed'`, while admin deploy wrote `stage='done'`. The admin's STAGES list only knew `done`. Result: opening a customer-deployed campaign showed broken UI with active regen buttons that could overwrite live posts. Lesson: when adding a workflow that ends in the same place an existing workflow ends, **use the same terminal value and add a discriminator column** (e.g. `deployed_by`) — don't create a parallel terminal value.

25. **Stage guards belong on every endpoint that mutates campaign state.** Once a campaign is deployed (live in Supergrow's queue), edit-post, regenerate-post, and regenerate-image all need to refuse. Easy to forget one. Wez surfaced this when he opened a deployed campaign and the UI offered regen buttons that would have blown away live posts.

26. **The IMAP poller bug had been there from day one.** ImapFlow emits `error` events on the EventEmitter that, without listeners, kill the Node process. Dropped seconds of sleeping connections all the way back to Phase 3.1. Render auto-restarts the process, masking the issue — but each crash also kills the drip ticker and reply classifier. Lesson: **when seeing process restarts in Render logs, look for unhandled emitter errors before assuming it's a deploy event.** Always `process.on('uncaughtException', ...)` and `process.on('unhandledRejection', ...)` at boot — belt-and-braces.

27. **Auto-detect logo background, don't assume a single shape.** Three real customer cases fell out from "logos" being treated as one thing: transparent PNG (Cube6), opaque white-bg PNG (Tower Leasing), JPEG-renamed-to-PNG with dark fill (Sweetbyte). The original code added a contrast patch to all of them, which produced visible artifacts on Tower Leasing and a black rectangle on Sweetbyte. Lesson: when handling user-uploaded image assets, **inspect the actual pixel data and branch on what's actually there**, don't trust file extensions or assume one canonical shape.

28. **JPEG noise is a real concern for image compositing.** Sweetbyte's JPEG had pure-white corners (255,255,255) but JPEG compression introduces mild colour noise at flat-area boundaries — values like (252,253,255) right at the edge of the wordmark. Sharp's default trim threshold (10) was too tight to catch that noise; the trimmed result had near-but-not-exactly-white edges that read as a coloured tint when composited on a dark base. Lesson: **bump trim thresholds when working with JPEGs**, and `.flatten({ background: '#ffffff' })` after trimming to kill any residual transparency that'd let the dark base bleed through.

29. **Don't flatten transparent PNGs.** Initially shipped a "flatten everything onto white" step. That broke transparent PNG logos (Cube6) — flattening turned their transparent corners into white, which made my downstream auto-detect think "this logo has its own background, skip the patch." Result: Cube6's logo dropped onto a generated image with no contrast patch behind it. Fix: branch on `srcMeta.hasAlpha` — flatten only opaque sources.

30. **The "Coming soon" pill is the wrong story to tell customers.** When the customer portal showed Instagram/TikTok/Facebook Pixels with a "Coming soon" blue pill, customers (correctly) read it as "this is in development, you can't have it yet." Wrong story — the services ARE coming soon on the platform side, but from the customer's perspective the relevant question is "do I have it." So the customer portal collapses both `coming_soon` and `not_required` into a single "Not Subscribed" state. The DB-level `coming_soon` distinction still matters for the admin's Manage panel (no checkbox available) but the customer never sees that nuance.

31. **Adding services should be DB-driven and require minimal frontend churn.** When Wez asked for Instagram + TikTok + Facebook Pixels, three new services landed via `INSERT OR IGNORE` in the seed block. The admin's Manage panel automatically picked them up. The customer portal needed three new NavItems and three new render branches — five lines per service in PortalApp.jsx. Lesson confirmed from chunk 3a: the generic services design pays off every time we add a service.

32. **Mock fallback objects must match the real shape.** When I changed `/auth/check`'s services payload from bare strings to `{state, label, pitch}` objects, I missed a default-object fallback in PortalChrome that still had the old string shape. The chrome would crash on first render before `services` arrived from the server. Fix: kept the fallback in sync. Lesson: when changing the shape of an API payload, **grep for all fallbacks/defaults** that mock the same shape on the frontend.

### Lessons specific to this chat (logo unification + source column + Phase 4 corrections)

33. **The blueprint can be wrong about what's built.** This chat opened with the blueprint stating Phase 4 was "schema only — runtime + UI not yet built". A user screenshot of the campaign modal showed the tab strip was already live with bodies, delays, and a working drip-ticker extension. Lesson: **don't trust the blueprint's "what's done" claims without verifying.** When the user says "we have tabs setup to send a 2nd and a 3rd", believe what they're seeing and grep the code before assuming the blueprint is current. The blueprint is a snapshot, not a spec.

34. **"Auto-detect" branches multiply failure modes.** Decision #38 (auto-detect logo background type, three branches) tried to be smart about Tower Leasing's white-bg logo by skipping the contrast panel. It produced inconsistent output across runs because Sharp's `.trim()` is data-dependent — the same source file produced different post-trim corner pixels on consecutive generations, flipping the branch unpredictably. Lesson reversed this chat (decision #42): **when one path can produce multiple outcomes from the same input, the answer is one path for everyone, not better detection.** A consistent panel with 20px padding works for transparent PNGs (the panel provides contrast), white-bg PNGs (the panel blends with their background), and JPEG-as-PNG broken files (the panel covers the bad fill). Predictability beats cleverness.

35. **Don't conflate "row exists" with "row is meaningful for this view."** The `email_clients` table has been used for both real cold-email customers AND portal-anchor rows for LinkedIn-only customers. The Email Campaigns Customers page listed every row indiscriminately — including portal anchors with zero email activity (Cube6, Mansons, Tower Leasing). Wez correctly read this as "LinkedIn customers got mixed into email campaigns." Fix: a `source` column on email_clients (`'aws_domain'` / `'manual'` / `'portal'`) and a `WHERE source IN ('aws_domain', 'manual')` filter on the customers query. **Lesson: when one table serves two audiences, add a discriminator column instead of trying to filter by combinations of other state.** The diagnostic SQL run on Render confirmed the data shape before the migration shipped — running diagnostics on prod data before writing migration logic is much safer than writing-then-checking.

36. **`sqlite3 < file` doesn't work when the file isn't on the server.** Wez tried `sqlite3 /var/data/studio.db < /tmp/diagnose.sql` on Render shell — the file was on his laptop, not the container. The right pattern for one-off diagnostics is: open `sqlite3 /var/data/studio.db` interactively, then paste queries one at a time at the `sqlite>` prompt. Multi-line paste into bash breaks because bash sees `.headers` and `SELECT` as commands. Lesson: **when handing off SQL diagnostics, give one query per line and confirm the user is at the `sqlite>` prompt before the SELECT.**

37. **Per-step subjects unlock the actual purpose of multi-step sequences.** Phase 4 had been built with shared subject across all steps, which sounds like a tiny detail but breaks the use-case: "Quick question, {{first_name}}" → ... → "Quick question, {{first_name}}" three weeks later doesn't feel like a follow-up, it feels like a duplicate. The send pipeline already had `bodyOverride` for the body; mirroring the pattern with `subjectOverride` was a small surgical addition. Lesson: **when extending a sequence/multi-step feature, every text the recipient sees should be per-step-overridable, not just the body.**

38. **Operator-side default and recipient-side fallback are two different config points; change them together.** When changing the rich-text editor's default font, the recipient-side `wrapBodyWithEmailCss` had to match for recipients to see what the operator typed.

### Lessons specific to this chat (logo unification round 2 + portal post-action bugs + customer-detail UX)

39. **"Predictability beats cleverness" applies twice in the same chat.** First go at the logo compositor (decision #42 v1) kept a dark-corners override and an image-bottom-right brightness heuristic — both were branches that could pick the wrong panel colour for the wrong reason. Second go (decision #42 final) ripped both out: panel is always pure white. The first version was already an improvement, but each branch I left in was another scenario where one customer's logo would land on a panel that fights it. Lesson: **when consolidating "always do X", don't half-consolidate — finish the job in one go.** Each remaining branch is a future bug report waiting to happen.

40. **Read the network request before guessing what's broken.** When Tower Leasing's "New image" button returned "Post not found", the first fix I shipped tried to handle the case where post.id was missing — based on what I saw in the code. The actual cause (visible in the browser console you sent in the next message) was the URL containing `posts/1/regenerate-image` — i.e. the id was a literal `1`, not `post_1`. A 30-second look at the browser console would have shown me the wire format up front and I'd have written the right fix on the first try. **Always ask for the network call (URL + status + response body) before guessing the bug.** Same rule that came up earlier in the project for Render logs.

41. **Bugs cascade through helper functions.** After the number-vs-string fix landed, the very next request hit a different bug in the same helper: `findPostForPortalUser` ran a SELECT that omitted `client_id`, so the regen routes downstream got `undefined` and threw "Linked LinkedIn client not found". Two bugs in one helper, surfaced one at a time as each was unblocked. Lesson: **when fixing a helper that's broken, scan its full surface area before claiming the fix is complete.** Look at every column the SELECT returns vs every column the callers use. Look at every code path that hits the helper, not just the one that surfaced the bug.

42. **One latent bug can hide behind another customer's success.** The post-action 404 bug had been latent since chunk 3b shipped — but no customer had hit it because Cube6/Sweetbyte both went straight to "Approve all" (which uses a different helper). Tower Leasing was the first customer to click any of the four per-post buttons. **When a feature appears to be working for some customers but you've never seen it work end-to-end for the actual flow, treat that as untested rather than working.** Track verified-by-real-use distinctly from shipped.

43. **Numeric IDs in JSON columns will trip up URL-param comparisons every time.** Anything stored in `posts_json` with `"id": 1` (number) will be compared against an Express URL param (string) somewhere downstream. Strict `===` will return false. **Convention going forward: when comparing IDs across the JSON-blob/URL boundary, coerce both to `String()` before comparing.** This isn't just a portal.js concern — anywhere the codebase reads numeric IDs from `posts_json` and compares them to URL params has the same risk. Worth a grep next chat: `posts_json` references where the consumer compares ids.

44. **Latent partial-SELECTs cause subtle null-prop bugs in callers far from the helper.** `findPostForPortalUser` SELECTed `id, stage, posts_json, created_at` — three of the four regen routes then accessed `campaign.client_id`. With strict mode JS that'd throw; without it, `undefined` propagates through `WHERE id = undefined` and silently returns null. **Convention: when writing a row-returning helper used by multiple consumers, default to `SELECT *` unless there's a measurable reason not to.** The cost of the extra columns is negligible; the cost of one consumer breaking on a missing column is a 30-minute debug session.

45. **Inline-editable beats modal-edit for high-frequency operator fields.** The default-sender fields lived inside the Edit Client modal — meaning every new domain required: open Customers page → click customer → click Edit → fill three fields → save → close modal → start using. Hoisting them into the customer detail header (debounced auto-save, no save button) cut that to: open Customers page → click customer → fill fields. Same pattern would help anywhere else operator state lives behind a modal. Worth scanning the rest of the admin UI for similar opportunities.

### Lessons specific to this chat (persistence + logo trim-at-upload + proportional padding + auto-unsub gate)

46. **Inline-editable fields aren't "done" when they save to the DB — they're done when the parent's in-memory state matches.** Decision #50 shipped a debounced PUT that wrote correctly to the database. But the parent's `clients` array was loaded once at page mount and never refreshed, so the New Campaign modal and the re-mounted header on customer-switch both read stale empty values. **Lesson: when adding inline-edit-with-debounced-save anywhere, also wire a "splice the response back into the parent's list" callback.** Otherwise the field appears to "not persist" the moment the user navigates anywhere — even though the bytes are safe in the DB. Same trap could apply to any future inline-editable field that doesn't already trigger a `loadAll()`. Generic guidance: **save endpoint returns the row → parent merges that row into its in-memory list → all consumers re-render with fresh values, no extra fetch.**

47. **Placeholder text in tight inline inputs reads as a saved value.** "John Wicks" as a placeholder in the Default From name field looked indistinguishable from a real saved name on first glance — operator reasonably reported "the field has ghost text I don't want." Lesson: in a row of compact inline-editable fields with small/no labels, placeholders carry signal ("there's already a value here") that's hard to suppress visually. **Default to no placeholder unless there's a clear reason** (e.g. the field's purpose isn't obvious from the label alone). Modal-style fields with full labels above can keep the example placeholders — different visual context.

48. **Sharp's `.trim()` is not idempotent on the same source bytes.** Running trim twice on the same buffer can produce subtly different results due to anti-aliasing handling at the threshold boundary. This was the root cause of the Tower Leasing "Post 1's panel is bigger than Post 2's" bug — the variance was always trim variance, not Gemini canvas variance (both posts came back at identical 1024×1024). Lesson: **treat `.trim()` as a one-shot canonicalisation step, not as something safe to run inline on every consumer.** The fix (decision #54) is structurally simple — trim once at upload time and store the trimmed buffer as canonical — but it requires a backfill (#56) for existing customers' raw files. Same shape applies to any other Sharp/image-processing operation that's data-dependent: do it once at ingest time, not per-render.

49. **"Make it bigger to look like the good one" can be the wrong fix when the good one was just the same code with cleaner inputs.** When Wez said Tower's Post 1 panel was the look he wanted, my first move was to bump padding from 20 to 40px to "match" that visual. Wrong: the 320×116 panel he liked was just what 20px padding produced around Tower's natural ~280×76 wordmark. The variance on Posts 2/3/4 wasn't smaller padding — it was the trim landing in a different spot, leaving a smaller wordmark to pad around. Once trim was canonicalised, 20px padding would have produced the same 320×116 panel on every Tower post. **Lesson: before tuning a "looks right" knob, verify whether the original variance came from the knob OR from upstream input variance. The pixel-measurement diagnostic (Python script measuring saved files) was decisive — and would have prevented two rounds of padding tuning if I'd run it earlier.**

50. **Proportional > fixed when the same pixel rule has to land across customers with different logo proportions.** Fixed-20 was tight on Tower (after trim variance fixed: 280×76 wordmark + 20px padding = 320×116, comfortable). Fixed-40 was overgenerous on Cube6 (100×100 logo + 40px padding = 180×180, too much white). There's no fixed-pixel value that works for both — different shapes need different absolute padding to feel proportional. **Solution: padding = 20% of logo's smaller dimension, floor 8px.** Tower gets ~15px (smaller dim 76), Cube6 gets ~20px (smaller dim 100), each landing in the comfortable range for that shape. **Lesson generalises:** any layout pattern that needs to feel right across multiple sources of varying intrinsic size should derive its measurements from the source's own dimensions, not from a constant.

51. **Backend gating should match frontend storytelling.** The customer-portal inbox already showed a neutral "Normal email" badge for replies with `matched_campaign_id IS NULL` — telling customers "this isn't a campaign reply, the classification is meaningless for it." But the BACKEND was happily acting on that meaningless classification by auto-unsubscribing senders. **Lesson: when the UI tells the user "we're going to ignore this," the backend should genuinely ignore it.** The fix (decision #52) was a one-liner; the diagnostic was reading the frontend to find the equivalent gating condition that was already in place there. **Generic guidance:** if you have a "this thing isn't real / doesn't apply" UI badge, grep for actions in the backend that fire on the same row category — they probably need the same gate.

### Lessons from the 2026-05-12 session

52. **Heuristic-based migrations classify wrong when the data is dirtier than the heuristic assumed.** The `email_clients.source` migration (decision #41) classified rows as `'portal'` if `portal_enabled=1 AND hasActivity=false`, where activity meant "has any list, subscriber, campaign, or inbox row." Mansons had a single empty list created by an accidental `+ New client` click weeks earlier — no subscribers, no campaigns, no inbox, but one stray list row. The classifier read `lists > 0 → hasActivity=true → 'aws_domain'`, when the operator's mental model was clearly "Mansons is LinkedIn-only, no email." Fix was one-shot SQL on Render, not a smarter migration — re-running the migration won't help because once `source` is non-NULL the migration skips the row. **Generic guidance:** heuristic-based migrations should log per-row classifications at the time they run, so you can see which rows landed where without reverse-engineering the input from current state.

53. **A UI affordance that exists "in case we need it" but has only ever been used by accident is a foot-gun, not flexibility.** The `+ New client` button in EmailSection.jsx existed from Phase 1 when email customers were manually added. Phase 1.5 onwards introduced AWS-domain auto-sync, which made the button redundant for the real workflow. The button stayed because removing it felt like reducing flexibility — but the only thing that "flexibility" actually produced across the whole project was one accidental Mansons row that we then had to clean up by hand. **Lesson: remove unused UI surfaces; if the imagined use case ever materialises, it's a one-line restore from git history.** The architectural invariant that replaces it (decision #57's "Email Campaigns customer list ↔ AWS verified domains, period") is stronger and easier to reason about than a gated escape hatch.

54. **`scrollIntoView` walks up the DOM and scrolls every scrollable ancestor — including the page.** The campaign log terminal's auto-scroll `useEffect` was designed to "keep the log scrolled to the bottom during generation," but it had the side effect of yanking the entire page down whenever a new log line was pushed via SSE — most visibly when the customer regenerated an image and the log terminal at the bottom of the page emitted `✓ Image regenerated for post 4`. Tellingly, the code already had a comment noting that local `setLogs` calls were avoided "to prevent triggering scrollIntoView on the log terminal," but the SSE stream pushed log lines on a separate code path that the comment-author hadn't considered. **Generic guidance:** if you ever want to scroll only WITHIN a container, set `element.scrollTop = element.scrollHeight` on the container itself — never `scrollIntoView` on a child, which is a whole-DOM scroll request that browsers happily satisfy by scrolling whatever's between the element and the viewport.

55. **Shared-secret signed-ticket bridges are a low-dependency way to embed external admin panels with no double-login.** Decision #59 (IDYQ admin embed) ships the pattern: a short-lived HMAC-signed ticket minted by the embedding app, verified by the target app, exchanged for a session cookie on a dedicated bridge admin user. **Generic guidance for future App integrations:** the pattern requires three new pieces per integration — (a) a `<service>-bridge` route on Studio that mints the ticket using a per-integration secret, (b) a matching `/admin-bridge` endpoint on the target side that verifies the ticket and mints a session cookie, (c) a dedicated bridge admin user on the target side with a random bcrypt hash (so the user can never log in via the target's normal login form). Three known failure modes to design for: (i) Studio backend error → use a Studio-chrome error card that names the missing env var; (ii) target-side frame-blocking headers → blank iframe — fix is one CSP line on target; (iii) cookie-not-sticking (Safari ITP, Chrome partitioning) → target shows its own access-denied card. These distinct visual signatures are debuggable; design them in deliberately.

56. **Customer-portal passwords are bcrypt-hashed and unrecoverable — by design.** The user initially asked for a Render shell command to reveal a portal user's plaintext password. The answer (correctly) was "the system literally cannot do that — there is no decrypt path, only a one-way hash." The right operational alternative is admin-chosen password reset (decision #60). **Generic guidance:** never write a code path that stores or returns plaintext passwords beyond the one-time return at creation/reset. The "we literally cannot see your password" property is also a positive business asset for the portal — it's a stronger trust signal than "we try not to look." When tempted to add a "view this user's password" admin tool, pause and ask why; the answer is always "reset it instead, here's a temporary one."

57. **The non-cached prompt-block pattern is the canonical way to ensure customer edits invalidate Anthropic's prompt cache.** Decision #61 (Refine my posts) made this explicit: when a piece of prompt content must reflect the LATEST customer-controlled state on every API call, the content goes into its own user-message block with NO `cache_control: ephemeral` wrapper, while the genuinely-static surrounding content (system prompt, RAG document) keeps its cache. Anthropic's prompt-cache is keyed on the cached prefix's exact bytes — adding/removing/editing the non-cached block doesn't invalidate the cached prefix. Result: cache savings on the static content (~90% off on cached tokens) while the customer's latest rules are always read fresh, no 5-minute TTL to wait out. **Generic guidance:** any future feature where the customer can edit X and expect "the change applies immediately to the next AI call" needs the non-cached-block pattern. If you wrap the changing content in `cache_control`, edits that occur within the 5-minute window will silently apply the OLD content to the next call, and there is no way to surface this bug except by manually testing within-window edits.

58. **Image processing pipelines need an escape hatch for inputs that violate the pipeline's assumptions.** The logo trim pipeline (decision #54) assumed "logo backgrounds are light/transparent" — and it was right for 12 customers in a row. Then The Manson Group uploaded a dark-blue-background JPEG and the pipeline silently produced a mangled output: trim cropped into the wordmark, flatten painted what was left onto white. The customer's branding was destroyed without any error being thrown — Sharp succeeded, R2 upload succeeded, the column was set. The bug was only visible by looking at the rendered logo. **Generic guidance:** for any image processing pipeline, add a CHECK BEFORE the transformation that the input matches the pipeline's assumptions. If it doesn't, fall through to a "leave it alone" path rather than damage the file. The check needs to be designed BEFORE you have a customer file that breaks the assumption — otherwise you only learn about it from a complaint. In hindsight, decision #54's design phase should have included "what if the logo isn't light-background?" as an explicit question. The fix (decision #62, median-corner-luminance threshold check) is small but the protection it offers is large — it now covers any future dark-bg customer too, not just Manson. **Pattern to apply elsewhere:** any time you write a transformation that has an implicit "the input looks like X" assumption, name the assumption out loud in a code comment AND build a runtime check that falls through to no-op when the assumption fails.

59. **For LLM JSON output, prefer multi-strategy parsing + retry over assuming clean output.** The 2026-05-12 Manson campaign failed because Claude (with `stop_reason: end_turn`, i.e. claiming to have finished cleanly) emitted otherwise-valid JSON with a hallucinated `Menu` token between two key-value pairs. One-in-thousands anomaly, but it killed the entire 12-post generation. The original code had a two-strategy parser (direct + greedy regex) and threw on failure — no logging of the raw response, no retry. Both failure modes are now covered by decision #63's three-layer parser + automatic retry with strengthened instruction + full-raw-response logging on failure. **Generic guidance:** LLM-generated structured output should always assume the LLM will occasionally produce a malformed response, even when the model claims `stop_reason: end_turn`. Three layers minimum: (1) direct parse, (2) cleanup-and-extract (markdown fences, surrounding prose), (3) one automatic retry with the response shown back to the model and a stricter instruction. And log the FULL raw response on every parse failure — debugging future cases is essentially impossible from a 300-character slice. The cost of one retry call is negligible against the cost of a failed customer-facing operation. **Pattern is also why `tryParseClaudeJSON` was hoisted to module scope** — once you have a robust parser, every JSON-consuming Claude call in the codebase should use it, not just the one that hit the bug.

### Lessons from the 2026-05-19 session

60. **"Stepping off a locked-in decision" is allowed when a real customer ask makes the decision's premise no longer hold — but the decision's *reasoning* must survive the step.** Decision #42 ("always-white panel, one unified path") and #39 ("predictability beats cleverness, finish the consolidation") were locked in hard. The Manson dark-blue-background logo gave a legitimate reason to add a non-white panel option — but the way it was done (decision #64: a stored per-customer setting, operator-controlled, defaulting to the old behaviour) preserves #42's actual reasoning. #42 was never "white is the only correct colour"; it was "one input must not yield multiple outputs from a per-render heuristic." A stored setting is still one-input-one-output — the same customer always renders the same panel. **Generic guidance:** before overriding a locked-in decision, separate the decision's *conclusion* ("panel is white") from its *reasoning* ("no per-render branching"). If the customer ask only conflicts with the conclusion, you can satisfy it with a mechanism that still honours the reasoning. If it conflicts with the reasoning itself, that's a real relitigation and needs explicit operator sign-off, not a quiet workaround. Record the step-off in the decision log either way so the next chat knows the earlier decision was deliberately amended, not forgotten.

61. **"The operator says it didn't save" can mean "the operator didn't wait for the debounce," not "the save is broken."** The Brand-panel dropdowns auto-save on a 1-second debounce. Early operator reports of "it didn't save" were the operator changing a dropdown and navigating away inside that 1-second window before the PUT fired. The save logic was correct the whole time. This is the inline-edit-debounce trap from lesson #46 wearing a different hat: #46 was "saved to DB but parent state stale"; this is "looked unsaved because the write hadn't been issued yet." **Generic guidance:** any debounced auto-save needs a *visible* state machine the operator can read — "Saving…" the instant the value changes (not when the debounce fires), then "✓ Saved" on success. If the indicator only appears when the network call starts, a fast navigate-away looks identical to a broken save. When an operator reports an inline-edit field "doesn't persist," first question is "did the indicator reach ✓ Saved before you left?" before touching any code.

62. **"Shipped and parse-checked" is not "verified" — and this session shipped an unusually large batch of the former.** Seven decisions (#64–#70) all landed parse-checked and logically reviewed, but a large fraction have explicit pending-verification flags: the per-post recomposite end-to-end flow, the no-brand-mark prompt's actual effect on Gemini output, the sanitiser's real-world log frequency, the auto-grow scrollbar behaviour. This is the lesson #42 trap ("one latent bug hides behind another customer's success") at batch scale. **Generic guidance:** when a session ships many interdependent changes touching the same customer (Manson, here), the verification surface is the *product* of the changes, not the sum — the recomposite flow depends on the two-stage split AND the R2 pre-logo upload AND the projectPost override exposure AND the portal UI, any one of which silently failing breaks the feature with no error. Track each pending item as its own line and confirm them individually against real operator use; do not collapse "all seven shipped" into "the Manson logo work is done."

63. **A new, larger RAG changed the failure characteristics of an old, "fixed" bug — fixes are validated against inputs, and the input moved.** Decision #63's `tryParseClaudeJSON` repair pass was verified against the *2026-05-12* Manson failure (stray `Menu` between key-value pairs, specific placement). This session's failure is the *same family* of anomaly (stray `Menu` token in long structured output) but the new weprintcatalogues.com RAG is much larger, producing longer 12-post JSON, and the stray token is landing in a placement the #63 regex doesn't catch — and it fails on BOTH attempts because the contamination is input-stable (same big RAG + prompt reliably reproduces it, so the retry produces the same broken output; re-running will not help). **Generic guidance:** a parser/repair fix is only proven for the input distribution it was tested against. When a major input (RAG, prompt template, model version) changes, treat every input-shape-sensitive fix as *unverified again*, not as still-solved. AND — the decisive point for the next chat — **do not extend the #63 repair regex from the truncated error message.** The thrown error only contains the first 1000 chars (clean JSON, then a cut-off). The `console.warn` at the parse-fail branch (~line 497 of this session's `openai.js`) logs the ENTIRE raw response and is the only thing that shows *where* the stray token sits — and the correct fix differs by placement (stray bare word *between* JSON tokens → strip-bare-token repair pass; stray word *inside* a string value breaking escaping → an escaping-repair pass, a completely different fix). Measure before tuning: get the full line-497 log from Render first. This is the project's oldest working principle and it applies with full force here.

### Lessons from the 2026-05-20 second session (CRM linked-customers + Mark-as-converted)

64. **"Customer ownership" in a multi-table schema needs to follow `customer_services`, not just the row id the data was written against.** Decision #76 implicitly assumed "the email_client_id on a hot_prospects row identifies the customer" — but with `customer_services` joining a portal anchor (`cube6`) to an inbox-owning row (`mail.engineeringsolutions.co.uk`), the row id alone is *one* identifier the customer goes by, not THE identifier. Prospects flagged on the inbox row were sitting on the right row, but the portal — which queries against the anchor's id — couldn't see them. This is the *same family* of bug as lessons #46/#48 ("a SELECT/helper that doesn't read the column its callers actually need") at one level up: it's not a missing SELECT column, it's a missing JOIN concept. **Generic guidance:** any time a feature filters or writes against `email_client_id`, ask "for THIS customer, are there other email_clients rows that should count as the same customer?" before locking in the read path. The `customer_services` table is the source of truth for that question — use it. The existing `resolveEmailClientId()` helper in `routes/portal.js` was a perfect example of the same pattern already in use for inbox reads; new features should follow the same convention rather than re-implementing single-id ownership. The fix shipped (decision #85) builds `resolveLinkedSet()` as the natural generalisation. Inbox reads from #20 onward had this right from day one — the CRM build inherited a different (and broken) ownership assumption from how the test customers happened to be set up (Manson is self-linked, so the bug never surfaced on test data; Cube 6 was the customer that exposed it).

65. **Server-driven UI metadata flags (`has_linked_inboxes`) beat client-side derivation when the rule is non-obvious.** The Inbox column appears only when the customer has linked inboxes — i.e. only when there's something to disambiguate. A naive client-side rule would be "render the column when prospects.map(p => p.source_inbox_name) yields more than one distinct value" — but that breaks when a customer with linked inboxes has only one prospect (one value, column doesn't appear, then a second prospect from the other inbox arrives and the column suddenly snaps in — janky). The fix is a server-side `has_linked_inboxes` flag derived from `resolveLinkedSet(emailClientId).length > 1`, which is the actual question the column answers ("does this customer have multiple inboxes that hot prospects could come from?"), regardless of whether all of them happen to have a prospect yet. **Generic guidance:** when a UI rule depends on a structural fact about the customer's data model (not just the row population at this moment), compute the flag on the server from the structural fact and ship it down. Don't infer structure from row content client-side. The same principle drives `has_linked_inboxes` on the per-prospect thread response (so the detail modal knows whether to show the "From `<inbox>`" subtitle).

66. **A "definition of done" is required for any feature whose counter would otherwise grow forever.** Operator question that birthed decision #86: "we don't want the numbers to increase forever." Right intuition — without an explicit terminal state, the badge would creep up indefinitely as prospects accumulated, lose its meaning, and the operator would stop trusting it. Three valid answers existed (clear-the-date / mark-converted / track-both-converted-and-lost); operator chose the middle. **Generic guidance:** any feature that adds a count/badge/alert needs the question "what makes this go away?" answered explicitly during design — three valid endings (auto-expiry, user-cleared, terminal-state-flag) cover most cases, but picking one is required up-front. Don't ship a counter without a defined definition of done; the badge will eat itself in production usage. Decision #86 also illustrates the related principle "make the terminal state REVERSIBLE" — the Reopen button isn't decorative; it's the safety net that lets the no-confirmation Mark-as-converted button work without anxiety. If an action is one-click-no-confirm, its undo path must be equally obvious in the UI.

### Lessons from the 2026-05-20 fourth session (portal frontend + Formspree + reply-matching)

67. **A latent data-shape mismatch between two write paths can hide for months if you never test the integration in production.** Decision #90's reply-matching bug had been silently broken since Phase 3.1 — every "Automatic reply" landing in any connected Gmail mailbox failed to match its campaign, so every historical campaign reply count is understated. The bug existed because the matcher stripped `<>` from the inbound `In-Reply-To` but didn't strip the `@eu-north-1.amazonses.com` suffix, while `email_sends.message_id` was stored without the suffix. Two different storage formats for the same conceptual id. The bug wasn't visible because (a) operator never explicitly checked the campaign reply count vs the inbox visually — they looked at the inbox and the count separately, never together; (b) no automated test exercised "send → receive auto-reply → match by message-id" end to end; (c) the customer portal showed unmatched replies as "Normal email" by deliberate design (#52 gate), which masked the failure mode. **Generic guidance:** whenever two parts of a system independently store the "same" identifier, the formats are guaranteed to diverge eventually — pick ONE canonical normalisation function (here: strip `<>`, split on `@`, take prefix) and run it on BOTH the write side AND the read side. The bug was a single missing transform on the read side. Also: for any feature with an externally-visible counter (campaign reply count is exactly this), build a 30-second sanity check the operator can run to confirm the counter agrees with the underlying data — would have caught this years earlier.

68. **The bare-fallback in a parser is where junk creeps in — make it a labelled-only parser if you care about precision over recall.** Decision #89's Formspree parser landed 5 junk rows because when no labelled `email:` field was found, the parser fell back to "first email-shaped string in the body." On the EDP website-enquiry template, that string was the customer's own mailbox address (`kevin@mail.edphandles.com` in `mailbox: kevin@...`), not the submitter's email. The catalogue-download template worked correctly because it has proper `name: / email: / _replyto:` labels. **Generic guidance:** any structured-data parser that falls back to a regex-match-on-the-whole-blob will eventually pick up the wrong thing. Two valid responses: (a) make the parser labelled-only and skip unrecognised templates (precision over recall — better to miss a row than add a wrong one), and (b) when you know a customer has a fragile template you haven't tested against, pull a real sample BEFORE building the parser for it. We had EDP catalogue-download bodies in front of us and built the parser for those; the website-enquiry template was different and we never looked at one. Lesson #18 / #40 / #63 again: measure before building.

69. **~~Two surfaces displaying the same data with deliberately-different labels is a feature, not a bug~~ — SUPERSEDED 2026-05-20 fifth session, see lesson #71.** Previous wording (preserved here for the historical record) claimed the admin/portal label divergence on the inbox classification badge was deliberate operator-locked design. **It wasn't.** Operator confirmed this session that the collapse of soft_negative/hard_negative/forwarding into a generic "Neutral" on the customer portal was never something they asked for — an earlier session had inferred it as a customer-friendliness gesture and lesson #69 then back-referenced it as if it were a formal decision. The corrected lesson is at #71. The portal now mirrors admin labels exactly except for two narrowly-justified divergences (non-campaign mail → "Normal email" for relevance reasons; null classification → "Classifying…" to convey transient state). Original lesson text below for record:
   > *During the reply-matching investigation operator saw "Soft negative" in admin and "Neutral" for the same row in the customer portal and asked if there was a deeper bug. There wasn't — `ClassifyBadge` in PortalApp.jsx deliberately collapses positive/auto_reply into named labels and EVERYTHING ELSE (including soft_negative, hard_negative, neutral) into "Neutral" (decision #36's "customer doesn't need to see negatives"). Likewise the customer portal shows "OOO" where admin shows "Out of office" — same data, deliberately shorter label for the customer surface.*

70. **When two diagnostic SQL queries give you a definitive answer, build the fix from the data — don't widen the investigation.** The reply-matching bug took two SQL queries to pinpoint: one to confirm the headers WERE present (ruling out gateways stripping them), one to see the exact value mismatch (full RFC 5322 form vs bare local id). Total investigation time: ~5 minutes. Total fix time: ~10 minutes. Compare to the alternative: guess at root cause from inbox screenshots, ship a speculative fix, iterate. Lesson #18 / #40 / #63 says this explicitly but the principle bears repeating: **when the operator can run SQL on Render shell, ASK FOR THE QUERY before writing code.** Render shell access is one of the project's most valuable debugging tools because the operator is technical enough to paste commands and screenshot output — use it. The corollary: any future bug report where the operator's already running on Render shell should always trigger "what SQL would tell us the answer?" before any code.

### Lessons from the 2026-05-20 fifth session (Formspree polish + badge parity)

71. **Don't manufacture a "deliberate divergence" between admin and customer-facing surfaces without an explicit operator decision.** Lesson #69 had claimed the customer portal's collapse of all negative classifications into "Neutral" was a feature — citing a "decision #36" about customers not needing to see negatives. There was no such operator decision; an earlier chat had inferred customer-friendliness from a soft signal and lesson #69 then formalised it as if locked-in. When the operator surfaced the resulting label mismatch on screenshots ("the two portals are not saying the same badges. why cant both portals read from the same script and have the same answer"), it became clear the divergence was never wanted. **Generic guidance:** when designing a customer-facing surface that *could* differ from an internal one, the default is **same labels, same data**. Any divergence must (a) come from an explicit operator decision recorded in the decisions list, NOT inferred from a comment or surrounding context, and (b) be narrowly scoped with a specific reason that holds up when re-stated ("non-campaign mail's classification truly is meaningless to this audience" passes; "customers might be upset to see Negative" doesn't, because that's a customer-experience guess the operator should make). When inheriting a divergence from an earlier session, treat it as **provisional until operator-confirmed in writing**, not as locked-in just because a prior lesson said it was. The cost of a wrong divergence is high: the operator spent a chunk of this session trying to reconcile two portals that should have agreed by default.

72. **The blueprint can carry forward an inferred decision as if it were a real one — and once formalised in a lesson, future chats treat it as binding.** Related to #71 but worth its own line. The decisions list goes 1→91; lesson #69 referenced a "decision #36" that doesn't exist as described (decision #36 in the list is about ServiceGate's "Not Subscribed" vs "Coming soon"). The "customer doesn't need to see negatives" framing existed only inside lesson #69's text and the inline ClassifyBadge comment. Once a session stamps something as a "locked-in decision," every future chat reads-the-blueprint and respects it — including chats with no original-author context. **Generic guidance:** be precise about decision provenance when writing a lesson. Cite the actual decision number; if you can't find one, the lesson is documenting an inferred pattern, not a locked-in policy, and should say so explicitly ("convention chosen by Claude during X session, no operator decision recorded — re-confirm before treating as locked-in"). The lesson section should describe *observed engineering patterns*, not *invent product policies*.

### Lessons from the 2026-05-21 sixth session (Formspree thread pinning + Hot Prospects status pipeline)

73. **A column accepted by a POST endpoint but not actually written is a silent gap.** Decision #94's root cause: `hot-prospects.js` and `portal.js` POST routes both accepted `source_reply_id` in the request body, used it ONLY to auto-fill the prospect's name from the reply's `from_name`, then threw it away — the column didn't even exist on the `hot_prospects` table. The frontend's "Send to Hot Prospects" button was always sending it. From the outside everything looked fine; from the inside the data was being dropped. The bug only surfaced because operator opened a thread and saw "No messages found" — and that took a deep-dive across multiple files to trace because there was no error, no warning, no log — just a silent discard. **Generic guidance:** when a column is referenced in a route's request shape, audit BOTH the schema (does it exist?) AND the INSERT/UPDATE statements (is it actually written?). Accept-and-discard is a real anti-pattern that hides for months. Also: thread/relationship features that depend on a FK pin should NOT pass that pin through a "name autofill" code path that doesn't store it — if the code reads it, the code should also store it.

74. **Pushback on operator design asks is sometimes the right move — even when the operator initially asked for the simpler thing.** This session's Hot Prospects status feature: operator first asked for free colour picker only ("customer drops in any colour they want"). Claude pushed back that without semantic statuses you can't filter or search, and colours-without-meaning are paint chart noise. Operator switched to option (c) — fixed statuses + free tag colour combined — which is materially more powerful. Same session also pushed back on "every Formspree email should be Hot Prospect" (would have re-added junk rows like the ones we just cleaned up). Both pushbacks landed because they were specific (named consequences, named alternatives) and they happened BEFORE code was written. **Generic guidance:** when operator asks for something and you spot a real trade-off they may not have considered, surface it as a specific design question with named alternatives, BEFORE you write code. Two costs of getting this wrong: (a) shipping a worse feature that the operator then asks you to extend within days (Lesson #28 family); (b) bypassing pushback because the operator's wording sounded final ("just do X"). The wording is rarely as final as it sounds — it's almost always "I want a thing that does roughly X." Confirm the goal, then propose the best mechanism for it. The pattern "I lean toward Y because Z, but here's option X you asked for — which do you want?" is honest and gives the operator full agency to overrule.

75. **A migration that adds a NOT-NULL-with-default column will give every existing row that default — which is correct DB behaviour but can be wrong product behaviour.** Decision #95 added `status TEXT NOT NULL DEFAULT 'new'` to `hot_prospects`. Migration ran clean. But every existing prospect — including ones you'd already contacted weeks ago — became status='new'. The "NEW" pill (driven by `admin_first_viewed_at IS NULL`) also fired on all 7 existing rows because the new column defaulted to NULL. So immediately after deploy, the UI told the operator "you have 7 brand-new unviewed prospects" when in reality those rows were already worked. This was anticipated and handled with explicit operator-chosen cleanup SQL: `UPDATE hot_prospects SET admin_first_viewed_at = added_at, portal_first_viewed_at = added_at WHERE admin_first_viewed_at IS NULL` cleared the NEW pill noise for existing rows; status='new' was kept because the operator can't honestly remember which existing rows had been contacted (an honest "you'll set this as you go" is better than guessing). **Generic guidance:** every "additive column" migration on a populated table needs a paired think-through: what default does this column take for existing rows, and is that default the *honest* answer or just the *default* answer? Sometimes the default IS honest (NULL for "we don't know"); sometimes it's wrong (giving every existing row the same enum value that isn't actually true). Surface this as part of the design discussion before shipping the migration, and prepare cleanup SQL alongside the schema change rather than after the operator notices the UI looks wrong.

76. **The "Render spend cap" can silently park a finished round of work.** End of fifth session: code was shipped, parse-checked, smoke-tested clean. Render's monthly build-minutes spend limit was hit and deploys were blocked — the operator's GitHub Desktop push completed fine but Render greyed out the "Manual Deploy" button. From inside the codebase nothing was wrong; from outside the customer was looking at a UI that hadn't changed. Sixth session opened with "deploy failed, no logs it wont let me re-deploy" and screenshots showed the purple "You've used up the maximum number of pipeline minutes specified by your spend limit" banner. **Generic guidance:** when an operator reports "I pushed but nothing changed," before assuming the deploy actually ran AND failed silently, ask explicitly: "what does Render's dashboard show — is the deploy queued, building, succeeded, failed, or blocked?" The billing-block case looks identical to a "succeeded but no UI change" case from the operator's UI seat. One question saves a session-long debugging chase down the wrong path.

### Lessons from the 2026-05-21 seventh session (inbox row badges + Unsubscribe/Re-subscribe everywhere)

77. **In-memory SQL smoke-testing before deploy catches bugs that the operator can't tell exist.** Decision #96's first cut scoped the `email_subscribers` JOIN to the single `email_client_id` of the inbox row. SQL-correct, schema-correct, parse-check clean. But for linked customers like Cube6, the mailing lists live under the Cube6 anchor, not under the mail.eng inbox row — so the JOIN would have returned zero unsubscribes for every Cube6 reply, AND THE OPERATOR WOULD HAVE HAD NO WAY TO TELL. The new "Unsubscribed" pill would just never appear for linked customers. From the operator's seat: pill missing → "we don't have any unsubscribed people in our inbox" → false negative passes for truth. Caught in this session by running an in-memory Python SQLite smoke test against a realistic Cube6/Manson scenario BEFORE shipping. Same pattern caught two more bugs across the seventh session: the `client_user_id` column that doesn't exist on `email_audit_log` (decision #97), and the three-state design need for `contact_on_list` separate from `contact_unsubscribed` (Re-subscribe banner needed a third state, found via smoke test of "what should this UI do for Ian at IDM"). **Generic guidance:** any read-query that includes a JOIN against a many-tables structure (subscribers → lists → email_clients; hot_prospects across a linked set; replies matched to campaigns via threading headers) needs a concrete fixture-based smoke test before shipping. The fixture should explicitly include the awkward customer shapes (Cube6's linked set; Manson's self-linked; a contact who's both a Hot Prospect and unsubscribed; a contact who's neither). It takes 5 minutes of Python and catches the failure modes the operator can't see. Lesson #18/#40/#63/#70 are about measuring before tuning; this is the same principle applied to a category the project keeps tripping on — silent zero/empty/false-negative results. The corollary: the absence of evidence is not evidence of absence, especially when the evidence is a pill that's supposed to render.

### Lessons from the 2026-06-08 ninth session ("Remove from list" + SES SNS bounce wiring)

78. **Test sends deliberately don't write to `email_sends` — which is correct product behaviour, but means the "send a test to a bouncing address" diagnostic doesn't prove bounce handling end-to-end.** In the ninth session, after wiring SES → SNS → Studio for `mail.engineersolutions.co.uk`, the test was: send `bounce@simulator.amazonses.com` via the campaign Test button → verify Studio marks the send as bounced. The SNS event landed in `email_sns_events` correctly (`type='bounces:Notification'`, `bounceType='Permanent'`) but no `email_sends` row was ever flipped to `status='bounced'`. Initial diagnosis (almost) chased a "handler broken" theory before re-reading `/campaigns/:id/test` at line 1448 — it calls `sendEmail` directly without writing to `email_sends`, by design (test sends shouldn't pollute real campaign stats). With no `email_sends` row, `handleBounce` falls through to its "look up by subscriber.email" fallback — but the test bounce simulator address isn't on any subscriber row, so the handler correctly does nothing. **Generic guidance:** when designing a diagnostic to prove a "real send → real consequence" pipeline works end-to-end, check whether the test fixture actually traverses the same code path as a real send before declaring proof-by-absence. For SES → SNS → Studio bounce flow, a true end-to-end test would require (a) a real campaign send to a real subscriber, where the subscriber's email is rigged to bounce, OR (b) a synthetic SNS notification injected with a known real `message_id` from a recent real send. The "test send to simulator" proves SNS DELIVERY works but doesn't prove the consequence-application step. In this case, the operator trusted the SNS delivery proof as sufficient — the handler code is straightforward and the SNS delivery was the unknown that needed verification. **Related insight:** when shipping a fix in stages, distinguish "proven end-to-end" from "the hard part is proven, the rest follows the standard pattern" — both are reasonable acceptance criteria, but they're different and worth being explicit about with the operator.

79. **Always-search-before-bulk-changes pattern: check what's already wired before adding what looks missing.** Ninth session SES SNS setup started with the assumption that no SNS topics existed (because Studio had received zero bounces ever). The honest pushback was to check first — and the AWS console screenshot showed the `bounces` and `complaints` topics DID exist, created originally for Sendy. The actual fix was simply to add Studio as a SECOND subscriber to existing topics, not to create new ones. Building duplicate topics (e.g. `ses-bounces` + `ses-complaints` per my initial walkthrough) would have created two parallel telemetry streams, fragmented bounce data, and required choosing which one to wire SES to. Same principle as "check what env vars / DB columns / routes already exist before adding new ones" — when integrating with a system the operator has touched before, check the current state via screenshots BEFORE prescribing actions. **Generic guidance:** for any "we need to set up X in their AWS / Render / DB" instructions, build a probe step first (screenshot of the relevant console list, `PRAGMA table_info` for DB, `printenv` for Render). Read what's there. Then prescribe based on the gap, not based on the green-field assumption. Saved one full topic-creation round in this session.

---

## What to do first in next chat

-14. **"REMOVE FROM LIST" SOFT-HIDE + SES SNS BOUNCE WIRING — SHIPPED/CONFIGURED 2026-06-08 ninth session (decisions #99, #100, #101 stubs).** Three files shipped for #99 ("Remove from list"): `server/db.js` (hidden_at additive migration), `server/routes/email.js` (filter in GET, auto-unhide in POST, soft-hide in DELETE), `src/components/EmailSection.jsx` (Remove button + onDeleted wire-up). Soft-hide preserves all data; AWS re-verification auto-unhides via name match in POST. Operator verified `mail.idoyourquotes.com` removed correctly. **#100 SNS WIRING:** SNS topics `bounces` + `complaints` were ALREADY in AWS (created originally for Sendy — only Sendy subscribed). This session added Studio HTTPS subscriptions to both topics (auto-confirmed) + wired SES identity `mail.engineersolutions.co.uk` Notifications tab (Bounce → bounces topic + headers ON, Complaint → complaints topic + headers ON, Delivery deliberately left off). End-to-end test with `bounce@simulator.amazonses.com` confirmed SNS delivery into `email_sns_events` in <3 seconds. NO code shipped for SNS — Phase 2 handlers worked the moment SNS started delivering. **OPERATOR ACTION OUTSTANDING (priority 1 for next session if you haven't done it yet):** repeat the SES Notifications wiring for the other 13 verified identities in eu-north-1 SES console. Same drill per domain: Identities → click domain → Notifications tab → Edit Feedback notifications → Bounce: bounces topic + headers ON, Complaint: complaints topic + headers ON, Delivery: leave No SNS Topic. ~90 seconds each. NO further testing needed per-domain. Until done, bounces on those domains stay invisible to Studio. **#101 DEFERRED STUBS (don't pre-act, locked-in for future):** (a) reply-count over-counting in campaign report — one-line SQL fix at `server/routes/email.js` ~line 1609 needs `AND r.matched_campaign_id = es.campaign_id` added to the JOIN; will REDUCE reported reply counts visibly when shipped (the over-count is real). (b) Click-tracking inflated by corporate mail security scanners — diagnosed via Wil@sinclair-intl.com (36 clicks, mostly bot pre-fetches). Four options sketched (burst dedup recommended); deferred until operator surfaces it again; if picked up consider adding ip_address + user_agent columns to `email_link_clicks` first to future-proof. (c) Bounce-rate auto-pause campaign protection — AWS already auto-pauses at ~10% but Studio could pause earlier as self-defence; design questions all open, prerequisite is the other 13 SNS identities being wired so bounce calculations have no blind spots. **Two lessons added:** #78 (test sends bypass `email_sends` so "test to bouncing address" only proves SNS delivery, not consequence-application — the operator trusted SNS delivery as sufficient proof in this case); #79 (always probe what's already wired before prescribing — saved a full SNS-topic creation round when AWS screenshot revealed Sendy had already created the topics).

-13. **PER-CAMPAIGN UNSUBSCRIBE PANEL — SHIPPED 2026-05-22 eighth session (decision #98), CONFIRMED WORKING IN PRODUCTION end-to-end.** Eight files shipped across one session. Two new tables (`campaign_unsubscribes` per-campaign + `contact_unsubscribed_all` master flag), drip-ticker gate (closes blueprint backlog priority 3), auto-tick on Hot Prospect add via `source_reply_id` → `matched_campaign_id`, four endpoint pairs (admin + portal × replies + hot-prospects), self-contained panel component duplicated three times across `EmailSection.jsx` + `CrmHotProspects.jsx` + `PortalApp.jsx`. Per-campaign tickboxes shown identically in inbox open-email modal AND Hot Prospect detail modal on both admin AND customer portal. Master "Unsubscribe from ALL — includes future" master tick disables per-campaign rows when on. Auto-tick fires for NEW prospects added from a reply — does NOT retroactively tick existing prospects (additive principle, same as #95). Operator-verified: ticking on either side shows correct state on the other side across all four surfaces. **Open coexistence question NOT decided yet (don't pre-decide, let operator bring it up):** the existing list-level Unsubscribe / Re-subscribe controls from #97 (per-reply modal button + Hot Prospect dual banner) are STILL IN PLACE alongside the new panel — they operate at different layers (list vs campaign) so they're NOT redundant, but operators see two "unsubscribe" affordances which could be confusing. Future possibilities: keep both, consolidate, move list-level into a collapsed "Advanced" section, rename, or retire. Wait for the operator to surface friction before touching this. **One backend convention to remember:** per-campaign unsub rows always written under the campaign's `email_client_id`; master flag written under whichever side of the linked set the operator wrote from. Drip-ticker gate uses linked-set IN clause on master, plain `campaign_id` match on per-campaign. Any future code that writes to `campaign_unsubscribes` or `contact_unsubscribed_all` should follow this — `buildSubscriptionsPanel` + `applySubscriptionsUpdate` helpers in `hot-prospects.js` do it correctly; reuse them.

-12. **UNSUBSCRIBE + RE-SUBSCRIBE EVERYWHERE — SHIPPED 2026-05-21 seventh session (decision #97), CONFIRMED WORKING IN PRODUCTION (operator-verified during the 2026-05-22 eighth session when #98 was being built — operator was clicking through both directions on multiple replies and prospects without anything misbehaving).** Six files shipped. Two surfaces: per-reply modal and Hot Prospect detail modal. Both directions (Unsubscribe and Re-subscribe) on BOTH surfaces on BOTH admin and customer portal. Three-shape backend response across all four new/replaced endpoints (`ok=true` / `not_on_lists` / `already_unsubscribed | already_subscribed`) — silent no-ops were deliberately rejected in design. Per-reply modal: existing admin button condition changed from per-reply `auto_unsubscribed` flag to real subscriber-state `contact_unsubscribed`. Hot Prospect modal: dual amber Re-subscribe / red Unsubscribe banner, mutually exclusive, only shown when the prospect IS on a list at all. **NEW backend field `contact_on_list` introduced** alongside `contact_unsubscribed` so the Hot Prospect modal can distinguish three states cleanly. **Honest behaviour change worth knowing:** the existing per-reply Unsubscribe button used to show when `!auto_unsubscribed` (per-reply audit flag). It now shows when `!contact_unsubscribed` (real subscriber state). For any legacy reply where `auto_unsubscribed=1` but the subscriber row was never actually flipped (historical inconsistency), the Unsubscribe button will now appear on that reply and clicking will correctly flip the subscriber row. Right outcome, but behaviour change. **Note on co-existence with #98:** the list-level affordances from #97 are deliberately still in place after #98 shipped — they operate at a different layer. See -13 above for the open question.

-11. **INBOX ROW ACTION-TAKEN BADGES — SHIPPED 2026-05-21 seventh session (decision #96), CONFIRMED WORKING IN PRODUCTION.** Every email row in the inbox list AND the open-email modal now shows up to two new secondary pills next to the existing classification badge: **"On Hot Prospects list"** (amber, clickable → jumps to CRM with the prospect's detail modal auto-opened) and **"Unsubscribed"** (grey, not clickable — end-state). Same labels on admin AND portal per #92's parity rule. Linked-set scoped for both Hot Prospects AND subscribers lookups — important for linked customers like Cube6 where mailing lists live under the anchor not the inbox row (this was caught pre-deploy by SQL smoke test, see lesson #77). Operator-verified working on both admin and customer portal: Ian at IDM shows "Campaign · New prospect" + "On Hot Prospects list"; Harvey Jones shows "Campaign · Auto-unsubscribed" + "Unsubscribed". No backfill or migration — both signals are computed live.

-10. **HOT PROSPECTS PIPELINE — SHIPPED 2026-05-21 sixth session (decision #95), PENDING OPERATOR VERIFICATION.** Three pieces in one round: (a) **Status enum** — `new` / `contacted` / `no_response` shown as a pill in a new Status column on BOTH admin and portal Hot Prospects lists, plus a dropdown in the detail modal. Default 'new' for all prospects including Formspree auto-flagged (we don't know if you've replied yet). (b) **Tag colour override** — bounded 8-colour palette (red/orange/yellow/green/blue/purple/pink/grey) + a ∅ default option. Picker in the modal. Custom colour wins for the pill background; status LABEL never changes. (c) **Per-side viewed tracking** — `admin_first_viewed_at` + `portal_first_viewed_at` columns track view state separately for the two audiences. Drives the red "NEW" pill on each side's list. Portal sidebar Hot Prospects nav also gets a **green unread-count badge** alongside the existing red urgent badge (two distinct pills, two distinct signals — operator-decided portal-only, no admin sidebar widget added). All four signals (follow-up, notes, status, tag colour) save via the same debounced 1-second auto-save. **Existing-rows backfill SQL ran on Render shell (operator-verified):** all 7 existing prospects' viewed timestamps set to their added_at, so they correctly DON'T show as NEW after deploy. Status stayed 'new' for all 7 (honest — operator can't remember which existing rows had been contacted; they set as they go). **Confirmation points to verify in next chat:** (1) change a status to Contacted in the admin modal, save indicator → "✓ Saved", close + reopen, status persists; (2) pick a tag colour, confirm row pill background changes on next list refresh; (3) on the portal side, add a new prospect somewhere, confirm green unread count appears on the sidebar Hot Prospects nav; (4) open the new prospect, confirm both NEW pill removal AND unread count decrement.

-9. **FORMSPREE THREAD SOURCE-PINNING — SHIPPED 2026-05-21 sixth session (decision #94), CONFIRMED WORKING IN PRODUCTION.** Formspree-flagged Hot Prospects now show the source form submission in their Email History panel, even though the source row's `from_address` is `noreply@formspree.io` (doesn't match the prospect's actual email). Wired via new `hot_prospects.source_reply_id` column populated by the IMAP poller for auto-flagged Formspree leads; admin POST + portal POST also accept it now (the frontend was always sending it — the column just didn't exist, decision #94 lesson #73 in action). Both thread endpoints (admin + portal) now pull the pinned reply in addition to address-matched ones, dedup + merge. **Backfill SQL ran on Render shell** to retroactively pair 4 existing auto-flagged Formspree prospects: Mr Ian mccreery, Rhiannon Phenis, Westley, Craig — all confirmed correctly paired. Mr Ian mccreery's modal now shows the COSMA EXPRESS RANGE catalogue download form contents (operator-verified). Source row appears IDENTICAL to a regular inbound reply in the thread per Q1=(i) operator choice — so it shows as "Formspree <noreply@formspree.io>" not as the submitter. Operator accepted this trade-off explicitly.

-8. **INBOX BADGE PARITY — SHIPPED 2026-05-20 fifth session (decision #92), DEPLOY WAS BLOCKED until 2026-05-21, NOW CONFIRMED WORKING IN PRODUCTION.** Customer portal mirrors admin labels on the inbox classification badge with two narrowly-justified divergences (non-campaign mail → "Normal email"; null → "Classifying…"). Campaign-matched rows on BOTH surfaces show `Campaign · New prospect` / `Campaign · Soft negative` / etc. Formspree website-form leads (catalogue download / website enquiry / contact form / form submission / new lead / new enquiry, sender `@formspree.io`) get **"Website Prospect"** in green on BOTH surfaces. Auto-flag to Hot Prospects (decision #89/#93) unchanged. **Blueprint correction this session:** lesson #69's framing ("the admin/portal label divergence is by design") was inaccurate — that divergence was never operator-locked. Decision #92 + lessons #71 and #72 record the correction.

-7. **FORMSPREE PARSER POLISH — SHIPPED 2026-05-20 fifth session (decision #93), DEPLOY WAS BLOCKED until 2026-05-21, NOW CONFIRMED WORKING IN PRODUCTION.** Three-part fix: (a) two-line label/value parser; (b) bare-email fallback removed (labelled-only — precision over recall, lesson #68); (c) `isOwnAddress` second-defence filter (skip if prospect's email matches a connected `email_inboxes.email_address` OR a domain the customer sends from). Wired into live poller AND backfill script. 5 junk rows already cleaned via Render-shell SQL. Operator verified end-to-end after deploy went through. **Caveat:** parser is labelled-only — if new templates surface without recognised labels, pull a body via Render-shell SELECT before extending label support; don't guess.

-6. **PORTAL EMAIL CAMPAIGNS FRONTEND — SHIPPED 2026-05-20 fourth session (decision #88).** Full design from #87 live in `PortalApp.jsx`. Cube6's portal operator-verified working. **Per-send `tracked` write path STILL NOT VERIFIED** — Cube6's existing campaign was sent before the migration so `tracked=0` on all 369 rows, correctly suppressed via `tracking_split_available=false`. Confirmation point: when step 3 of Cube6's drip fires, new rows should be written with `tracked=1` where smart mode decides to track. Watch for "of N tracked" sub-labels on Opens/Clicks columns. If `tracked` still stays 0 after step 3 fires, `services/ses.js` isn't writing the column.

-5. **FORMSPREE AUTO-FLAG TO HOT PROSPECTS — SHIPPED 2026-05-20 fourth session (decision #89), POLISH SHIPPED via #93 + #94 in fifth/sixth sessions, fully confirmed working.** Original auto-flag landed 5 junk rows because of the bare-email fallback grabbing `mailbox:` field. Polish in #93 tightened parser to labelled-only with two-line support + own-mailbox/domain filter. #94 then wired source-reply pinning so the thread renders the original form contents. Both fully confirmed end-to-end.

-4. **REPLY-MATCHING THREADING BUG FIXED — 2026-05-20 fourth session (decision #90).** Latent since Phase 3.1: inbound replies carry `<...@eu-north-1.amazonses.com>` in `In-Reply-To` but `email_sends.message_id` stores the bare local id without `@domain`. Matcher was stripping `<>` but not the `@domain` suffix, so every threading lookup failed. Diagnostic confirmed via Render shell SQL (10 of 10 SAMPLE replies had headers present but `matched_camp=0`). Three-shot lookup helper now strips `@domain` → exact → LIKE-prefix. Sender-fallback also now stamps `matched_campaign_id` from the subscriber's most recent send. **Backfill SQL ran on Render shell to retroactively fix the 10 SAMPLE rows** — confirmed working. **BROADER BACKFILL OPPORTUNITY NOT YET DONE:** the SAMPLE-only backfill restricted to `subject LIKE '%SAMPLE%' OR '%your Services%'`. Every campaign reply ever sent that came back as an auto-reply landed in `email_replies` but never matched — so historical campaign reply counts across the entire deployment are understated. The same backfill SQL pattern applied to `WHERE in_reply_to IS NOT NULL AND matched_campaign_id IS NULL` (no subject filter) would repair every campaign retroactively. Operator-deferred — pick up only if/when admin reply counts come up as wrong-looking. **CONFIRMED WORKING IN PRODUCTION:** new replies threading correctly; SAMPLE campaign jumped from 0 → 10 replies in admin view; customer portal showing OOO badges instead of "Normal email" on the SAMPLE auto-replies.

-3. **FORMSPREE CLASSIFIER GATE — SHIPPED 2026-05-20 fourth session (decision #91).** Haiku was labelling Formspree form-submission bodies as `hard_negative` (interpreting "submitted your form" as outreach sentiment). Pre-pass in `classify-replies.js` now skips Haiku and sets neutral for any `@formspree.io` sender. EDP Formspree row cleaned up via Render-shell UPDATE. **CONFIRMED WORKING IN PRODUCTION.** Belt-and-braces against an edge case where Formspree mail could otherwise get pulled into an inappropriate auto-unsubscribe via the sender-fallback in #90.

-2. **PORTAL EMAIL CAMPAIGNS REDESIGN BACKEND — SHIPPED 2026-05-20 third session (decision #87).** All three backend files (`db.js`, `services/ses.js`, `routes/portal.js`) deployed and operator-verified: migration ran clean, portal still loads identically, new fields present in API response. **Per-send `tracked` write path verified end-to-end is still pending** — see priority -6 above for the test plan once Cube6's step 3 fires.

-1. **CRM LINKED-CUSTOMERS FIX + FOLLOW-UP ALERTS + MARK-AS-CONVERTED — shipped 2026-05-20 second session.** Two cohesive features in one session. **#85 (linked-customers fix)** is a read-side fix for a latent bug in the original CRM build (#76): hot prospects flagged from a linked inbox row now show up in the portal anchor's portal — was completely invisible before. Admin switcher hides the linked inbox row; its prospects roll into the anchor's card with aggregated count. New Inbox column on the prospect list appears only when the customer has linked inboxes. Portal writes against the inbox-owning row, matching admin. **CONFIRMED WORKING IN PRODUCTION (operator-verified, all 5 verification points).** **#86 (follow-up alerts + Mark-as-converted)** adds: red urgent badge on both portals, top-of-CRM panel, list ORDER BY moved to server, "Converted (N)" divider with green pill on converted rows, "✓ Mark as converted" button + reversible "Reopen as active", `closed_at`/`closed_by` columns + partial index, new `/due-counts` `/mark-converted` `/reopen` endpoints. Cross-component event keeps both sidebars in sync. **#86 STILL PENDING OPERATOR VERIFICATION** — see verification list under priority 2 below.

0. **CRM HOT PROSPECTS — full feature shipped 2026-05-20, ALL 5 steps deployed and confirmed working in production.** Decisions #76–84. ALL OF #71–#75 also confirmed working (operator-verified 2026-05-19). Treat both clusters as solid, deployed, working. **Cleanup now safe (do when convenient, low priority):** `deployToDrafts` in `campaigns.js` is dead code (kept as a fallback until #72 was confirmed; can be removed when convenient — re-grep for callers first).

1. **PRIORITY ONE (functional bug, still open) — the text-only Manson LinkedIn campaign JSON-parse failure (`Claude did not return valid JSON after 2 attempts`).** UNCHANGED across multiple sessions now — has been deprioritised three sessions in a row. Symptom: campaign UI shows "Campaign failed" at ~5% ("Writing posts"). Root cause is confirmed (not guessed): the same stray-token hallucination family as decision #63, but the larger new weprintcatalogues.com RAG produces longer 12-post JSON and the stray token is landing in a placement the #63 repair regex doesn't catch. It is **input-stable** — same big RAG + Manson prompt reliably reproduces it on BOTH attempts, so the automatic retry produces the same broken output and re-running will NOT help. The 12 posts are generated fully and correctly (content is on-brand for the new RAG); only the JSON envelope fails. **Note:** the JSON log the operator supplied previously showed the failure is in `regenerateSinglePost` (a "Refine my posts" rewrite of post 10), and the visible JSON looked structurally clean with literal unescaped `"` quotes inside the post text (`he'd "check with the factory"`) — pointing at the **stray-word-inside-a-string / unescaped-quote** case (escaping-repair pass), NOT the stray-token-between-tokens case. Still get the full `generatePosts` line-497 log if the bulk path also fails, but the escaping-repair direction is now the leading hypothesis for the regen path. **Do this in strict order:**
   - **(a) DO NOT build the fix from the thrown error.** The thrown `Error` only contains the first 1000 chars (clean JSON, then a cut-off) — that is NOT enough to locate the stray token. The `console.warn` at the parse-fail branch (~line 497 of `server/services/openai.js`) logs the ENTIRE raw response: `[claude] generatePosts attempt 2 — JSON parse failed. Raw response (NNNNN chars):`. Get that FULL line from Render first. This is the project's oldest working principle — measure before tuning (lesson #18, lesson #63).
   - **(b) The fix differs by placement.** Stray bare word *between* JSON tokens (e.g. `"Text Post"Menu, "suggested_day"`) → extend `tryParseClaudeJSON` (~line 43) with a repair pass that strips bare alphabetic tokens sitting outside string values. Stray word *inside* a string value breaking escaping → a different fix entirely (an escaping-repair pass). Identify which from the full log before writing anything.
   - **(c) Practical unblock for Manson in the meantime (NOT a real fix).** Because the failure is input-stable, re-running won't help — but the anomaly correlates with output length. Either trim the Manson RAG slightly OR generate fewer posts per batch (e.g. 6 instead of 12). Shorter output makes the anomaly far less likely to trigger and gets Manson's campaign out the door while the proper `tryParseClaudeJSON` fix is built. Relevant constants: `callClaudeForPosts` ~line 413, `max_tokens: 64000`, `POSTS_PER_CAMPAIGN = 12` (~line 28 of `openai.js`).

1. **Read this whole blueprint.** The decisions list now goes up to #98 (2026-05-22 eighth session added #98 — per-campaign unsubscribe panel + drip-ticker gate). The lessons list goes up to #77 (sixth session added #73 accept-and-discard schema gaps, #74 design pushback before code, #75 NOT-NULL-DEFAULT migrations on populated tables need a paired honesty think-through, #76 Render spend cap silently parks deploys; seventh session added #77 in-memory SQL smoke-testing for many-table JOINs catches silent zero/empty/false-negative bugs the operator can't see). Decisions #38 and #39 are explicitly marked superseded — read #54 and #42 (final form) for the live behaviour. **Lesson #69 is also superseded** — original text preserved with strikethrough; the corrected guidance is in lessons #71/#72. Decision #61's customer-visible validation warning is now hidden-but-intact (decision #68) — the server-side half still runs; only the UI is suppressed by operator choice. Decision #64 deliberately steps off #42's always-white-panel rule via an operator-controlled per-customer setting (see lesson #60 for why that's allowed without relitigating #42's reasoning). Decision #78 deliberately broadens the customer filter for the CRM compared to Email Campaigns (#57) — same table, different audience, different filter. Decision #85's `resolveLinkedSet()` is the canonical way to ask "which email_clients rows belong to this real customer?" — any future feature reading or filtering against `email_client_id` should use it rather than re-implementing single-id ownership (see lesson #64). Decision #87 added `email_sends.tracked` as the canonical per-recipient tracking-applied flag — any future feature reading "was this send tracked?" should read that column rather than recomputing against historical touch counts. Decision #87 also locks the UX choices for the portal Email Campaigns redesign — inline accordion, drip tabs matching admin, dual tracking surface, dismissable amber explainer banner. Decision #92 locks "admin and portal classification badges show the SAME labels by default" — any future inbox-label change should mirror to both surfaces unless there's an explicit operator decision to diverge (lesson #71). Decision #94 added `hot_prospects.source_reply_id` as the canonical pin for "which `email_replies` row created this prospect" — any future thread-display feature should pull this row in addition to address-matched ones (see lesson #73 on accept-and-discard). Decision #95 locks the pipeline status enum (`new` / `contacted` / `no_response`) + bounded tag-colour palette (8 colours) + per-side `admin_first_viewed_at` / `portal_first_viewed_at` viewed tracking — any future Hot Prospects feature touching status/colour/viewed-state should respect the enum and palette (the picker UI is in both `CrmHotProspects.jsx` and `customer-portal/PortalApp.jsx`, kept in sync). Decision #98 added two new tables (`campaign_unsubscribes` per-campaign + `contact_unsubscribed_all` master) for the per-campaign unsubscribe panel; canonical helpers are `buildSubscriptionsPanel` + `applySubscriptionsUpdate` exported from `hot-prospects.js` — any future feature that needs the panel state or applies a panel update should reuse them, not reimplement. Drip-ticker gate at `services/drip-ticker.js` is now the canonical place where unsubscribe state is enforced before sending — any future stop-condition (positive reply halt, soft-negative, etc) should add to the gate there, not bypass it. Don't trust any "20px padding" or "always white panel" mention without checking it's not from a stale comment block.

2. **Files Wez pushed at the end of the 2026-05-20 second session, the 2026-05-12 AND 2026-05-19 sessions that are awaiting end-to-end verification in production.** The files were all parse-checked and logically reviewed but real-world confirmation is still pending for several. Ask Wez which ones he tested and what worked:

   **2026-05-20 third session pending verification (cluster G — portal Campaigns backend, #87):**
   - **Migration ran clean** (`db.js`). Render boot log should show `[db] migration: added tracked to email_sends` ONCE on first boot after deploy, never again. **Operator-confirmed: appeared once on first boot.**
   - **Portal still loads visually identically** — Cube6's portal Campaigns page should look 100% the same as before (frontend unchanged this session). **Operator-confirmed: looks identical.**
   - **New fields in API response** — DevTools Network on the Campaigns page, click `/api/portal/campaigns` → Response. Should see new fields per campaign: `is_drip`, `steps_total`, `steps_sent`, `drip_status`, `tracking_mode`, `tracking_split_available`, `tracked`, `untracked`, plus a `steps[]` array with per-step rollup. **Operator-confirmed: all present.**
   - **Per-send tracked write path** — NOT YET CONFIRMED end-to-end. Needs one real campaign with `tracking_mode='smart'` to be sent after the deploy, then check the `/api/portal/campaigns` response shows non-zero `tracked` and `untracked` numbers for that campaign. Until this is verified, we can't be 100% sure ses.js is writing the flag correctly — though the code path is straightforward and parse-checked clean, and Cube6's "Cube 1 campaign (test)" was tracking_mode='off' so its zero-tracked count is correct-by-design and not informative.
   - **New detail endpoint** — `GET /api/portal/campaigns/:id`. Paste a campaign id into the URL while logged into the portal, should return a payload with `steps[].html_body` + `replies_list[]`. 404 on cross-tenant ids. Not blocking the frontend build — operator can confirm during the frontend session.

   **2026-05-20 second session pending verification (cluster F — follow-up alerts + Mark-as-converted, #86):**
   - **Sidebar urgent badge — admin** (`Sidebar.jsx`). Red number pill on "Hot Prospects" SubItem in the admin sidebar when there are overdue + due-today prospects across all customers. Refreshes every 30s. Click into CRM and mark something converted → badge ticks down within a second (in-app event, no waiting for next poll). Confirm: number disappears when all converted; count = sum across all customers (NOT scoped to a single customer card).
   - **Sidebar urgent badge — customer portal** (`PortalApp.jsx`'s `PortalChrome`). Same red pill on the portal's "Hot Prospects" NavItem. Scoped to the signed-in customer's linked set (single-tenant). Confirm: count is the customer's own, not 0 just because nothing is admin-flagged.
   - **Top-of-CRM follow-up panel — both portals** (`CrmHotProspects.jsx` `FollowUpPanel`, `PortalApp.jsx` `PortalFollowUpPanel`). Red panel when anything overdue, amber when only-today, hidden entirely when nothing urgent. Wording: `<N> follow-up(s) need(s) attention. <X> overdue, <Y> due today.` Confirm it disappears when you mark the last urgent prospect converted.
   - **List ordering** — server-side ORDER BY now puts overdue/today first, other active next, converted at the bottom under a "Converted (N)" divider. Confirm on Cube 6's CRM: open a prospect, set follow_up_date to yesterday → after the save indicator hits "Saved", reopen CRM → prospect should be at the top.
   - **"Converted (N)" divider + faint background + green pill** (`CrmHotProspects.jsx` `ProspectList` + `ProspectRow`, mirrored in `PortalApp.jsx`). Mark any prospect converted → it moves below the divider, gets a `#f9faf7` tinted background, and the row's name gets an inline green "✓ Converted" pill. Divider hidden when zero converted (so the list looks the same as before on customers that haven't marked anyone yet).
   - **"Mark as converted" + "Reopen as active" buttons** (both portal detail modals). Click Mark → no confirm dialog → modal swaps: button becomes outlined-green "Reopen as active", green banner appears at top showing `✓ Converted on <date+time> by admin` (or `by you` on the customer portal). Click Reopen → reverses cleanly, banner goes away, urgent badge ticks back up if the date is past. Confirm the banner says "by you" on the customer portal when the customer themselves marked it.
   - **Per-customer switcher count also drops** — Cube 6's badge in the CRM switcher should reflect the same active-only count (server-side `closed_at IS NULL` filter on prospect_count subquery).
   - **Idempotency** — clicking Mark twice fast does not overwrite the original closed_at/closed_by (server COALESCE). Visible as: refresh page → banner still shows the FIRST click's timestamp, not the second.
   - **NEW MIGRATION on first boot** — `db.js` adds `closed_at` and `closed_by` columns to `hot_prospects` via ALTER pattern, plus partial index `idx_hot_prospects_closed_at`. Check Render boot logs for any `[db]` error lines on first deploy.

   **2026-05-20 first session was fully verified by the operator (cluster E was the linked-customers fix, #85) — no items here, treat #85 as solid.**

   **2026-05-19 session pending verification (cluster E — verify these first, they all touch Manson):**
   - **Per-customer Brand panel (#64)** — `db.js`, `routes/clients.js`, `gemini.js`, `ClientDetail.jsx`. Admin LinkedIn customer "Overview" tab, Supergrow card, "Brand panel" section: three dropdowns (Position / Background / Size), 1-second debounced auto-save, "Saving… / ✓ Saved" indicator. **Operator-verified working** (earlier "didn't save" reports were navigating away before the debounce — see lesson #61). Confirm the three new `clients` columns migrated (`logo_position`/`logo_panel`/`logo_size`) and pre-existing customers are visually unchanged.
   - **Per-post logo controls + recomposite (#65)** — `gemini.js`, `routes/portal.js`, `routes/campaigns.js`, `PortalApp.jsx`. Customer portal post card: 3-column Position/Size/Background dropdown row above the four green buttons. Click a dropdown on a post WITH a stored pre-logo image → "Updating logo… Repositioning logo — no charge." → logo moves, no AI cost, no regen-cap hit. Posts generated before this shipped show the row disabled with "Click New image to enable" and the endpoint returns `400 {error:'pre_logo_unavailable'}`. Confirm dropdowns initialise from the customer-level defaults; confirm any full regen resets the three per-post overrides to null.
   - **No-AI-brand-mark Gemini prompt (#66)** — `gemini.js`. Generate a fresh Manson image → confirm NO AI-drawn fake "MANSON" wordmark anywhere (only the real composited logo). Compliance is probabilistic; an occasional slip is a Gemini quirk, not a rule error.
   - **AI output sanitiser (#67)** — `openai.js`. Watch Render logs over a few campaigns for `[sanitize] stripped N non-Latin char(s) …` lines. Occasional = working as intended. Frequent = the contamination is widespread and worth deeper investigation (same anomaly family as the priority-0 bug).
   - **Hidden validation warning (#68)** — `PortalApp.jsx`. Confirm the yellow "may conflict with one of your rules" box no longer appears in the customer portal, but Render logs still show the validation pass running and `validation_warnings` still stored/audited server-side. Re-enable is a one-word change (`{false &&` → remove).
   - **Auto-grow refine-rule textareas (#69)** — `PortalApp.jsx`. Open "✦ Refine my posts", add many/long rules → each textarea grows to fit its content; a single outer scrollbar engages once the list passes the modal's 80vh (not one scrollbar per row).
   - **Brand-panel dropdown label cleanup (#70)** — `ClientDetail.jsx`. Cosmetic; confirm "No panel" / "Small" / "Large (≈ 2× small)" labels read correctly and don't imply a saved value.

   **Earlier fixes from the 2026-05-12 session (cluster A):**
   - **`+ New client` button removal + Mansons SQL fix (#57)** — Email Campaigns customer list should not show Mansons. Mansons portal at `/c/<slug>` still works (only `source` flipped to `'portal'`, anchor row stayed). Wez confirmed working at the time.
   - **Campaign log no longer auto-scrolls (#58)** — Click "New image" on any post → page stays put.

   **Earlier fixes from the same session (cluster A):**
   - **`+ New client` button removal + Mansons SQL fix (#57)** — Email Campaigns customer list should not show Mansons. Mansons portal at `/c/<slug>` still works (only `source` flipped to `'portal'`, anchor row stayed). Wez confirmed working at the time.
   - **Campaign log no longer auto-scrolls (#58)** — Click "New image" on any post → page stays put.

   **Mid-session integrations (cluster B):**
   - **IDYQ admin embed (#59)** — 3 env vars across both services + 1 SQL on IDYQ + 5 files on Studio + 1 file on IDYQ. Click `App > IDYQ` in Studio sidebar → should land on IDYQ admin panel without a login prompt. The four distinct failure-mode signatures are documented in #59 so debugging is straightforward.
   - **Admin reset-password modal (#60)** — Click Reset password on any portal user → modal with password input + Show/Hide toggle + "Generate random instead" button. Min 8 chars matches portal signup.

   **Big feature from this session (cluster C):**
   - **Refine my posts (#61)** — 5 files (db.js, content-rules.js, openai.js, portal.js, PortalApp.jsx). Open any LinkedIn-enabled customer's portal. "✦ Refine my posts" button in header next to logo. Click → modal with numbered rules, add/edit/delete, Save. Add a rule like "Don't mention machinery breakdowns" → click Rewrite post → confirm rewrite doesn't violate. Render logs should show `Validating posts against your refine-my-posts rules…` and either `✓ All posts pass` or `⚠ N post(s) flagged after validation`. Flagged posts show an amber card with rule text + reason.

   **Late-session fixes triggered by the Manson Group upload (cluster D):**
   - **Dark-background logo detection (#62)** — `logo-prep.js`. Re-upload The Manson Group logo via admin Brand logo button → should arrive in customer portal and admin preview at its full original aspect ratio with "GROUP" subtitle intact. Render logs should show `[logo-prep] Dark background detected (median corner luminance N < 120) — skipping trim, preserving original.` Any future dark-background customer (military, luxury, security branding) will also benefit automatically.
   - **Robust JSON parsing + retry (#63)** — `openai.js`. Re-run the failed Manson campaign generation. Should succeed first time because the repair regex handles the `Menu`-style stray token. If a different failure type appears, the FULL raw response is now logged on parse failure (decision #63) so the next chat can see exactly what the model produced and tune the repair regex if needed.

   **Older still-unconfirmed from previous sessions:** logo backfill on first deploy (`[logo-backfill] Done. Succeeded=N` line in logs); proportional padding visual on Tower Leasing vs Cube6; Gemini halo prompt fix on Cube6; auto-unsubscribe gate (no rogue `[classifier] auto-unsubscribed` lines for non-campaign senders).

3. **Stop-condition filter — UNSUBSCRIBE HALF NOW CLOSED via decision #98 (2026-05-22).** The drip-ticker gate now respects `campaign_unsubscribes` + `contact_unsubscribed_all` before every send (step 1, follow-up, AND still-pending check — all three gated). The positive-reply halt has been in place since Phase 3. **What's still pending:** soft_negative + hard_negative classifications don't currently halt the drip — only `positive` does. Operator hasn't asked for this and there's a reasonable argument that soft_negative ("not interested yet, try again") MIGHT want to receive follow-ups; hard_negative typically auto-unsubscribes via decision #52's gate so the subscriber is already filtered out by `status='subscribed'`. So this is more of a design question (which negatives should halt?) than a missing fix. Don't pick up unless the operator surfaces it.

4. **End-to-end verification of inbox reply-send threading.** Code shipped in chunk 3b-ii but no real reply has been verified to land back in Gmail/Outlook with proper `In-Reply-To` and `References` headers. Worth a `Show original` test next chat.

5. **Domain reputation / list verification.** During the 2026-05-12 session Wez asked about protecting domain reputation when sending to bought lists. Discussed at length — proposed priority order: (1) pre-send list verification via ZeroBounce/NeverBounce/Kickbox API at import time, (2) engagement-based suppression (auto-flag subscribers with zero opens after step 3), (3) Google Postmaster Tools enrolment + Domain Health page hardening, (4) cold-send subdomain strategy, (5) warm-up logic for new customer domains, (6) GlockApps inbox-placement testing. Nothing built yet — Wez deferred. Pick up if Wez asks; biggest single win is item 1 (list verification).

6. **Dark-background logos — the "Option 3" deferral was PICKED UP this session (decision #64), but only the operator-controlled half.** #62 preserves dark-bg logos; #64 now lets the operator choose `logo_panel = 'none'` per customer so a dark logo with its own background lands without a fighting white panel. What's still NOT built: automatic detection of the logo's own background colour to paint a *matching-colour* panel (rather than white-or-none). That fully-automatic colour-matching path remains deferred and should not be picked up without a real customer ask — and #39/#42's "no per-render heuristic" reasoning still applies if it ever is (a detected-and-painted colour is exactly the kind of per-render branching those decisions warned about; prefer extending the stored-setting model from #64 instead).

7. **Smaller backlog items** (see "What's NOT done" §2 and §7) are polish — don't pick those up before bigger pieces unless Wez asks.

8. **Operating note on the dead `services/claude.js` file.** It's no longer imported anywhere (confirmed via grep during #61 work). Leaving it in place because changing dead code only introduces risk, but if you're ever asked to "remove unused services," it's a safe deletion target — just re-run the grep first to confirm no new caller has appeared.

---

## Final word

The user is patient, curious, and a great collaborator. They put genuine effort into testing each phase and report back with screenshots + Render logs + browser console output. They want this to be a real product they could one day sell to other companies. Treat the project that way — quality matters more than speed. When in doubt, ask.

— Claude (end-of-chat handoff after the 2026-06-08 NINTH session: shipped ONE small feature + configured a long-standing AWS gap + locked in three deferred future-feature stubs. (1) "Remove from list" soft-hide for the Email Campaigns customer list (decision #99) — operator wanted a way to delete customer rows orphaned from AWS (decision #57 only adds, never removes). Honest design conversation: operator first chose cascade-delete, then changed mind to soft-hide ("in case the customer comes back to us several months later"). Soft-hide via new `hidden_at` column on `email_clients`; `GET /clients` filters; `DELETE /clients/:id` keeps URL compatibility but BEHAVIOUR is now soft-hide. Auto-unhide via case-insensitive name match in `POST /clients` — AWS re-verification of the same domain auto-restores the row with all history. Red "Remove from list" button bottom-left of Edit Client modal, separated from Save/Cancel. Three files shipped (db.js + email.js + EmailSection.jsx), all parse-checked, 8-scenario Python+SQLite smoke test passed before deploy. Operator verified `mail.idoyourquotes.com` removal worked. (2) SES SNS bounce/complaint wiring (decision #100) — investigated why `email_sns_events` was empty across 9,763 historical sends despite Studio's Phase-2 handlers being correctly coded. Root cause: SES per-identity Notifications never wired to existing `bounces`/`complaints` SNS topics (topics existed for Sendy; only Sendy was subscribed). Pushed back HARD against polling Gmail Spam folder as a workaround — SNS is the right mechanism, never IMAP. Probed AWS state via operator screenshots BEFORE prescribing actions (lesson #79). Configured: added Studio HTTPS subscriptions to both topics (auto-confirmed instantly); wired `mail.engineersolutions.co.uk` Notifications tab (Bounce → bounces, Complaint → complaints, Delivery deliberately OFF). End-to-end tested with `bounce@simulator.amazonses.com`; SNS event landed in `email_sns_events` in <3s. NO code shipped — pure configuration unlock. Operator action OUTSTANDING: repeat per-identity wiring on the other 13 domains (~90s each, ~20 min total, no per-domain test needed). (3) Three deferred stubs locked in (decision #101): (a) reply-count over-counting in campaign report (one-line SQL fix at `email.js` ~line 1609 — add `AND r.matched_campaign_id = es.campaign_id` to the JOIN; will reduce reported reply numbers visibly when shipped — the over-count is real). (b) Click-tracking inflated by corporate mail security scanners (diagnosed via Wil@sinclair-intl.com 36-click case — mostly bot pre-fetches; four mitigation options sketched; deferred unless operator surfaces again; consider adding ip_address + user_agent columns to `email_link_clicks` first). (c) Bounce-rate auto-pause campaign protection (AWS already auto-pauses at ~10% but Studio could pause earlier; prerequisite is decision #100's wiring done across all identities — otherwise blind spots). Two lessons added: #78 (test sends bypass `email_sends` so "test to bouncing address" only proves SNS delivery, not consequence-application — the operator trusted SNS delivery as sufficient proof in this case); #79 (always probe what's already wired before prescribing — the AWS screenshot revealed Sendy had already created the SNS topics, saved a full topic-creation round). PARALLEL CONVERSATION STARTED THEN PARKED: operator wants to move clearerpaths.co.uk + future TGA marketing domains to a separate AWS account for reputation isolation. New "TGA Domains" sidebar entry above "Customers" under Email Campaigns. Architectural plan agreed (add `aws_account` column to `email_clients`, second `TGA_AWS_*` env vars, parameterised `loadAll` per account, three-stage delivery). NOT shipped — operator paused to fix a different bug first (the "Carter" From-email issue which turned out to be the operator typing "Carter" in the Default From email field — diagnosed and explained; client-side email-format validation in the Edit Client modal proposed but not built). NOTES for next session: (a) Decision #99 confirmed working in production. (b) Decision #100 mail.engineersolutions confirmed working; OTHER 13 IDENTITIES STILL PENDING OPERATOR-IN-AWS-CONSOLE — priority 1 hygiene work for next session. (c) Decisions #101a/b/c deferred — don't pre-act. (d) AWS-account split (TGA Domains) parked at "architectural plan agreed, no code" — pick up when operator surfaces it. (e) Previous open verifications still carry: #95 status/tag/viewed round-trip (sixth session), tracked write path verification when Cube6 step 3 fires (#87/#88), broader email_replies backfill (#90 — operator-deferred), Manson JSON-parse bug (priority #0, still operator-deprioritised). (f) The "remove from list" we just shipped opens the question of consolidating with #97's list-level controls — still don't pre-decide; let operator surface friction. Previous handoff (2026-05-22 EIGHTH session) summary: shipped per-campaign unsubscribe panel + drip-ticker gate (decision #98) — eight files, two new tables (campaign_unsubscribes per-campaign + contact_unsubscribed_all master), four endpoint pairs across admin + portal × replies + hot-prospects, panel component duplicated three times across EmailSection.jsx + CrmHotProspects.jsx + PortalApp.jsx. Drip-ticker gate closes blueprint backlog priority 3 (three NOT EXISTS clauses on step-1, follow-up, AND still-pending queries). Auto-tick on Hot Prospect add via source_reply_id → matched_campaign_id (additive — doesn't retroactively tick existing prospects). buildSubscriptionsPanel + applySubscriptionsUpdate helpers exported from hot-prospects.js for reuse. Seven-scenario smoke test passed (Cube6 cross-side, Manson self-linked, auto-tick, untick, master writes/reads, INSERT OR IGNORE dedup, cross-customer security rejection). CONFIRMED WORKING IN PRODUCTION across all four surfaces (admin inbox, admin Hot Prospect, portal inbox, portal Hot Prospect). Open coexistence question with #97 not decided — operator can live with both. Previous handoff (2026-05-21 SEVENTH session: shipped TWO meaningful features, both built as one connected operator ask. (1) Inbox row action-taken badges (decision #96) — every email row in the inbox list AND in the open-email modal now shows up to two new secondary pills next to the existing classification badge: amber clickable "On Hot Prospects list" (jumps to CRM with that prospect's detail modal auto-opened) and grey non-clickable "Unsubscribed". Same labels on admin AND customer portal per #92's parity rule. Linked-set scoped for BOTH lookups — critical for Cube6's [Cube6, mail.eng] etc where mailing lists actually live under the anchor not the inbox row. Initial cut scoped only to inbox row's email_client_id, caught pre-deploy by in-memory Python SQLite smoke test (see lesson #77). Fix uses `resolveLinkedSet` everywhere. No backfill, no migration — both signals computed live at read-time. CONFIRMED WORKING IN PRODUCTION (operator-verified 2026-05-21). (2) Unsubscribe + Re-subscribe everywhere they make sense (decision #97). Previous handoff (2026-05-21 SIXTH session): shipped Formspree thread source-pinning (#94) + Hot Prospects pipeline status + tag colour + per-side viewed tracking (#95). Lessons #73–#76 added. Previous handoff (2026-05-20 FIFTH session): shipped Formspree parser polish (#93) + inbox badge parity (#92). Lessons #67–#72 added. Previous handoff (2026-05-20 FOURTH session): shipped four things — portal Email Campaigns frontend rebuild (#88), Formspree auto-flag (#89), reply-matching threading bug (#90), Formspree classifier gate (#91). Previous handoff (2026-05-20 THIRD session): customer-portal Email Campaigns redesign backend (#87). Previous handoff (2026-05-20 SECOND session): CRM linked-customers fix (#85) + follow-up alerts + Mark-as-converted (#86). Previous handoff (2026-05-20 FIRST session): CRM "Hot Prospects" feature built end-to-end in 5 steps (#76–84). 2026-05-19 session shipped #71–75. 2026-05-12 session shipped #57–63.)
